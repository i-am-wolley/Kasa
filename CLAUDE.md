# Kasa — session memory

Kasa is a household operating system — a model of a house that tracks what depletes (Stock), what recurs (Cycles/Routines), and what ages (Assets), and warns before anything fails. Sibling app to Miso (kitchen, `../Kitchen/Pantry-OS-App`), sharing the Nature Distilled design language but a separate deployment. Full product spec: `Base Resources/KASA_BUILD_MEMO.md` (737 lines — read it, don't guess from this summary). **This file is build-state truth; the memo is product truth.** Build plan: `C:\Users\Vinod\.claude\plans\foamy-kindling-scone.md`.

## Locked decisions (confirmed with user, deviate from the memo's literal defaults)

| Decision | Why |
|---|---|
| **UI-first, mock data before Firebase** — not the memo's §12 literal order (Firebase → engine → first screen) | Same pattern as sibling app Mina: visible progress every session. `mock-data/` fixtures match the real entity shapes exactly so wiring Firebase later is a data-source swap, not a rewrite. |
| **Mobile AND tablet simultaneous first-class targets**, desktop (`>1024px` three-pane) deferred | User's explicit call — Kasa's core interaction (swipe done/snooze while standing in a room) is a phone/tablet-in-hand use case, unlike Mina's desktop-review app. |
| **New, separate Firebase project** (not folded into Miso's `pantry-os-prod`) — not started yet, Phase 4 | User confirmed cross-project Miso→Kasa data flow is possible later via either a second client-side `FirebaseApp` instance or a Cloud Function with cross-project IAM. `users/{uid}` should carry `email` from day one so a future linking step has something to match on (Auth UIDs are project-scoped, won't match across projects). |
| **Design tokens pulled from the real `miso-design` skill**, not memo §9.1's fallback hexes | The memo itself says its §9.1 values are "fallback if extraction is incomplete" — extraction is not incomplete (`~/.claude/skills/miso-design/tokens/`). Ground/ink/accent in `tokens.css` are real Miso values; earth secondaries + consequence tiers (§9.2) are Kasa-original, kept as authored. |
| **Bottom tab bar at every breakpoint up to real desktop** — not memo §8.1's literal "bottom tabs (phone) / sidebar (tablet+)", effective 2026-08-03 | User's call: tablet apps that keep bottom tabs (Instagram/X on iPad) read as more consistent than switching to a rail, and it stays thumb-reachable for Kasa's in-hand swipe interactions. A left sidebar is deferred to the true desktop pass (Phase 7, `>1024px`) rather than appearing at 600px. |
| **Type is Geist + Geist Mono + Fraunces italic**, NOT Playfair Display SC + Karla | Real Miso ships Geist/Geist Mono with Fraunces italic reserved for its one special-occasion serif use (Gourmet Weekend). The memo's §9.1 fallback guessed Playfair+Karla — corrected per the memo's own "pull real values" instruction. Fraunces italic carries Kasa's "named things" treatment (routine titles, asset names) — see `.named` class in `app.css`. |
| **5th tab is Wishlist, not More** — not memo §8.1's literal tab list, effective 2026-08-04 | User's call: "More" (Assets/People/onboarding) moved to a small button next to the logo in the top-left header, freeing the 5th bottom-tab slot for Wishlist, a primary frequently-visited feature rather than a settings-style menu. Still exactly 5 tabs total (memo §8.1's own limit), just a different 5th. |
| **Habits are personal, separate from Routines** — new concept, not in the memo | User's explicit call, with an eye on a stated future direction: "in the future we will make available individual classification for routine and habit... within a household there can be household routine and individual routine." Routines stay household/space-scoped using the 6-trigger engine; Habits are person-scoped, no space, no trigger — just a daily done/not-done log, visualized as a 72-day square grid. Kept genuinely separate rather than bolted onto the routine engine, since routine due-dates and habit streaks are different enough concepts that forcing one model onto both would have compromised both. |
| **Phase 5 explicitly excludes §5.2 burn-rate learning and §5.3 adaptive intervals** — everything else in §5.4-§5.9 is built | User's explicit call, 2026-08-05: "never do 5.2, 5.3... rest all is nice." Both would need real longitudinal usage history to learn from (an EWMA burn rate, a median-of-last-5-completion-gaps interval) that a fresh household genuinely doesn't have yet — the user's call not to build them now stands regardless of that, but it's also why they'd have been the least demo-able of the six anyway. Revisit only if asked. |
| **A household can contain more than one House** — new layer above Space, not in the memo | User's explicit call, 2026-08-05: "a household can have more than one house, with its own spaces, routines, assets." Every space now carries a `houseId`; items/assets/routines inherit house scoping transitively through their own `spaceId` rather than each carrying a second foreign key. See Round 17. |
| **Schema-version migration mechanism** (`js/migrations.js`) — general-purpose, not a one-off backfill | User's explicit call, 2026-08-05: "when certain attributes are made and we might need to migrate old data, create a mechanism for version bump." Replaces the old ad-hoc per-field-backfill pattern (`withExpectedLife()`, `withCatalogLink()`, manual mock-data email adds) for future schema changes — see Round 17 for the mechanism and its first real migration. |
| **Google sign-in is mandatory** — no local-only/Skip path, effective 2026-08-06 | User's explicit call: "let's make it mandatory to login to use the app... then we can save the info and retrieve back." Apple and email-link auth are deliberately still not wired (Apple "later," email link "not needed") — Google only for now. See Round 18. |
| **Firestore: one `data/main` document per household**, not a fully granular collection-per-entity layout | Deliberate simplification, Round 18 — a household's data (a few dozen spaces/items/assets/routines) is nowhere near Firestore's 1MiB document ceiling, and this way none of `state.js`'s ~30 existing mutator functions needed to change at all; `db.js`'s `startAutoSave()` piggybacks on the same `subscribe()`/`notify()` signal every mutator already calls. Schema otherwise mirrors Miso's own `households/{code}` + `memberUids` pattern (`Pantry-OS-App/firestore.rules`). Revisit only if a household's data ever actually approaches the size ceiling. |
| **Onboarding creates spaces only** — no assets/stock/routines/tasks/habits, effective 2026-08-06 | User's explicit call: "allow options to add spaces... but let's not add any assets/routines/tasks/habits — doesn't make sense." The room-by-room "suggested for this space" flow (House's own Add Space) is still how assets/stock/routines get added, just never automatically during the six-question wizard itself anymore. See Round 18. |
| **Notifications are on-device/foreground-only for now**, not true background push | User asked directly whether push is possible. It is, but needs a server that can wake a device on a schedule — Firebase Cloud Messaging + a scheduled Cloud Function + upgrading the Firebase project to the Blaze billing plan, none of which exists yet. `js/notify.js` builds the honest client-only version (Notification API, checked while a tab is open) rather than silently claiming to be the real thing. Revisit once Phase 6's Cloud Function proxy exists anyway (AI features already need one). See Round 19. |
| **Real-time sync stays whole-document last-write-wins**, not a field-level merge | Deliberate, matching Round 18's own one-`data/main`-doc decision — a genuine per-field merge would need the granular collection layout that was explicitly avoided. `db.js`'s `onSnapshot` listener (Round 20) makes another device's changes show up live instead of only on next reload; it doesn't resolve two devices editing at the exact same moment. Revisit only if that turns out to matter in practice. |

## Build-plan phase status

| Phase | Status |
|---|---|
| 0 — Foundation (tokens, app shell, mock data, icon subset) | ✅ done |
| 1 — Today screen (engine, House Line, swipe done/snooze, effort filter) | ✅ done — first reviewable milestone |
| 2 — House, Stock, Assets, People screens, full CRUD | ✅ done |
| 3 — Content packs as real content, onboarding archetype flow | ✅ done |
| 4 — Firebase (Google auth, Firestore, rules) | ✅ done, 2026-08-06 (Round 18) — Google sign-in only (Apple/email-link/Storage deliberately not wired, see Locked decisions); Firestore persistence via a single `households/{code}/data/main` doc per household; `firestore.rules` mirrors Miso's own pattern. App Check not configured yet. |
| 5 — Intelligence | ✅ done except §5.2/§5.3 (explicitly excluded, see Locked decisions), 2026-08-05 — snooze learning, load balancing, batching, load smoothing, seasonal intelligence, correlation/forecasting all built in `js/intel.js`, see Round 14 |
| 6 — Insights (deterministic half only — health score, week trend, attention digest, help-on-leave impact, habits) | ✅ done, 2026-08-04 — AI half (photo sweep, bill scan, weekly AI digest) still blocked on Phase 4's Cloud Function proxy, see Round 10 |
| 7 — Remaining packs, PWA polish (`sw.js`, `manifest.json`), desktop three-pane, performance pass | packs + PWA polish ✅ done, 2026-08-05, see Round 14 — desktop three-pane deliberately deferred (user's explicit call this round), performance pass not started |

## Round 39 — confirmed weekly-overdue-carryover and start-date-edit semantics live, item quantities in space/stock tiles rounded to 2dp (added 2026-08-10, same session)

User asked two things: to re-confirm (with live proof, "u are 100% sure?") how a missed weekly routine behaves once a new week starts, and whether editing a routine clears its overdue state; and reported item quantities inside a space's Stock tiles were showing as long fractions, asked for 2dp only.

- **Confirmed live, not just by re-reading the code**: a missed weekly (`floating_since_last`) routine's occurrence is a persistent record — nothing about a new calendar week starting causes it to reset, move, or regenerate. `regenerate()` only ever creates a **new** occurrence for a routine with no open occurrence already; an existing open occurrence just sits there, and `stateOf()`/`overdueDays()` keep comparing it against today, so it reads `overdue` (climbing day count) indefinitely until completed. Verified with a real routine: forced an occurrence's `dueAt` 8 days into the past, confirmed `overdue`/8 days; called `updateRoutine()` with an unrelated field patch (not touching `startDate`) and confirmed the exact same occurrence, same `dueAt`, still `overdue`/8 days afterward (Round 37's fix holding). Then edited the **start date** specifically to a genuinely different date and confirmed that — and only that — does move it: the stale occurrence is dropped and a fresh one generated at the new date (`pending`, no longer overdue) — this is intentional, documented behavior (Round 21/23), not a bug. An edit to any other field never touches an existing occurrence at all.
- **Item quantities rounded to 2dp wherever shown as plain text** (`js/ui/components.js`, `js/routes/house.js`, `js/routes/stock.js`) — auto-depleting items accumulate long floating-point tails (e.g. `0.9384291452777778`) that were being printed raw in House's per-space Stock tile grid ("within the spaces," the user's own words — `house.js`'s `detailHtml()` had zero rounding, unlike Stock's own `stepper()` control which was already fixed for this in Round 10) and, less commonly, in Stock's own tile fallback caption (`stock.js`'s `tileHtml()`, the case where an item has neither a day/month rate nor a usage rate set). Extracted the existing local rounding logic already used by `stepper()` into a shared, exported `round2()` in `components.js` (display-only, same as before — stored values keep full precision) and applied it at both sites. `stock.js` already had its own separate `round2()` local (used extensively throughout its consumption-mode math) — reused that instead of importing the new one, to avoid a duplicate-declaration collision.
  - **Deliberately NOT applied to the item edit sheet's own Quantity number input** — rounding that pre-filled value would mean re-saving the item with any unrelated field changed (rename, space move, etc.) silently resubmits the *rounded* number as the new authoritative `qty`, which could trip the save handler's own `qtyChanged` threshold (`> 0.001`) purely from the rounding difference and incorrectly reset the auto-depletion checkpoint (the exact class of bug Round 23 already fixed once, for a different reason). The number input is also editable, not read-only display text, so a power user correcting stock can still see/type full precision there if they need to.
- Verified live end-to-end on the `xathurak@gmail.com` test account: a disposable test item seeded with qty `0.9384291452777778` showed as "0.94 roll" in both House's space-detail tile and Stock's own tile grid; zero console errors. Test item deleted after.

## Round 38 — wishlist checklist works while adding a new project (not just editing), per-sub-item cost auto-summed into project cost, routine start-date-today no longer skips to the next occurrence, date-input text left-aligned (added 2026-08-10, same session)

User follow-up, four items: the Wishlist project checklist's add-item controls (task/asset/item) only ever appeared once a project already existed to edit, not while adding a brand-new one; asked for a per-sub-item cost that automatically sums into the project's overall Estimated cost, with old projects backfilled to `cost: 0` on migration rather than left undefined; reported a routine with today's date as its start date was skipping straight to its next computed occurrence instead of occurring today first; asked for every date input's text to be left-aligned, not centered.

- **Checklist now works while adding a new project** (`js/routes/wishlist.js`) — `openWishSheet`'s `subItems` local (already the one working array for both add and edit mode, from an earlier round) is now shown by the checklist section regardless of whether `entry` exists; the section's gate changed from `entry && isProject` to plain `isProject` throughout (body markup, `newSubKind` wiring, the whole add/rewire/persist block). `persistSubItems()` only calls `updateWishlistItem` `if (entry)` — for a brand-new project it just keeps the Estimated cost field in sync and defers actual persistence to the final "Add idea" click, which now bundles `subItems` into `readWishFields()`'s returned object. The "Buy" sub-item flow (opens the real Assets/Stock add sheet) now branches: editing an existing project reopens `openWishSheet({ entry })` as before; adding a new one instead captures a `draft` snapshot (title/space/priority/notes/subItems) right before the nested sheet replaces `sheet-root`, and reopens `openWishSheet({ draft })` on return — so a title already typed, or sub-items already added, survive the round trip through a nested "Add asset"/"Add item" sheet.
  - **Real bug caught during live verification, not shipped**: the checklist section is only ever part of the sheet's *initial* `bodyHtml`, computed once from `isProject` at open time — for a genuinely fresh "Add idea" click, the default type is "asset" (`isProject` false), so the checklist markup was never in the DOM at all, and clicking the "Project" type chip afterward did nothing to reveal it (the existing handler only ever toggled the catalog-name vs. plain-title fields). Fixed by having the Type-chip click handler detect a transition into or out of "project" and, only then, re-invoke `openWishSheet` with a `draft` snapshot of whatever's currently typed (same mechanism as the nested-sheet round trip above) plus an explicit `draft.type` — `initialType` now resolves `draft?.type ?? entry?.type ?? ...` (draft wins) so this also works correctly when switching type mid-*edit* of an existing entry, not just mid-add. A second bug caught in the same pass, before it shipped: naively falling through to "always show the catalog name field" for any non-transitioning click broke a *redundant* reclick of the already-selected "Project" chip (would incorrectly hide the checklist and title field it was already showing) — fixed by keeping the original type-based (not transition-based) show/hide as the fallback once a transition has been ruled out.
- **Per-sub-item cost, summed into the project's Estimated cost** (`js/routes/wishlist.js`) — each checklist row (task/asset/item) now carries its own `cost`, entered via a new `₹ cost` number field next to the existing Add row, shown inline on each row next to its title (same line as the Mark done/Buy button, per the direct request). `sumSubItemCosts()` totals them; the project's own Estimated cost field switches to read-only and shows that live sum the moment the checklist has anything in it (label changes to "Estimated cost (sum of checklist items)"), reverting to a plain editable field if the checklist is ever emptied back out. `persistSubItems()`/`readWishFields()` both keep `estimatedCost` in sync with the sum on every change, not just at save.
- **Migration for pre-existing checklists** (`js/migrations.js`, v7→v8, `CURRENT_SCHEMA_VERSION` 7→8) — backfills `cost: 0` onto every wishlist project's existing sub-items that predate this field, per direct instruction ("put 0 as the default during migration as else it will be redundancy") — keeps the stored data itself honest rather than leaning on a runtime `|| 0` fallback everywhere it's read.
- **Real engine bug fixed: a routine with today as its start date skipped today** (`js/engine.js`'s `firstFloatingOccurrence`) — was comparing `start` (parsed from a date-only string, always local midnight) against `now` (the current moment, hours later in the day) as exact timestamps: on the very day a start date is set to today, `start < now` always reads true, so the walk-the-interval-forward loop ran once and skipped straight to the next occurrence instead of landing on today. Fixed by comparing at day granularity via the same `dateOnly()` convention `stateOf()`/`overdueDays()` already use (see their own comments) — "today" now correctly counts as on-or-after the start date rather than already past it. Verified directly via `computeNext()` (startDate = today, now = mid-afternoon same day → `dueAt` correctly resolves to today) and via a full UI round trip (created and deleted a real test routine with today's start date, confirmed its occurrence landed on today, not a week out).
- **Date-input text left-aligned** (`app.css`) — the existing `.text-input[type="date"] { text-align: left; }` rule (Round 24) was confirmed still rendering correctly in this session's own browser, so the report is most likely a stale-deployment lag on the user's own device (same class of issue flagged once before this session for their GitHub Pages deployment) rather than a live bug — but added a more robust belt-and-suspenders rule anyway, `text-align` on the host `<input>` doesn't reliably reach the native date-edit segments on every Chromium version: `.text-input[type="date"]::-webkit-datetime-edit` / `[type="time"]::-webkit-datetime-edit { text-align: left; }` targets the actual internal box directly.
- Verified live end-to-end on the `xathurak@gmail.com` test account: a brand-new "TEST Repaint Project" — checklist section confirmed appearing the instant the Project chip is clicked on a fresh Add sheet; a task sub-item (₹500) and an asset sub-item (₹3,200, "Floor lamp") added, Estimated cost confirmed live-summing to ₹3,700 and going read-only; the asset sub-item's "Buy" button confirmed opening the real Add Asset sheet pre-filled with its name, saving it, and correctly returning to the *same* in-progress Add-project form with title/priority/checklist all intact (not reset); final "Add idea" click confirmed writing a real `wishlist` entry with `type:"project"`, both sub-items' costs and `createdId` correct, and `status` auto-set to `"acquired"` once every item was marked done; the real linked asset record confirmed created with the correct name/space. The type-chip edge cases (asset→project→project-reclick→item→project) all confirmed showing/hiding the right fields at each step, including the reclick regression caught and fixed before considering this done. Zero console errors throughout. Test wishlist entry and its linked asset deleted after verification.

## Round 26 — acting on the QA report: consequence tier rename, person-deletion cascade, Kitchen content, duplicate-asset nudge, and a dozen smaller UX fixes (added 2026-08-09, same session)

User follow-up on the QA simulation report (see Round 24's linked Artifact): asked to fix findings 4.2 (Kitchen has no starter content), 4.3 (duplicate assets can't be told apart), 4.5 (orphaned data on deletion), plus a long list of pasted notes — a person-leaves-household question, Today tile reorder, removing Re-run onboarding, fitting Today's filter chips on one mobile line, alphabetizing space pickers everywhere (grouped by house when multiple are active), left-aligning date inputs, removing the start-date helper paragraph, and a full consequence-tier rename + relabeling audit across every default routine in the app, "then give me the logic."

- **Consequence tier rename, the big one** (`routine.js`, `today.js`, `insights.js`, `intel.js`, `app.css`, `tokens.css`, `catalog.js`, `packs/*.json`, `roomTemplates.js`, `mock-data/routines.js`, `migrations.js`) — "safety" → **"unsafe"** everywhere (same meaning, plainer word, zero reclassification needed). "degrading" retired; every routine that used it was individually re-examined by title and reclassified into whichever of the two survivors actually fits: **"unhygienic"** (RO/water-filter servicing, showerhead descaling, furnace/air-purifier filters, chimney mesh, water dispensers, coffee makers, pool filters, pest/damp storage checks — anything genuinely about cleanliness or water/air quality) or **"damaging"** (appliance servicing, vehicle maintenance, financial/paperwork routines, mechanical wear-prevention — the "if you skip this, something breaks or degrades physically" bucket). One routine ("Review and file warranty documents") didn't fit either and was downgraded to cosmetic instead. "If skipped, it" → "**If skipped, it is**" (grammar). Tier wash intensity on Today's row backgrounds now graduates with severity (6/8/10%, was a flat 7/7/9%) so the three non-cosmetic tiers read as visually distinct, not two nearly-identical washes with one slightly-stronger third. `migrations.js` v4→v5 applies the exact same title-keyed reclassification to any household's already-saved routine data (idempotent — matches on the OLD values, so re-running it is a no-op).
- **What happens when a person leaves the household** (`state.js`, `people.js`) — answered directly, since nothing happened before: the person record just vanished and every reference to their id was silently left dangling. Now: routines/tasks lose the assignee hint (`defaultAssigneeId`/`assigneeId` → null) but are NOT deleted — the chore still needs doing, it just has nobody's name on it anymore. Habits are different: they're inherently personal (person-scoped, no meaning without an owner, per the original "Habits are personal" design call), so they're deleted along with the person, taking their habit-log history with them. Ledger completions (`doneBy`) are deliberately left untouched — a historical record of who actually did something stays true after that person leaves. The delete confirmation now says exactly what's about to happen ("Remove Radha? — 2 routines will be unassigned, 1 habit will be deleted") instead of a bare "Remove X?", via a new `personDeletionImpact()` — same cascade-warning pattern already used for deleting a house.
- **4.2 — Kitchen finally has starter content** (`roomTemplates.js`, `catalog.js`, `icons.js`) — Kitchen has been a universal implied space type since Round 18 but had zero suggested content; "Add space → Kitchen" offered nothing. New `HAND_AUTHORED.kitchen` entry surfaces the 6 kitchen appliances that already existed in the asset catalog but were never linked to any room type (Refrigerator, Gas stove, RO/water purifier, Chimney, Mixer grinder, Microwave), plus 3 new standalone routines (counters/stovetop cleaning, trash, monthly degrease). Pantry staples (Rice, Wheat flour, Sugar, Salt, Cooking oil) didn't exist in the item catalog at all — added, with a new `pantryStaple` icon (a tied sack, matching the existing 24×24/1.8-stroke hand-drawn technique). "Garbage bags" turned out to already exist under "Trash bags" — reused rather than duplicated.
- **4.3 — Duplicate-asset soft nudge** (`assets.js`) — Round 7's "no warning, add freely" call for assets stands (two ACs in one room is legitimate) — but adding a SAME-NAMED asset to a room that already has one is far more often an accidental double-add than a deliberate second one (confirmed live: this account's own Bathroom had had two identically-named, indistinguishable "Geyser" records sitting in it from earlier testing). Now a `confirm()` nudge — not a block — asks "There's already a {name} in this room. Add another one anyway?" only when adding fresh (never fires when editing an asset against itself).
- **Smaller fixes, same round**: Today's stat tiles reordered (Overdue, Due today, This week, Completed, Low stock — was Low stock before Completed); "Re-run onboarding" removed from More (dead code path removed too — `houses.js`'s own "Add house" launches the same wizard directly and is unaffected); Today's 5 filter chips (10 free minutes?/Today/This week/Batches/Smoothen) get a dedicated tight nowrap+flex:1 row for mobile, same technique already proven for Stock's period chips; every space-picker (routine/item/asset Space fields, House's own grid) now sorts alphabetically, grouped by house first when more than one house is active — extracted as a small `sortSpaces()` helper duplicated consistently across the three route files; date/time inputs are explicitly `text-align: left` now (Chrome/Edge on Windows were right-aligning/centering the native value by default); the routine builder's Start date field lost its explanatory paragraph underneath.
- **The classification logic, as requested** — for any future routine (built-in or custom): ask "if this is skipped, does the house become less clean/hygienic (water, air, food-contact surfaces, pests, damp) — or does something mechanical/financial actually break or degrade?" The former is unhygienic, the latter is damaging. If neither really applies, it's cosmetic. "Unsafe" is reserved for the same things "safety" always was — gas leaks, fire/smoke detection, structural/electrical hazards — nothing was reclassified into or out of that tier, only renamed.
- Verified live end-to-end throughout: migration confirmed running against this account's real ~54-routine household (schemaVersion 4→5, exact per-title reclassification spot-checked); tier tints and breakdown text confirmed reading the new names correctly; person-deletion cascade confirmed via a disposable test person (routine/task survived unassigned, habit deleted, confirm-dialog text exactly right); Kitchen template confirmed via both `templateFor()` directly and a full real "Add space" UI round trip (6 assets + 7 items + 3 standalone routines, 10 total routines once asset-linked suggestions are included); duplicate-asset nudge confirmed firing and correctly blockable. Zero console errors throughout. One real mistake this round, caught and fixed immediately: a stray combined multi-statement test script intermittently threw on chained property access after an `addRoutine`/`addTask` call for no reproducible reason (each individual call succeeded in isolation) — worked around by splitting into single-statement calls rather than chasing a tooling quirk further.

## Round 25 — item consumption reworked again: "how many times does it last" replaces a direct rate, Reorder now a "uses left" count (added 2026-08-08, same session)

User follow-up on Round 24's consumption redesign, asking whether it makes sense: rename "Consumption" to "Automate consumption" in the item sheet, and "Quantity" to "Quantity now"; rather than asking directly for a per-day/month/use rate (Round 24's design), ask the easier real-world question "how many times can 1 unit be consumed" and derive the rate automatically as 1 divided by that; move Automate consumption above Reorder so the reorder question can be auto-populated from it; Reorder itself should ask "how many uses are left" (a count), not a raw quantity.

- **Direct-rate entry replaced with "how many X does 1 unit last"** (`stock.js`) — Round 24 asked "how much (ml) is used per day," a number most people don't actually know off the top of their head. Now asks "How many days does 1 [unit] last?" (or months/uses, matching whichever consumption mode is picked) — a much easier real-world estimate ("this roll lasts about 40 days" vs. "I use 0.025 rolls a day"). The typed number (`timesPerUnit`) is never stored itself — `burnRate`/`perUseQty` (the same underlying fields as before, and everything downstream that depends on them — `applyAutoDepletion()`, the days/uses-left projections — is unchanged) are computed as its reciprocal, `1 / timesPerUnit`, in the item's own unit. Editing an existing item reconstructs `timesPerUnit` by inverting whichever rate is already stored — a pure UI reframing, no data-model or migration change, consistent with Round 24's own "storage stays the same" approach. Switching Day↔Month still re-expresses the typed number so it doesn't silently change meaning (40 days ↔ 1.33 months), same principle as Round 24's rate-conversion, just applied to the new field.
- **Automate consumption moved above Reorder** (`stock.js`) — field order is now Unit → Quantity now → Automate consumption → Reorder, so the reorder question can react to what was just answered above it, per direct request.
- **Reorder reframed as a "uses left" count** (`stock.js`) — when consumption is Off, Reorder is unchanged (a plain "Reorder when this many are left" quantity, since there's no rate to convert a count against). Once a consumption mode is picked, Reorder switches to "Reorder when this many uses are left" — a round, easy-to-answer count (defaults to 3) — and the actual quantity threshold (`parLevel`) is computed automatically as `usesLeft × (1 / timesPerUnit)`, never typed directly. A live caption under the field ("≈ 0.08 roll left") shows the computed quantity equivalent so the number being saved is never a mystery. Editing an existing item reconstructs the uses-left count the same way (`parLevel × timesPerUnit`) — small rounding drift is possible round-tripping through 2dp storage (e.g. 3 → stored as 0.08 → reconstructed as 3.2), harmless and expected, not chased further.
- **Renamed for clarity, no behavior change**: "Consumption" → "Automate consumption"; "Quantity" → "Quantity now" (distinguishes current on-hand amount from the new uses-left/times-per-unit concepts sitting right next to it in the same sheet).
- Verified live end-to-end: label text confirmed for Day/Month/Per-routine-use across both a countable unit (roll: "How many days does 1 roll last?") and a measured unit (ml: "How many uses does 1 ml last?"); the reorder caption confirmed live-updating and mathematically correct (3 uses ÷ 40 = 0.075, displayed and stored rounded to 0.08); a full save-then-reopen cycle confirmed `burnRate`/`autoDeplete`/`parLevel` all correctly derived and reconstructed; the "consumption mode picked but times-per-unit left blank" validation confirmed still blocking save with a toast. Zero console errors throughout.

## Round 24 — real root cause found for the mobile house/space duplication bug, Due today literal-day fix, mandatory routine space, alphabetical House grid, item consumption redesign, auto-add person on join (added 2026-08-08, same session)

User report, six items: Today's "Due today" section was showing things due later in the week, not just today; a "major bug" — creating a new house and adding spaces caused some kind of cross-house duplication/interlinking, routines created twice, no filter removing them, not reproducible on web but happening on mobile; routine Space should be mandatory, defaulting to Whole home; House's space grid should sort alphabetically; a request to rework item consumption tracking to be simpler and more intuitive (piece/roll/pack consumption framed as whole-unit counts, ml/g/kg/litre as measured amounts, both sharing one Day/Month/Per-routine-use consumption picker); and a new person joining a household with a new email should automatically get a real Person record with their name and email.

- **"Due today" fixed to mean literally today** (`today.js`) — root cause: `state === "due"` (from `engine.js`'s `stateOf`) is true for the ENTIRE due-window (memo §5.1's head-start, default 3 days before the literal due date), not just the due date itself — an occurrence due 3 days out with `windowDays: 3` already reads as "due" today. Reproduced directly against real data (one occurrence: `offset: 3, windowDays: 3, state: "due"`) before fixing. Both the "Due today" section (`render()`) and its matching stat tile (`statRowHtml()`) now additionally require `r.offset === 0` (the literal day offset from today) — the "This week" toggle is the existing, correct place to see days-ahead items; the default view no longer leaks them in early. Verified live: the exact leaking occurrence ("Wipe entry doormat," offset 3) confirmed absent from the rendered Due Today list after the fix, present only under This week.
- **Real root cause found for the house/space duplication bug** (`packs.js`) — a genuine, serious bug, not a guess: `generateFromArchetype()` (which BOTH onboarding and houses.js's "Add house" run) generated every space/asset/item/routine id via a bare local counter (`` `${prefix}_${idSeq}` ``, `idSeq` starting at 0 on every fresh page load) with **zero session differentiation** — completely different from `state.js`'s own `genId()`, which salts every id with `Date.now().toString(36)`. Consequence: any TWO separate app sessions that each ran onboarding-driven generation (e.g. "Add house" today, then again in a different session next week — a fresh page load on mobile is far more frequent than on a long-lived desktop tab, since mobile OSes/browsers aggressively kill backgrounded PWA tabs) produced IDENTICAL ids for their Nth/Nth+1/etc. generated space — `sp_1`, `sp_2`, `sp_3`... every time, regardless of what already existed. Caught by direct inspection of the actual shared test account's real data: an existing "studio" house's spaces were `sp_1`/`sp_3`/`sp_4`/`sp_10`/`sp_19`/`sp_60` — exactly the bare-counter pattern, confirming this had already run at least once in production data. Two different space objects (different `houseId`, different name) sharing one `id` in the flat `state.spaces` array means every `byId()` lookup for that id — used constantly for icon resolution, space-name display, edit-sheet targeting, deletion — resolves to whichever one `Array.find` hits first, cross-contaminating data between the two houses. This plausibly explains every symptom reported ("spaces are interlinked," "no filter removes them," "routines are created twice," "unable to recreate on web" since a single continuous browser tab never resets `idSeq`). **Fixed** by deleting `packs.js`'s own counter entirely and delegating to `state.js`'s `genId()` instead (imported directly — confirmed no circular dependency, `state.js` doesn't import from `packs.js`). Verified live: `generateFromArchetype()`'s freshly generated space ids now carry the same timestamp-salted shape as everything else in the app (`sp_msh33ya91` instead of `sp_1`).
  - **Same class of bug found and fixed in `catalog.js`** while auditing for the same pattern — `getOrCreate()`'s custom-catalog-entry keys (`AST-CUSTOM-N`/`ITM-CUSTOM-N`) used the identical bare-counter scheme. Unlike `packs.js`'s ids, the `custom` array itself is genuinely session-only (documented, unrelated to Firestore) — but the generated *key* gets stamped onto real items/assets via `catalogKey`, which DO persist, so two different real-world custom entries typed in two separate sessions could silently alias to the same key. `state.js`'s `genId` isn't importable here without creating a circular dependency (`state.js` already imports from `catalog.js`), so this one was fixed self-contained with the same `Date.now().toString(36)` salting technique instead. Verified live: a freshly registered custom item now gets a key like `ITM-CUSTOM-msh3449y2`.
  - `engine.js`'s own `nextOccId()` counter was audited too and left alone — occurrences are explicitly excluded from Firestore serialization (`serializeState()`) and fully regenerated fresh every session (`hydrateState()` clears `state.occurrences` before calling `regenerate()`), so there's no cross-session merge for a bare per-session counter to ever collide against. Confirmed safe, not the same bug class.
- **Routine Space made mandatory, defaulting to Whole home** (`routine.js`) — previously, opening the routine builder with no `defaultSpaceId` in context (e.g. Today's quick-add "+" switched to "Household routine") left the Space chip-group with nothing pre-selected, and saving silently wrote `spaceId: null` — genuinely reproduced against this exact session's own real data (a pre-existing "test" routine with `spaceId: null`, evidence this had already happened in practice). New `defaultWholeHomeSpaceId()` resolves the currently-active house's own Whole home space as the pre-selected default (same resolution pattern `requiresItemsFieldHtml()` already uses for Utility); the save handler now also blocks with a "Pick a space" toast if somehow still empty. Verified live: opening the shared sheet, switching to Household routine, confirmed "Whole home" pre-selected with the current house's name hint.
- **House's space grid sorted alphabetically** (`house.js`) — was insertion order (a newly added space always landed last regardless of name); now `.sort((a, b) => a.name.localeCompare(b.name))`. Verified live: Bathroom, Entry, Kitchen, Living room, Utility, Whole home — correct alphabetical order.
- **Item consumption reworked for simplicity** (`stock.js`) — replaced three separate, overlapping controls (a rate number, a day/week/month/usage period chip, and a SEPARATE "auto-deplete" toggle that only sometimes applied) with one "Consumption" chip: **Off / Per day / Per month / Per routine use**. Picking Day or Month now always live-depletes the actual quantity — the old confusing middle state ("a rate is set but nothing is actually counting down, it's just a display estimate") is gone entirely; if you told it a rate, it counts down, full stop. The amount field's own label reads differently depending on the item's unit (2026-08-08, user request: countable units piece/pack/roll read "How many are used," measured units ml/g/kg/litre read "How much (ml) is used") — both share the exact same three modes underneath, just phrased naturally either way. "Reorder at (par level)" relabeled to "Reorder when this many are left," same field, plainer language. Underlying storage is unchanged on purpose (`burnRate`/`perUseQty`/`autoDeplete`/`lastDepletedAt` — same fields `state.js`'s `applyAutoDepletion()` and the day-left/uses-left projections already depend on), so this is a pure UI simplification, not a data-model or migration change; opening an existing item that had a rate set with auto-deplete previously OFF now just shows as "Per day," and re-saving turns live depletion on — the intended effect of removing the middle state, not a bug. Switching between Day and Month still re-expresses the typed number (10/day ↔ 300/month) so it doesn't silently mean something different once saved. Verified live end-to-end: unit-aware label text confirmed for both piece and ml; Day→Month conversion confirmed exact (10 → 300); a saved item's `burnRate`/`autoDeplete`/`perUseQty` fields confirmed matching the selected mode; the "mode picked but amount left blank" validation confirmed blocking save with a toast.
- **New person auto-created when joining a household** (`state.js`, `boot.js`, `people.js`) — new `addPersonForJoiningUser({ name, email })`: skipped entirely (returns the existing match) if a person with that email is already on the roster (case-insensitive) — never creates a second entry for the same real person, e.g. if the household owner had already pre-added them as Help/Member in anticipation. Wired into all three places a signed-in user's Firestore household actually loads: `boot.js`'s fresh-join flow (`joinAndEnter`), `boot.js`'s main returning-user auth path (defensive backfill for a household joined before this feature existed — a no-op for every normal session where the record's already there), and `people.js`'s mid-session "Join a different household." `name` is whatever Google's own `displayName` already provides (first+last together — the Person model has no separate first/last fields to split it into, matching how `resetForNewHousehold` already does the equivalent for a brand-new household's very first member). Verified live via direct calls: a new email correctly created a new Member-kind person; a second call with the same email (different casing) correctly returned the existing person instead of creating a duplicate.
- **A real bug caught by this round's own verification step, not shipped**: the first live reload after wiring `addPersonForJoiningUser` into `boot.js` threw `ReferenceError: addPersonForJoiningUser is not defined` — forgot to add it to the top-of-file import list from `state.js`. Caught immediately via the standard hard-reload + console-error check (not left for the user to find), fixed before moving on. Worth restating the lesson this project has already learned more than once: always hard-reload and check console errors after any edit, never assume a diff is correct just because it looks right.
- **Also fixed while testing this round, unprompted but directly adjacent**: `house.js`'s `openRoomTemplateSheet` (the "Suggested for {room}" confirm sheet shown right after adding a space) had no re-entrancy guard at all — every checked item/asset/routine was added in a synchronous loop with `closeSheet()` only called at the very end, so a second tap before the sheet visually responded could double-fire the whole handler. Added a `saving` guard (blocks a literal re-entrant call) plus content-level dedup checks for items (same per-room duplicate-stock guard `stock.js`'s own `openItemSheet` already enforces, mirrored here since `addItem()` itself has no such check) and routines (by title within the same space) — a second, independent layer in case two genuinely separate taps both land before the first tap's disabled state takes visual effect. Assets deliberately NOT deduped by content (Round 7's own "no warning, add freely" call for assets stands — two ACs in one room is legitimate), protected only by the re-entrancy guard. `onboard.js`'s own confirm button got the same re-entrancy guard for defense-in-depth, even though `seedHousehold()` turned out to already be idempotent-per-house on inspection (a double-fire there would still have double-called `onDone?.()`). Verified live: a normal single-tap "Add space → Bedroom" still correctly created 4 assets + 9 routines with zero duplicate titles, confirming the new guards don't interfere with the ordinary path.
- Verified live end-to-end throughout (zero console errors after the one self-caught-and-fixed import bug above): Due Today's literal-day filtering against real leaking data, the packs.js/catalog.js id-generation fix's new timestamp-salted output, mandatory Space defaulting to Whole home, House's alphabetical grid, the full item-consumption save/edit/validate cycle, person auto-creation's dedup behavior, and the room-template sheet's normal single-tap flow post-guard.

## Round 23 — mandatory start date for every trigger type, mobile date-field appearance fix (added 2026-08-08, same session)

User request: make the start date mandatory across every routine and repeat type, not just `floating_since_last`, with explicit past/future semantics — "if the start date is in the past, logically the next occurrence should be based on the repeat selected. If in the future, the first occurrence should be then." Also reported the date input still looked "big and bulky" on mobile compared to other fields despite Round 21's height fix, and asked for it to match the Title field's box exactly.

- **Start date generalized to every trigger type** (`engine.js`) — was previously a `floating_since_last`-only field. `computeNext()` gained a shared pre-switch check: for any trigger type, if the routine's never been completed and its `startDate` is in the future, that date wins outright as the first occurrence, full stop — no per-type special casing needed since every type already treats "the computed date" as the due date regardless of trigger shape. A past (or today) start date is different per type: `floating_since_last` is the one trigger genuinely anchored to a point in time (normally the last completion), so a new `firstFloatingOccurrence()` walks the interval forward from the start date until it reaches a point on/after today — this is what "next occurrence based on the repeat selected" actually means for an interval-based routine (e.g. start date 25 days ago + a 10-day interval correctly lands 5 days from today, not immediately due and not literally 25 days overdue). Every other trigger type (`fixed_calendar`, `usage_meter`, `condition`, `seasonal`, `on_mode`) already computes "the next occurrence from today" with no completion history, which already **is** "based on the repeat selected" for a calendar/velocity/season-anchored trigger — so a past start date on those needed no new logic, just the future-date override above. Missing `startDate` (any pre-existing routine, mock data, or pack content created before this field existed) falls straight through to each type's original behavior — no migration needed, nothing breaks retroactively.
- **One shared, mandatory field, not six duplicates** (`routine.js`) — the old per-trigger "Start date (optional)" field living only inside the `floating_since_last` params block is gone; a single "Start date" field now sits once, right after the Repeats chip group, visible and populated (defaulting to today, matching Task's own due-date convention) regardless of which trigger type is selected — switching trigger types no longer hides or re-renders it. `buildTrigger()` now stamps `startDate` onto whichever trigger object it returns, for all six types. The save handler blocks with a "Pick a start date" toast if it's ever cleared, for real routines (habits/tasks are unaffected — a task already has its own mandatory due date, habits have no trigger at all).
- **Editing an existing routine's start date generalized too** (`state.js`'s `updateRoutine`) — Round 21's "moving an un-completed routine's start date moves its already-generated occurrence" fix was `floating_since_last`-only and worked by stamping the occurrence's `dueAt` directly to the raw start date, which would have been wrong for a past-dated edit (instantly overdue, ignoring the new past-vs-future logic above). Now generalized to every trigger type by dropping the stale open occurrence and letting `regenerate()` rebuild it fresh through the engine — so a past or future edited start date is always resolved the one real way (`computeNext`), never duplicated or approximated here.
- **Suggested-routine and service-routine creation extended to all trigger types** (`assets.js`, `house.js`) — Round 22's "new asset routines default to next Saturday" only stamped `startDate` onto `floating_since_last` templates (the only type that had a start-date concept then); now applied uniformly to whatever trigger type a suggested-routine template actually uses, since a start date is mandatory for all of them now.
- **Mobile date-field appearance** (`app.css`) — Round 21's fix (explicit `height` + a normalized calendar-icon indicator) capped the *box height* but the user reported it still read as visually "bulky" next to a plain text field on mobile. Added `-webkit-appearance: none; appearance: none` to `type="date"`/`type="time"` inputs — mobile Chrome/Safari apply their own native chrome/internal-padding to date inputs that isn't fully overridden by height/padding alone; `appearance: none` hands the box back to this app's own `.text-input` styling entirely. Not independently visible in this session's desktop Chrome (same limitation noted in Round 21 — desktop doesn't apply the native chrome mobile does), but confirmed via `getComputedStyle` that the Start date field's height/padding/border/radius/font-size/box-sizing are now byte-identical to the Title field's, so the fix is layered on a verified-correct baseline rather than a guess.
- Verified live end-to-end: the shared Start date field confirmed present and populated across all 6 trigger types on one sheet without re-render/reset when switching between them; save correctly blocked with a toast when cleared; a `floating_since_last` routine with a start date 25 days in the past and a 10-day interval correctly computed its next occurrence 5 days out (anchored cycle math, not "due now"); a `fixed_calendar` routine with a start date 12 days in the future correctly used that exact date as its first occurrence (bypassing the RRULE day-matching entirely, as intended for a literal future override); editing that same routine's start date to a new date correctly moved its already-generated occurrence via the regenerated-not-stamped path. Zero console errors throughout.

## Round 37 — real bug found: an incomplete routine could never go overdue once anything re-saved it (added 2026-08-10, same session)

User report: a weekly routine genuinely due yesterday, not completed, was showing due *next week* instead of overdue. Asked two things first, answered directly before any code change: does smoothing ever touch overdue items (no — it excludes them entirely, checked before it even builds its candidate list), and is smoothing a household or personal setting (household — `state.household.smoothingMode`, no per-person override). Neither explained this specific report, so investigated further per the user's own instruction ("create a new task and then move dates to see what happens... use only xathurak for checking").

- **Root cause found and fixed** (`js/state.js`'s `updateRoutine`) — a *different* bug from Round 25's smoothing-eligibility one, with a similar symptom. Since Round 23 made a start date mandatory for every routine, `updateRoutine`'s "if editing the start date, drop the stale occurrence so it regenerates correctly" logic (built in Round 21) checked `if (routine.trigger?.startDate)` — true for literally every routine — instead of checking whether the start date had actually *changed* as part of the update. That meant **any** edit at all to a routine that had never yet been completed (even an unrelated one, even a no-op save) unconditionally dropped its current occurrence and let it regenerate fresh via `firstFloatingOccurrence()` — which, by design, always walks forward to a point on/after "now" and can never land in the past. Net effect: once anything re-saved an incomplete weekly (or any floating-interval) routine, it could never show as overdue again — it would always read as due at some future point in its cycle instead, exactly matching the report.
- **Fix**: capture the routine's start date before applying the patch, and only run the drop-and-regenerate logic when the start date actually changed. A genuine start-date edit still correctly moves the occurrence (verified — new occurrence id, due date matches the newly-set date); an unrelated edit (or an empty no-op patch) no longer touches the occurrence at all.
- Verified live end-to-end via a precise, controlled reproduction rather than guessing from existing data (confirmed still on the `xathurak@gmail.com` test account per direct instruction): created a disposable weekly routine, manually aged its occurrence's due date to 3 days in the past (simulating a missed cycle, `stateOf()` correctly read "overdue" at that point), then called `updateRoutine()` with an empty patch — before the fix, this silently replaced the occurrence with a fresh one 7 days in the future (`pending`, never overdue); after the fix, the exact same occurrence persisted, still correctly reading "overdue". Separately re-confirmed the original start-date-edit feature (Round 21/23) still moves the occurrence correctly when the date genuinely changes. All test routines deleted after. Zero console errors throughout.

## Round 36 — Espresso machine and Coffee grinder added to the asset catalog (added 2026-08-10, same session)

User request: "add espresso machine, coffee grinder into the assets library with any relevant routines and also the aging etc."

- **`AST-ESPRESSO-MACHINE`** (`js/catalog.js`) — `expectedLifeYears: 8` (built more robustly than a basic drip maker, but still real water/mineral contact — sits between the existing Coffee maker's 5 and larger water-contact appliances like a dishwasher's 10). Two suggested routines: "Descale espresso machine" (90 days, unhygienic — mineral buildup) and "Clean group head and portafilter" (14 days, unhygienic — coffee-oil residue, the thing that actually needs frequent attention on a real espresso setup, distinct from descaling).
- **`AST-COFFEE-GRINDER`** — `expectedLifeYears: 7` (a dedicated burr/blade grinder generally outlasts a multi-purpose mixer grinder's 6, and sees less abuse). One suggested routine: "Clean coffee grinder burrs" (30 days, unhygienic — oil residue buildup affecting taste and risking staleness).
- New `espressoMachine`/`coffeeGrinder` icons (`js/ui/icons.js`) in the same hand-drawn 24×24/1.8-stroke technique as the rest of the set — a boxy machine body with a dial and group-head drip streams for the espresso machine (deliberately distinct from `coffeeMaker`'s rounded-carafe silhouette), a tapered hopper over a grind chamber for the grinder (distinct from `mixerGrinder`'s wider jar-on-base look).
- Deliberately **not** added to any room template's suggested-content list (same as the existing plain Coffee maker, Kettle, Toaster entries) — available via the catalog typeahead whenever an asset is added, not force-suggested into every kitchen, since not every household has a dedicated espresso setup.
- Verified live end-to-end: typeahead search for "espresso" correctly surfaced the new entry; selecting it pre-filled Expected life to 8 and offered both suggested routines pre-checked; saving created the real asset (correct `category: "appliance"`, `icon: "espressoMachine"`) plus both linked routines; the icon rendered cleanly in the Assets list. Test asset and routines deleted after. Zero console errors throughout.

## Round 35 — Forecast combines same-asset strips, routine duplication no longer re-copies shared Utility/Whole-home items (added 2026-08-10, same session)

User follow-up: "on the forecast... instead of multiple strips, possible to combine similar strips for similar assets?" — 3 exhaust fans were rendering as 3 near-identical rows even within the same category cluster. Also: "when routines duplicated, if it consumes an item from utility/whole home then dont create a new item in that room.. as items in utility and whole home are considered for all spaces."

- **Forecast combines same-name aging-asset strips** (`js/routes/insights.js`) — within each category cluster, assets sharing the same name now collapse into one row ("3× Exhaust fan") instead of one per asset. New `combineFailureGroup()`: a lone asset renders exactly as before (nothing to combine); a group shows an age range (or a single age if they're all the same), a count of how many have a genuinely rising service frequency, and the same "worth budgeting for replacement" framing. Groups are sorted most-urgent-first the same way individual rows were before (any rising-frequency asset beats a group with none, then by the oldest in the group).
- **Real bug found and fixed in the duplication cascade**: `resolveRequiredItemsForSpace()` (Round 30) was matching a required item against the TARGET ROOM specifically, with no awareness that Utility/Whole home items are already shared across every room in their house (Round 15's own "Uses this stock" design) — duplicating a routine that used a Utility item into a different room in the SAME house would have silently created a redundant second copy of that item in the new room. Fixed: a required item whose own space is Utility or Whole home now either (a) stays pointed at the exact same shared item, untouched, when the target room is in the same house — it already applies there — or (b) for a genuinely cross-house duplicate, resolves against the TARGET HOUSE's own Utility/Whole-home space (reuse-or-create there, never the target room) — a shared item was never room-scoped to begin with, so the target room itself is never the right place to look.
- Verified live end-to-end with disposable test data (created and deleted within single script calls to avoid a real risk found mid-testing — a live Firestore `onSnapshot` echo can overwrite test data seconds later if left across separate calls, not a bug, just a real timing hazard worth remembering for any future live-household testing): same-house duplication confirmed creating zero new items and keeping the exact same shared-item reference; cross-house duplication (this account's real second house) confirmed creating exactly one new item in the target house's own Utility space, never the target room. The real Forecast section confirmed rendering 3 real exhaust fans as one combined "3× Exhaust fan" strip. Zero console errors throughout.

## Round 34 — the signed-in person's own habits show under "Everyone" by default; explained habit overdue/missed semantics (added 2026-08-10, same session)

User request: "for habits, when logged in in today everyone.. all the respective logged in users habit needs to appear if that habit is scheduled for the day." Also asked directly: does a missed habit exist as "overdue," or does it delete and reappear on the next scheduled date?

- **Real gap fixed**: Round 18's own privacy rule ("habits shown only to the person doing them") had `memberFilter === null` (the default "Everyone" view) literally mean *nobody's* habits — every signed-in person had to tap their own name in the filter row before they'd ever see their own habit checklist, every single session. Now `js/routes/today.js`'s `visibleDueHabits()` resolves the Firebase Auth email to a Person record (`currentPersonId()`, same lookup `addPersonForJoiningUser` already uses) and treats "Everyone" as "at least show me my own" — other people's habits stay exactly as hidden as before under the default view; explicitly tapping a specific person's filter chip still works unchanged.
- Verified live against the real household: confirmed the real "Hair Oil application" habit was correctly excluded from a fresh test only because it had genuinely already been checked off today (not a bug); built two disposable test habits instead (one for the signed-in Vinod account, one for Keerthana) — under the default "Everyone" view only Vinod's own showed, Keerthana's stayed hidden; explicitly tapping Keerthana's filter chip correctly showed hers instead. Both deleted after. Zero console errors throughout.
- **Answered directly, no code change** (verified against the actual `isHabitDueToday`/`isHabitScheduledOn`/habit-grid code, not guessed): a habit has no "overdue" state at all — that's a routine-only concept backed by a real `occurrences` record. A habit is just "is today one of its scheduled days, and is today already logged done" — evaluated completely fresh every day, nothing carried forward. A missed day isn't deleted (there was never anything to delete — habits are logged forward only, never backfilled) and doesn't "reappear" either; the next scheduled day is simply evaluated on its own terms when it arrives, regardless of whether the previous one was done or missed. The one lasting effect of a miss: it breaks the current streak (`habitStreak()` counts consecutive done-days ending today-or-yesterday) and shows up retrospectively as a red "missed" cell in the 72-day grid (`habitGrid.js`) — a scheduled day in the past with no matching log entry.

## Round 33 — "Always on Saturday" toggle for floating routines (added 2026-08-10, same session)

User request: "can we add schedule always on saturday option? a toggle, even though i give the repeat every days when even this toggle is on, it automatically chooses the following saturday."

- **New `snapToSaturday` flag on `floating_since_last` triggers** (`js/engine.js`, `js/routes/routine.js`) — a new "Always on Saturday" On/Off toggle sits right under "Repeat every (days)" in the routine builder, `floating_since_last` only (the trigger type the request was actually about). It doesn't replace the interval — the interval still drives *roughly* when the routine is next due; the toggle just snaps that computed date forward to the nearest Saturday on/after it (`engine.js`'s new `snapToSaturday()` helper: `(6 - dayOfWeek + 7) % 7` days forward, 0 if already Saturday). An interval that already lands on a Saturday stays exactly there — it never gets pushed a needless week further. Distinct from the existing `nextSaturdayDateStr()` (used only as a brand-new asset-routine's default start date, which deliberately always jumps a full week even if today IS Saturday) — this snaps an already-computed date, so it can land as little as a day or two later, whatever the interval actually produced.
- Verified live: direct `computeNext()` calls confirmed a 3-day interval from a Monday snapping Thursday → the following Saturday, a 5-day interval that already landed on a Saturday staying put, and a full save-through-the-UI round trip (toggle On, 3-day interval, real routine created) producing a real occurrence whose `dueAt` fell on a Saturday (`getDay() === 6`). Zero console errors throughout.

## Round 32 — Insights' Forecast section clustered by signal type and asset category (added 2026-08-10, same session)

User follow-up: "the forecast in insights, can it be clustered somehow? like similar assets together or something else intelligent?" — it was one flat list interleaving three unrelated §5.9 signal types (aging-asset failure predictions, consumable coupling, neglected spaces) with zero visual distinction between them.

- **Forecast now groups by signal type first** (`js/routes/insights.js`) — labeled sub-sections ("Aging assets" / "Consumables to watch" / "Neglected spaces"), each only rendered if it actually has something to show.
- **"Aging assets" further clusters by the asset's own catalog category** ("similar assets together," directly per the request) — water heating, climate & air, appliances, furniture, etc. — biggest cluster first, and within each cluster the more urgent ones (service frequency genuinely rising, not just old) sorted to the top. The category sub-label itself only shows when more than one category is actually present, so a single-category household doesn't get a pointless redundant label.
- **Real data gap found and fixed along the way**: the clustering needs each asset's `category`, but nothing had ever actually copied that field from the catalog onto a real asset record — only `icon`/`expectedLifeYears` got that treatment. Confirmed live against the real household: every single asset had `category: undefined`. Fixed two ways — `assets.js`'s save handler now stamps `category` from the resolved catalog entry on every future add/edit (not user-editable, always trusted from the catalog), and a new migration (v6→v7, `CURRENT_SCHEMA_VERSION` now 7) backfills it for every asset that already existed, resolved via `catalogKey`.
- `intel.js`'s `failurePredictions()` now carries `category`/`icon` through on each prediction so insights.js can cluster/render without re-resolving the asset itself.
- Verified live end-to-end: the migration confirmed running against the real household (every asset's `category` populated correctly — climate/furniture/appliance, matching each one's actual catalog entry); the Forecast section confirmed rendering "Aging assets" → "Climate & air" (3 exhaust fans, the biggest real cluster) → "Furniture" (mattress) in exactly that clustered order; a full regression pass across Today/House/Stock/Insights/Wishlist confirmed clean. Zero console errors throughout.

## Round 31 — duplication picker is multi-select (added 2026-08-10, same session)

User follow-up: "multiple spaces can be selected for the duplicates.. can we implement that" — the room picker from Round 29/30 only ever let you pick one target room per duplicate action.

- **`openDuplicateToRoomSheet()` switched to a multi-select chip-group** (`js/ui/components.js`) — `onPick` now always receives an array of room ids (even for a single pick), so every caller handles one shape rather than two. Each of the three callers (`routine.js`, `stock.js`, `assets.js`) now loops the chosen rooms, aggregating results into one honest summary toast instead of firing once per room with no combined feedback:
  - Routines/assets: `"Duplicated to 3 rooms (+ 3 routines)"` — the asset case correctly aggregated across all target rooms in testing (3 rooms → 3 new asset copies + 3 new linked routine copies, each routine correctly pointing at its OWN room's asset copy, not cross-linked).
  - Stock items: reports success and blocked rooms separately in one toast — e.g. `"Duplicated to 1 room — already tracked in Master Bedroom"` — since a multi-room item duplicate can legitimately succeed in some rooms and hit Round 7's duplicate-guard in others within the same action.
- Verified live end-to-end with disposable test records (created, duplicated into 3 rooms at once, deleted after): confirmed all 3 target chips stay independently selected (not resetting each other, the exact class of bug Round 23 fixed for chip-groups generally); confirmed the asset+routine cascade fans out correctly per room; confirmed the mixed success/blocked item case reports both halves in one toast. Zero console errors throughout.

## Round 30 — duplication now cascades: an asset takes its linked routines with it, a routine's required items get reused or created (added 2026-08-10, same session)

User follow-up on Round 29's duplication feature, asked as questions first ("answer me first then u do any changes"): does duplicating an asset also duplicate its linked routine(s)? Does duplicating a routine also create its linked asset/items? Does duplicating a routine that then gets a new asset also pull in that asset's *other* routines? Are all fields actually copied? Then, once answered (no/no/no, yes/yes): asked to build the cascade, plus reuse an existing same-name item in the target room instead of creating a duplicate.

- **The cascade is deliberately one-directional** (`js/state.js`) — **duplicating an asset cascades DOWN to its linked routines** (an asset's service routine is conceptually part of it — duplicating "Split AC" without "Service Split AC" would leave the new room's AC with no reminder at all), but **duplicating a routine never cascades UP to force-create its asset** — it only passively reuses an already-existing matching asset in the target room if one's there, otherwise stays unlinked exactly like before. This one-way rule is what directly answers the third question ("if you duplicate the routine and create the new asset, do the other routines for that asset get duplicated") — no, never; only an explicit asset-duplicate action ever pulls in sibling routines. A two-way cascade would have had no natural stopping point.
- **New shared internals**: `findMatchingItemInSpace()`/`findMatchingAssetInSpace()` (catalog-key match, same rule Round 7's own add-time duplicate-guard already uses); `resolveRequiredItemsForSpace()` (per user request — reuse a matching item already in the target room, else create a real duplicate, per required item); `buildRoutineCopy()` (the shared core building one routine copy with its items resolved and its asset link either forced — used by the asset cascade — or passively looked up — used by a standalone routine duplicate). `duplicateAsset()` now loops every routine with `routine.assetId === asset.id`, duplicates each via `buildRoutineCopy` with the new asset's id forced in, and re-points `serviceRoutineId` to whichever new routine corresponds to the original's.
- **Return shapes changed** from a raw copied record to `{routine/asset, itemsCreated, itemsReused}` (asset also includes `routines: []`) so the calling UI can build an honest summary toast instead of a bare "duplicated" — e.g. `"Asset duplicated (+ 2 routines, 1 item created, 1 reused)"`. Updated both call sites (`routine.js`, `assets.js`) accordingly.
- **Still open, not changed this round**: duplicated items keep the source's current stock quantity, and duplicated assets keep the source's service history/last-serviced date — both flagged again as questionable (a new room's stock/service history didn't actually happen there) but left as-is pending explicit confirmation.
- Verified live end-to-end with a full synthetic scenario (disposable test records, deleted after): an asset with 2 linked routines, one requiring 2 items (one with a same-catalog match already in the target room, one without) — duplicating the asset correctly produced a new asset id, both routines duplicated with new ids pointing at the new asset, `serviceRoutineId` correctly re-pointed, the matched item reused (exact same id, no duplicate created) and the unmatched item freshly duplicated. Separately verified a standalone routine duplicate's passive asset-reuse both ways — correctly linked when a matching asset existed in the target room, correctly left `null` when it didn't. A real UI click-through (Assets screen → Duplicate button → pick room → save) confirmed the toast read exactly `"Asset duplicated (+ 1 routine)"`. Zero console errors throughout.

## Round 29 — routine/item/asset duplication across rooms, filter-row alignment fix (added 2026-08-10, same session)

User follow-up: Round 28's filter-row fix kept the chips on one line but they weren't left-aligned flush with the Routines/Tasks toggle row above; and a new ask — duplicate an existing routine/item/asset into a different room, as its own genuinely separate record ("these are unique assets, items and routines for those rooms so need to have unique ids").

- **Filter row alignment, the real cause found** — `routinesFilterRowHtml()`'s output wasn't wrapped in a `.today-section` the way the Routines/Tasks kind-toggle row above it was, so it was missing that wrapper's 14px horizontal padding entirely — the filter chips sat flush against the screen edge while the toggle chips above them sat inset. Fixed by wrapping the filter row in its own `.today-section` (`padding-top:0` to keep the two rows visually grouped, no wasted gap). Verified live: both rows' left edges now measured pixel-identical.
- **Duplicate to another room — routines, stock items, and assets** (`js/state.js`, `js/ui/components.js`, `routine.js`/`stock.js`/`assets.js`) — each entity's edit sheet gained a "Duplicate to another room" button (existing records only, not shown on Add). Clicking it opens a small shared room-picker sub-sheet (`openDuplicateToRoomSheet()`, one helper in `components.js` rather than three near-identical ones) listing every other visible room; picking one creates a genuine full copy with its own fresh id (`genId()`, same as any new record) and the chosen `spaceId`, then closes back out with a toast. Each entity's own `duplicate*()` function in `state.js` (`duplicateRoutine`/`duplicateItem`/`duplicateAsset`) decides what a "clean" copy means for that type:
  - **Routines**: `assetId`/`requiresItemIds` cleared — those point at the SOURCE room's own asset/stock records, which don't exist in the target room; the duplicate starts unlinked (re-link by hand if the target room has its own matching asset/items) and gets a fresh occurrence via the normal `regenerate()` pass, same as any newly added routine.
  - **Stock items**: same duplicate-guard a fresh add already has (Round 7) — refuses (with a toast) to create a second tracked record if the target room already has that exact catalog item, since that's almost always an accidental double-add rather than intent, same reasoning as the original guard.
  - **Assets**: never blocked — Round 7's own call stands ("two ACs in one room is legitimate"), so duplicating across rooms is unrestricted too. `serviceRoutineId` cleared (points at a routine tied to the source asset's own id); the duplicate starts clean and gets its own linked service routine the next time its service interval is set, same as any brand-new asset.
  - All three logged to the new Activity log (Round 26) as their own `*_duplicated` type, same as every other mutator.
- Verified live end-to-end against the real household, each with a disposable test duplicate created then deleted immediately after: a routine ("Clean Balcony") duplicated into Kitchen with a new id and cleared `assetId`; a stock item ("Toilet cleaner") duplicated from Bathroom ENTR into Bathroom MBR with its own new id, and separately confirmed the duplicate-guard correctly blocks (with the exact toast text) trying to duplicate it into a room that already has one; an asset ("Split AC") duplicated into Kitchen with a new id and cleared `serviceRoutineId`. Zero console errors throughout. One recurring environment-only issue hit repeatedly during this round's verification, not a real bug: this dev session's browser had stale disk-cached copies of `components.js`/`stock.js`/`assets.js` from earlier in the same long session, at one point throwing a real `SyntaxError` (a missing-export mismatch between an old cached `components.js` and a freshly-loaded `routine.js` expecting the new `openDuplicateToRoomSheet` export) — resolved each time by an explicit `fetch(url, {cache:"reload"})` followed by a fresh navigation; the deployed app's own service worker is unaffected (confirmed network-first in Round 14) and end users don't hit this.

## Round 28 — Routines & Tasks filter row fits one line, More menu grouped into logical sections (added 2026-08-10, same session)

User follow-up: the new Routines & Tasks screen's filter chips (Active/Paused/All/By space/By category) weren't staying on one line, left-aligned; and More's item order was "just there" — never deliberately ordered.

- **Routines & Tasks filter row now fixed to one line, left-aligned** (`app.css`) — `.member-filter-row` (originally sized for 2-3 member-filter chips) didn't reliably fit Routines & Tasks' 5 chips plus a divider on a narrow phone. Added a scoped `.routines-filter-row .chip` override (smaller padding/min-height/font, same tightened-chip technique already used for Today's own chip row) plus explicit `flex-wrap: nowrap` and `justify-content: flex-start` on the shared `.member-filter-row` base. Verified live: all 5 chips confirmed on a single line (one unique top offset) with the first chip's left edge flush against the row's own left edge (zero gap).
- **More menu grouped into three labeled sections, in a deliberate order** (`js/boot.js`) — Manage (Houses → People & Household → Assets → Routines & Tasks, top-down through the household's own structure: houses contain spaces, spaces have people/assets/routines in them) → Settings (Load smoothing, Notifications) → History (Activity log, last — a look-back utility, not a day-to-day destination). `MORE_ITEMS` gained a `section` field; a new `moreSectionsHtml()` renders a small eyebrow header whenever the section changes, reusing the existing `.eyebrow` label style rather than inventing new visual language. Verified live: row order and section headers both confirmed exactly as specified.

## Round 27 — a full browsable Routines & Tasks list, grouped by space or category (added 2026-08-10, same session)

User follow-up: "in the more can we also add a full routine/task list clustered or categorised in a nice way.. currently we dont have that anywhere" — Today only ever shows what's currently due/overdue, and House only shows a space's routines one room at a time; there was genuinely no single screen listing every routine and task in the household at once.

- **New `js/routes/routinesTasks.js`, reached via More → "Routines & Tasks"** — a Routines/Tasks kind toggle, and for Routines specifically a second choice of how to group them: **by space** (alphabetical, house-tagged when more than one house is active — reuses the same `sortSpaces()` helper already duplicated in `routine.js`/`stock.js`/`assets.js`, sorted within each space by consequence-tier severity then title) or **by category** (Unsafe/Damaging/Unhygienic/Cosmetic section headers, severity-ordered, alphabetical within each). An Active/Paused/All filter sits alongside. Tasks group by their own natural status buckets (Overdue/Due today/Upcoming/Done) — no grouping choice needed there, status already is the one useful clustering. Rows reuse the same `.occ-row`/`data-tier` tint styling Today already uses, so a routine's consequence tier reads the same way here as it does everywhere else in the app. Tapping any row opens the exact same shared edit sheet (`routine.js`'s `openRoutineEditor`) Today/House already use — full edit/pause/delete capability, not a read-only list. A "+ Add" button in the topbar opens the same sheet fresh, defaulting to whichever kind (Routine/Task) is currently selected.
- Habits deliberately excluded — the request was specifically "routine/task," and habits already have their own dedicated tracking view (People & Household / Insights).
- Verified live against the real active household: space-grouping and category-grouping both confirmed producing correct section counts and ordering (5 space sections totaling 13 routines; category-grouping correctly showed only the tiers actually present, Unsafe correctly omitted since none existed); 3 disposable test tasks (overdue/due-today/upcoming) confirmed landing in their correct status buckets, then deleted; tapping both a routine row and a task row confirmed opening the shared edit sheet correctly pre-filled with that exact record; the Add button confirmed opening a fresh (not pre-filled) sheet. Zero console errors throughout.

## Round 26 — depletion steps by whole days only, real Off state for load smoothing, and a new Activity log (added 2026-08-10, same session)

User follow-up: asked whether they'd see fractional (30min/2hr-scale) depletion amounts as the day goes on, and said that's not wanted — just step down once per day; asked for load smoothing's settings to be restructured as a real On/Off toggle with an Automatic/Manual sub-choice nested under On, defaulting to Manual the first time it's ever switched on; and asked for a new activity log in More tracking every kind of change — asset creations, routine/habit/task adds/edits/completions (by the user or by Kasa automatically), depletions, "any changes in the app" — with a request to first list what's trackable, define a clustering strategy for readability, then implement it.

- **Auto-depletion now steps by whole days, not a continuous fraction** (`js/state.js`'s `applyAutoDepletion()`) — was `elapsedDays = (now - lastDepletedAt) / 86400000`, applied as a continuous real number every time state was read, so checking the same item at 11pm and again at 11:30pm the same night showed two different, both-fractional quantities. Now floors to whole days (`Math.floor(elapsedMs / 86400000)`) and only acts once that's ≥1 — `lastDepletedAt` advances by exactly that many whole days (not to "now"), so a partial remainder correctly carries over rather than being lost. Checking twice in the same day now always shows the identical number; it only ever steps down once a full 24 hours has passed. This also structurally subsumes Round 24's `MIN_DEPLETION_ELAPSED_MS` 60-second reentrancy floor (removed, now redundant) — a synchronous re-entrant call microseconds later can never see a full day elapsed either way. Verified live: an item checked 30 minutes after its last checkpoint showed no change at all; forcing a 3.7-day-old checkpoint correctly depleted exactly `floor(3.7) = 3` days' worth in one step.
- **Load smoothing: a real Off state, not just Automatic/Manual** (`js/state.js`, `js/boot.js`) — `household.smoothingMode` gained a genuine `"off"` value alongside the existing `"auto"`/`"manual"` (`regenerate()`'s automatic pass and Today's "Smoothen" chip both already only special-cased specific values, so adding a third that skips both was a small, safe change). The More → Load smoothing sheet was restructured to match: an On/Off chip first, and — only when On — a Manual/Automatic sub-choice nested underneath. Per direct request, the sub-choice defaults to Manual whenever the household is *currently* off and gets switched on in that same sheet visit; re-opening the sheet on an already-on household preserves whatever mode it was actually in. Verified live: starting from Off, toggling to On defaulted the sub-field to "Manual — I'll tap Smoothen" exactly as specified; the household's own live setting was restored to its original "Automatic" afterward.
- **Activity log — a new feature, `js/routes/activity.js`, reached via More** — logs virtually every meaningful state change, `actorKind` distinguishing something the signed-in person did ("user") from something Kasa decided on its own ("kasa": auto-depletion, automatic load smoothing). Tracked, per the "list everything first" ask: routines (add/edit/delete/complete/snooze/pause/resume), habits (add/edit/delete/checked/unchecked), tasks (add/edit/delete/complete/uncomplete), stock items (add/edit/delete/manual qty adjust/auto-depletion), assets (add/edit/delete/serviced), spaces (add/rename/delete), houses (add/rename/delete), people (add/edit/remove), wishlist (add/edit/delete/acquired), and household-level settings (smoothing mode, household mode switch, smoothing runs). Deliberately NOT logged: schema migrations (an internal technical event, not something a household needs to see in "what happened in my house") and occurrence generation itself (derived/regenerated every session, not a real event). `state.js`'s `logActivity()` is called directly inside each mutator, at the actual point of change — never inferred after the fact, since that's how a log quietly goes wrong. Persisted through the existing Firestore sync unchanged (it's just another top-level `state` field, included automatically since `serializeState()` only explicitly excludes `occurrences`/`lastSmoothingMoves`) but capped at 500 entries (oldest dropped first) — this household's entire state still lives in one Firestore document (see the locked-decisions table), and an unbounded log would eventually threaten the 1MiB ceiling that decision accepted as safe for normal household data. New `js/migrations.js` step (v5→v6, `CURRENT_SCHEMA_VERSION` now 6) backfills `activityLog: []` for any household saved before this shipped.
  - **Clustering strategy** (the part actually asked to be "defined"): group by calendar day first (Today/Yesterday/weekday name/date), then by category within a day (Routines/Habits/Tasks/Stock/Assets/House/Household/People/Wishlist, each with its own icon), then — the actual collapsing rule — any *exact type* (e.g. "routine added," "item auto-depleted") with 3 or more entries on the same day collapses into one summary row ("4 routines added," tap to expand into the individual entries) instead of listing each one; anything under 3 of a kind just lists individually, since there's nothing gained collapsing two things into "2 things." This specifically targets the real failure mode a flat, uncollapsed log would have here — a room-template "Add space" or onboarding pass can create dozens of same-type entries in one sitting, and without collapsing, that one moment would bury everything else that happened that day.
  - **UI**: filter chips (All / whichever categories actually have entries / "By Kasa" to isolate automatic actions) above a day-grouped, clustered list — reuses the app's existing `.list-row`/`.today-section`/chip-filter visual language rather than inventing new components. New `activityLog` icon (a clock face) added to `js/ui/icons.js` in the same hand-drawn 24×24/1.8-stroke technique as the rest of the set; category rows reuse existing icons (routine/flame for habits/task/stock/warranty for assets/house/person/wishlist), and the `sparkle` glyph (already established elsewhere in the design system for "AI/automatic") tags any row where `actorKind === "kasa"`.
  - Verified live end-to-end: a disposable test item's add/edit/auto-deplete/delete cycle all appeared correctly attributed (user vs. Kasa); 4 disposable test routines added in a row correctly collapsed into one expandable "4 routines added" cluster, and expanding it, then applying the "Routines" category filter, correctly kept it expanded and showed each one's real title; zero console errors throughout a full regression pass across Today/House/Stock/Insights/Wishlist plus the new screen itself.

## Round 25 — load smoothing narrowed to cosmetic-only, re-verified the white-space chip-group fix under touch events (added 2026-08-10, same session)

User report: a weekly "unhygienic"-tier routine, missed yesterday, should have shown as overdue today but instead had silently moved a week out; also asked to re-check whether the white-space chip-group reset (Round 23) is truly fixed, since it looked like it was still happening on mobile; and asked for a plain explanation of how time-based auto-depletion actually behaves (concretely: add 2 units at 11pm, deplete 1/day — what happens when).

- **Load smoothing (§5.7) narrowed from cosmetic-or-unhygienic to cosmetic-only** (`js/intel.js`) — root cause wasn't a bug in the overdue-exclusion guard (that check, `stateOf(...) !== "overdue"`, was already correct and verified still correct) — it's that smoothing evaluates occurrences *before* they go overdue too, and "unhygienic" had carried smoothing-eligibility forward from the old pre-Round-26 "degrading" tier, which was vaguer than what "unhygienic" specifically means now (cleanliness/water/air/pests). A missed weekly unhygienic routine got its due date silently pushed +7 days by the load-smoothing pass on an earlier day (while it was still "due", not yet overdue) — by the time "today" arrived, its real due date already pointed a week out, so it correctly never showed as overdue, just not in the way the user expected or wanted. Fixed by restricting smoothing eligibility to `consequence === "cosmetic"` only — genuinely inconsequential routines (dust a shelf, wipe a table) can still be nudged a week to balance load; anything with a real hygiene/health consequence no longer can. No migration needed — occurrences aren't persisted (`serializeState()` excludes them, regenerated fresh every session from routines+ledger), so the fix self-heals the next time each household's `regenerate()` runs. Verified directly: a synthetic unhygienic weekly routine forced into an over-ceiling week no longer moves (`wasMoved: false`), while cosmetic-tier occurrences in the same test still did (15 other moves happened normally).
- **White-space chip-group reset (Round 23) re-verified, confirmed still fixed** — re-tested from scratch on a guaranteed-fresh module load (not reusing any earlier test tab, to rule out this session's own repeated stale-cache artifacts), and this time with `PointerEvent`s carrying `pointerType: "touch"` (pointerdown/pointerup/click) dispatched directly on both the chip-group container and its `.field` wrapper, rather than only a generic mouse `click` — closer to how a real phone actually dispatches a tap. Selection held correctly (`unchanged: true`) under this touch-style sequence too. Also re-confirmed via a fresh grep that no `<label>` remains anywhere in `js/` other than the comment documenting why the fix exists — the bug has exactly one root cause and it's fixed at the source. The most likely explanation for it still appearing on a real phone is the same class of issue this session's own testing kept running into: a mobile browser/PWA session holding an old cached copy of `components.js` from before the fix shipped. Told the user directly: force-close and reopen the app (or a manual pull-to-refresh) should pick up the fix, since the service worker (`sw.js`) is already network-first, not cache-first, for JS/CSS (see Round 14) — it's the browser's own underlying HTTP cache holding a page instance open from before the deploy, not anything the app itself is doing wrong.
- **How auto-depletion actually behaves, explained** (`js/state.js`'s `applyAutoDepletion()`, unchanged) — not a running clock or a midnight rollover; it's a lazy comparison against real elapsed wall-clock time, checked whenever `getState()` is next called (e.g., opening the app, or any interaction). Concretely, for the user's own example: add 2 units at 11pm with a "deplete 1/day" rate — `lastDepletedAt` gets stamped to that 11pm timestamp. The next time state is read (could be seconds later if the app's still open, or days later if not), it computes exactly how much real time has passed since that 11pm stamp and depletes `rate × elapsedDays` in one step — so checking at 11:30pm the same night shows ~0.02 depleted (half an hour's worth), checking the next evening at 9pm shows ~0.92 depleted (22 hours' worth), and checking 3 days later in one go correctly shows ~3 depleted, not capped at "1 per calendar day" or requiring the app to have been opened on each of those days. There's no calendar-day boundary involved at all — it's a genuine rolling 24-hour-equivalent rate against whenever the last check happened, which is also exactly why Round 24's fix needed a 60-second floor (see above) rather than removing the time-check entirely.
- Verified live end-to-end: smoothing eligibility change confirmed via a disposable synthetic routine (created, tested, deleted — never left in the shared household); white-space fix re-confirmed under simulated touch events on a verified fresh module load; zero console errors throughout.

## Round 24 — real root cause found for Today's chip-row "needs several clicks" bug: a self-triggering notify() cascade (added 2026-08-09/10, same session)

User follow-up on Round 23's chip-row overflow fix: clicking Batches/10-min/Smoothen to toggle it back off "doesn't happen immediately... only after several times it toggles" — a real, distinct bug from Round 23's overflow/truncation fix, not fully explained by it. Also asked to shorten "10 free minutes?" to "10 free min?".

- **Root cause: `applyAutoDepletion()` was re-triggering itself in a synchronous cascade on every single click, anywhere on Today** (`js/state.js`) — found via direct instrumentation (temporary `console.log(new Error().stack)` inside `notify()`, removed after diagnosis), not guesswork. Sequence: a chip click calls `render()` directly → `render()`'s own `getState()` call runs `applyAutoDepletion()`, which (for any item with auto-deplete on) always found a *technically* nonzero elapsed time since `lastDepletedAt` (even a few microseconds counts as `elapsedDays > 0`), depleted a negligible amount, stamped `lastDepletedAt` to "now", and called `notify()` → `notify()` synchronously re-invokes every subscriber including Today's own `render` again → that nested render's `wireEvents()` calls `getState()` yet again → same story, a few more microseconds have passed since the checkpoint was JUST stamped, so it's still "elapsed," `notify()` fires again → repeats. The existing `applyingDepletion` reentrancy guard only blocks a call nested *directly inside* the current `notify()`'s call stack — it does nothing once that `notify()` returns and control unwinds back to the outer `render()`'s own `wireEvents()`, which calls `getState()` fresh and finds yet another sliver of "elapsed" time. Measured directly: a single click produced 4 extra `notify()` calls and **14 separate DOM replacements** of the whole Today screen in under 300ms — each one tearing down and rebinding the chip row's click listeners out from under whatever the user did next, which is what actually produced the "needs several clicks" feel (a genuinely fast double-click, or even the same physical click's own event dispatch, could land mid-cascade on a button that's about to be replaced).
- **Fix**: added a minimum meaningful elapsed-time floor (`MIN_DEPLETION_ELAPSED_MS = 60000`, i.e. 60 real seconds) before `applyAutoDepletion()` treats any elapsed time as worth acting on. Real stock depletion only ever needs day-scale granularity — a 60-second floor is invisible to genuine catch-up (closing the app for hours/days still works exactly as before, see Round 23's verified catch-up test) but comfortably larger than any synchronous re-entrant call gap (which is always microseconds), so the self-triggering loop can't start. Verified live: the same instrumented test that showed 4 notify()/14 replacements per click now shows **0 notify()/1 replacement** per click, and 5 rapid-fire clicks on Batches each toggled correctly on the first try with only a 50ms gap between them (previously flaky under that timing).
- **Label shortened** (`js/routes/today.js`) — "10 free minutes?" → "10 free min?", per direct request. Updated the two code comments referencing the old label text too (`today.js`, `intel.js`) for consistency; left the dated historical comments in `app.css` from Round 23 as-authored since they correctly describe what was true at the time they were written.
- Verified live end-to-end: fresh-tab reproduction of the original bug (4 notify()/14 replacements per click, toggle sometimes not visibly flipping) confirmed via direct stack-trace instrumentation before the fix; same test confirmed clean (0/1) after; 5-click rapid-fire toggle test all passed; label text confirmed rendering as "10 free min?" after a full cache-clear reload. Zero console errors throughout. Temporary debug instrumentation (`console.log` in `notify()`) was removed before finalizing.

## Round 23 — label-wrapped chip-group click bug, stock auto-deplete checkpoint reset, binary toggle sizing, today-chip-row overflow (added 2026-08-09, same session)

User reported a batch of 7 bugs from live use, spanning several screens. Note: this session had already moved the Stock consumption model to a "how many X does 1 unit last" (`consumeMode`: off/day/month/usage) design and added Today's `.today-chip-row` (10 free minutes?/Today/This week/Batches/Smoothen) plus a manual-smoothing mode, in rounds between Round 22 and this one that weren't written back to this file before a context compaction — visible now only via in-code comments dated 2026-08-08. This entry covers only what was verified and fixed in the current session.

- **Root cause found for a real, hard-to-explain bug: clicking blank space in a routine's Space field silently reset the selection to the alphabetically-first space** (`js/ui/components.js`) — extensive live debugging (mutation observers, event-listener monkey-patches, isolated minimal reproductions) eventually traced it to the HTML spec itself, not app logic: `field()` wrapped every sheet field — including multi-button chip-groups — in a `<label class="field">` with no `for` attribute. Per spec, a label with no `for` forwards any click on itself (including blank padding) to its first labelable descendant — for a chip-group that's the first `<button>`. Confirmed directly: a click dispatched with `bubbles:false` never reproduced the bug (never reaches the label ancestor); the same click with `bubbles:true` reliably did. Fixed by changing `field()` to render a plain `<div class="field">` instead of a `<label>` — `.field`'s CSS selectors are element-agnostic so no styling broke, and nothing in the codebase relied on the implicit label-focuses-input behavior for single-input fields. This one fix also explained a second, separately-reported symptom (below).
- **Stock auto-deplete: editing an already-running item's quantity got silently shaved down to a decimal** (`js/routes/stock.js`) — root cause: saving an edit to an item that already had `autoDeplete` running kept its *existing* `lastDepletedAt` checkpoint (by design, so an unrelated edit like a rename wouldn't reset the depletion clock) — but that meant typing a fresh whole-number quantity (e.g. restocking to 10) still carried the OLD checkpoint's already-elapsed time, which got applied against the NEW number the moment state was next read. Fixed by also resetting the checkpoint to now whenever the saved quantity actually differs from the item's pre-edit quantity (`qtyChanged` check), while still preserving the old checkpoint for a genuinely unrelated edit with quantity untouched.
- **Auto-deplete catch-up behavior, explained and verified** (`js/state.js`'s `applyAutoDepletion()`, unchanged) — answered directly: no, the app doesn't need to be opened every day. Depletion is driven by comparing real elapsed wall-clock time against each item's own `lastDepletedAt` timestamp on every `getState()` read, not a running timer — so closing the app for several days and reopening it correctly applies the full elapsed depletion in one step. Verified directly: an item seeded with a `lastDepletedAt` 3 days in the past and `burnRate:1` correctly dropped from qty 10 to ~7 on the next state read.
- **Binary stock item's out-of-stock control** — investigated as a possible separate bug, but direct live testing (toggle click, both directions) showed the Stock screen's own binary toggle logic (`js/routes/stock.js`) was already correct end-to-end (label, `aria-pressed`, bucket, and status all updated correctly). The reported "mark out of stock also shows as in stock" is best explained by the same `<label>` click-forwarding bug above — the item's own edit sheet uses the identical `field()`-wrapped chip-group for "In stock? Yes/No," so selecting "No" and then clicking anywhere else in that field's blank area before saving would have silently flipped it back to "Yes." Fixed by the same `components.js` change; no separate logic fix needed. Also shrank the toggle button itself (`app.css`) from the generic `.chip`'s 44px tap-target floor to a smaller 30px-tall variant scoped to `.tile-stepper .chip`, per the explicit "reduce the size" ask.
- **Today's chip row (10 free minutes?/Today/This week/Batches/Smoothen): mobile text cutoff and a possible click-target problem** (`app.css`) — two real issues in the same rule: (1) all 5 chips shared equal `flex:1`, so "10 free minutes?" — by far the longest label — truncated hardest; fixed with per-chip `flex-grow` weights roughly proportional to label length. (2) the row never set `min-width:0` on its chips — flex items default to `min-width:auto` (their intrinsic content size), which silently overrides `flex-basis:0%` and can let the row's true content overflow past its visible, clipped container, leaving a chip's real clickable box offset from what's actually rendered on screen — a plausible explanation for a "click problem" distinct from the text cutoff. Verified live at a true 375px mobile width with the actual sub-600px font-size/padding: all 5 chips now render fully un-truncated on one line with no overlapping boxes.
- Verified live end-to-end for all 7 reported items, each via direct DOM/state inspection rather than visual-only checks: the label-forwarding fix (chip-group container click no longer resets selection, confirmed both via a synthetic dispatch and via a fresh module import of the fixed component in isolation); wishlist's Space field confirmed alphabetically sorted with a house-name hint per option (`sp_...` list rendered as Balcony/Bathroom/Bedroom/.../Whole home, each tagged "My Home"); the Today stat tiles' "Due today" tile confirmed switching the view from "week" back to "today" on click; the stock quantity-reset fix confirmed via a seeded 3-days-stale checkpoint correctly catching up in one state read; the binary toggle confirmed correct in both directions with a disposable test item (created and cleaned up on the live, Firestore-synced household — deleted immediately after); the chip-row fix confirmed via exact-pixel measurement at simulated mobile width. Zero console errors throughout. Temporary debug instrumentation (`console.trace` added mid-session to `routine.js` while tracing the click bug) was removed before this round's changes were finalized.

## Round 22 — reverted Round 21's smoothing exemption for a real Saturday default, service-routine auto-creation, manual smoothing toggle, calendar-schedule multi-weekday (added 2026-08-07, same session)

User follow-up on Round 21: disliked the "never smooth a routine until it's been completed once" fix ("i dont line at least once compelted. so revert this back") and clarified what was actually wanted — a new asset's suggested routines should always default their first occurrence to the following Saturday, not today, as a deliberate scheduling choice rather than a smoothing workaround; when an asset's "Service every N days" field is filled in, that should now actually create a linked routine (plus ask for an optional last-service date to seed the calculation), and clearing the interval should remove the button/routine rather than leaving a dangling one; load smoothing should be switchable between Automatic and Manual (manual surfacing a one-tap "Smoothen" trigger on Today, on the same row as Batches); and the calendar-schedule trigger UI should hide whichever of Day-of-month/Day-of-week doesn't apply to the selected cadence, and allow selecting more than one weekday.

- **Round 21's smoothing exemption reverted** (`intel.js`) — `applyLoadSmoothing()`'s ledger-completion check (excluding a routine's occurrences from smoothing until it's been done at least once) is gone; smoothing evaluates every open, non-overdue occurrence again, same as before Round 21. Reverted per direct instruction, not because the old bug came back.
- **Real fix: new asset routines start next Saturday** (`state.js`, `assets.js`, `house.js`) — new `nextSaturdayDateStr()` in `state.js`. Every suggested-routine template created off a catalog asset (both at asset-creation time in `assets.js` and via House's room-template cascade in `house.js`) now gets its `floating_since_last` trigger's `startDate` stamped to the next Saturday instead of being left unset (which `engine.js` defaults to "due right away" — the actual root cause of Round 21's "always Saturday" complaint, now addressed as an intentional default rather than patched around in the smoothing algorithm). Non-`floating_since_last` trigger types (e.g. `usage_meter`) are untouched.
- **Service-routine auto-creation from "Service every N days"** (`assets.js`, `state.js`) — filling in a positive `serviceIntervalDays` on an asset now creates (or updates) a real linked routine automatically, titled "Service {asset name}", `floating_since_last` with `intervalDays` = the entered number; a new optional "Last service date" field (shown only once the interval field has a value) seeds the routine's `startDate` as `lastServicedAt + intervalDays` when given, else falls back to next Saturday. The asset's own `serviceRoutineId` (new field) tracks the link so re-saving updates the same routine's trigger rather than creating duplicates, and clearing the interval back to blank/zero deletes the linked routine and clears `serviceRoutineId`. The "Mark serviced" button is now conditional on `serviceIntervalDays > 0` — an asset with no service schedule has nothing to mark serviced against, and previously showed the button anyway with no routine backing it. `markAssetServiced()` also now completes the linked routine's open occurrence (not just the asset's own `lastServicedAt`/meter fields), so servicing an asset from its own detail screen and completing its Today row both keep the same state in sync. This directly answers the question Round 21 left open ("does the interval field create a routine?") — yes, now.
- **Manual/Automatic smoothing toggle** (`boot.js`, `state.js`, `today.js`) — a new More → "Load smoothing" sheet (Automatic/Manual chip, defaulting to Automatic via migration step 4, `CURRENT_SCHEMA_VERSION` bumped 3→4). Automatic keeps `regenerate()` running `applyLoadSmoothing()` on every mutation, unchanged. Manual stops that and instead shows a "Smoothen" chip on Today, next to "Batches" and the existing "10 free minutes?"/"Today"/"This week" chips, all on one row — clicking it calls a new `runSmoothingNow()` and toasts how many occurrences moved (or that nothing needed rescheduling). The chip only renders when the household's mode is actually Manual, so Automatic households see the same row as before.
- **Calendar-schedule trigger UI fixes** (`routine.js`) — Cadence (Monthly/Weekly) now shows only the relevant field: Day-of-month hidden when Weekly is selected, Day(s)-of-week hidden when Monthly is selected, toggled live on the Cadence chip's click handler (mirrors the existing pattern already used for the Kind toggle and trigger-type swaps). Day(s)-of-week is now a multi-select chip group (was single-select) — `engine.js`'s `nextRRuleOccurrence` rewritten to parse a comma-separated `BYDAY` list and return whichever selected weekday comes up soonest, so "Mon,Thu" is due on whichever of the two arrives first, not just the first day in the list.
- Verified live end-to-end: service-routine creation/update/deletion cycle confirmed via direct state inspection (interval=90 + last-serviced date correctly computed a `startDate` 90 days out; clearing the interval correctly deleted the routine and reset `serviceRoutineId`; "Mark serviced" button presence tracked `serviceIntervalDays > 0` exactly); smoothing toggle confirmed switching to Manual correctly surfaces "Smoothen" next to Batches on the same row, and running it either reports a move count or "Nothing needed rescheduling"; calendar-schedule UI confirmed Monthly/Weekly field-hiding both directions, and multi-weekday selection confirmed end-to-end with a real saved routine (`FREQ=WEEKLY;BYDAY=MO,TH`, generated occurrence correctly due on the nearer of the two selected days). One test-process note, not a code bug: an initial manual screen-coordinate click meant to deselect Sunday during UI testing missed its target and left a stray `SU` in the saved rrule despite looking visually deselected in the screenshot; re-run with precise DOM-level clicks on the actual chip buttons confirmed the underlying multi-select toggle logic was correct all along. Zero console errors throughout.

## Round 21 — load-smoothing bug on brand-new routines, duplicate suggested routines, date-input sizing (added 2026-08-07, same session)

User report, four bugs plus one question, from live use: date input boxes visibly taller than other fields across sheets; asked whether editing an existing routine's start date to a future date actually moves it; reported new assets' suggested routines "always" land on the following Saturday; reported suggested routines re-offered (and re-created as duplicates) even when already linked to an asset; asked whether "Service every N days" on an asset creates an actual Today-visible routine.

- **Root cause found for "always lands on Saturday"** — a real bug, not a hardcoded day, reproduced directly: added a Geyser asset with its two suggested routines (`js/catalog.js`'s `AST-GEYSER` entry) and found "Service geyser" landed a full week out from its correct due date (today) while its sibling routine correctly landed today. `intel.js`'s `applyLoadSmoothing()` (§5.7) had no concept of "this occurrence was only just generated" — it evaluates every open occurrence in the current week against the household's effort ceiling, with zero exemption for a routine's own first-ever appearance. If the household's current week already happens to be near/over the ceiling (easy for a household with any real history), a brand-new asset's suggested routine gets shoved into whichever week has headroom before the user has ever seen it once — and since a given household's own due-date distribution is fairly stable session to session, that "week with headroom" reliably resolves to the same day of the week, reading as an arbitrary fixed day.
  - **First fix attempt was insufficient**: excluding only occurrences generated within the *same* `regenerate()` call missed the real case — an asset's several suggested routines are each added via their own `addRoutine()` call (and therefore their own `regenerate()` pass), so routine #1's occurrence had already stopped being "this call's fresh output" by the time routine #2's own call ran, and was smoothable again by their combined effort.
  - **Real fix**: `applyLoadSmoothing()` now excludes any occurrence whose routine has never been completed (checked against the ledger, not against timing) — a routine only becomes smoothing-eligible once it's actually been done at least once. Session/timing-independent, correctly protects a whole multi-routine batch regardless of how many separate calls it takes. Verified live: re-ran the same Geyser-asset reproduction after the fix, both suggested routines correctly landed on today, `occ.smoothed` false on both.
- **Duplicate suggested routines fixed** (`assets.js`) — "+ Add suggested routines" (and the pre-checked list shown when first linking a catalog asset) now excludes any template whose title is already a real routine linked to this asset, both at render time (`renderSuggestedRoutines()` gained an `existingTitles` filter) and again defensively right at save (in case anything changed in between). The button itself now only appears at all if there's at least one *not-yet-added* suggested routine to offer — an asset with everything already linked shows nothing to re-add. Verified live: an asset with both suggested routines already linked shows no "+ Add suggested routines" button at all; deleting one of the two brings the button back showing only that one.
- **Editing an existing routine's start date now moves its occurrence** (`state.js`'s `updateRoutine`) — previously `startDate` only ever affected a routine's very first computed occurrence at *creation* time (Round 19); editing an *already-existing*, never-yet-completed routine's start date left its already-generated occurrence stale, since `regenerate()`'s "no open occurrence exists" rule means the engine never revisits a routine that already has one open. Now: if the edited routine is `floating_since_last`, has a `startDate`, and has never been completed, its current open occurrence's `dueAt` is moved to match directly. Once the routine's been completed once, this stops applying — engine.js's normal floating-since-last-completion logic takes back over, unchanged. Verified live via direct occurrence inspection: a routine due today, edited to a `2026-10-01` start date, had its existing occurrence's `dueAt` move to `2026-10-01` in place (same occurrence id, not a new one).
- **Date/time input sizing** (`app.css`'s `.text-input`) — switched from `min-height` alone to an explicit `height` (plus `box-sizing:border-box` and a normalized `::-webkit-calendar-picker-indicator`), since native `type="date"`/`type="time"` controls render their own internal calendar/clock-icon UI that can compute a taller intrinsic content box than a plain `type="text"` input on some browsers (especially mobile WebKit) even with identical CSS padding — `min-height` doesn't prevent that, only an explicit `height` reliably caps it. Not independently reproducible in this session's desktop Chrome (both types measured identically there), but a standard, well-documented class of cross-browser inconsistency and a safe fix regardless.
- **Answered, not built**: "Service every N days" (`assets.js`'s asset-level service-interval field) only ever sets `asset.serviceIntervalDays`/computes `nextServiceDue` for the Assets screen's own "Service due in N days" display — it does **not** create any routine or Today-visible occurrence by itself. The only thing that creates a real, Today-visible service reminder is a *suggested routine* actually being added (either at asset-creation time or via "+ Add suggested routines" later) — a separate, parallel mechanism from the plain interval field. Flagged to the user as a real product gap worth deciding on, not fixed unprompted since it's a design choice (should setting the interval auto-link a routine, or should they stay intentionally decoupled — an asset you're just tracking warranty/service-history for vs. one you also want nagging you).
- Verified live end-to-end throughout; zero console errors.

## Round 20 — real-time multi-device sync, real mid-session household switching, performance pass (added 2026-08-06, same session)

User asked for three specific pending items to be executed (real-time multi-device sync, the mid-session "join a different household" stub, and Phase 7's performance pass), with everything else explicitly left in pending.

- **Real-time multi-device sync** (`db.js`) — `startAutoSave()` now also opens an `onSnapshot` listener on the same `data/main` document it writes to, so a change made on one signed-in device shows up on another device's already-open session close to immediately, not just on that device's next reload. Two guards keep it from fighting the local save loop: `snap.metadata.hasPendingWrites` skips a device's own not-yet-server-confirmed optimistic write, and a remembered `lastSavedSnapshot` (the JSON this device itself last wrote) skips the server's confirmation echo of that same write. Only a snapshot that's genuinely different from anything this device wrote triggers `hydrateState()`. Still whole-document last-write-wins, not a field-level merge (see Locked decisions) — two devices editing at the literal same moment can still have one clobber the other, but a device just sitting open now actually sees changes made elsewhere instead of only finding out on its own next reload.
- **Mid-session "join a different household" is real now** (`people.js`) — was a stub (see the old code comment, now replaced) because switching an already-syncing household to a different one needed the old household's sync stopped before the new one starts hydrating, or a stray save could land on the wrong Firestore document. Now: `joinHouseholdRemote()` → `stopAutoSave()` (old) → load + `hydrateState()` (new) → `startAutoSave()` (new). Uses `auth.js`'s existing `getCurrentUser()` directly rather than threading `currentUser` down from `boot.js`.
- **Firestore offline persistence enabled** (`firebase.js`) — `initializeFirestore()` with `persistentLocalCache()` + `persistentMultipleTabManager()`, replacing the plain `getFirestore()` call. Without this, Firestore only queued offline writes in memory for the current page session; a reload while still offline would have lost them and failed to read anything. This is what memo §12's "airplane mode: full read/write... sync on reconnect" acceptance test actually needs — the Service Worker's own offline shell (Round 14) only ever covered the static app files, never the data layer. `persistentMultipleTabManager` lets more than one Kasa tab on the same device share the cache instead of the first tab locking the rest out of it (relevant now that multi-device/multi-tab sync is real too).
- **Performance pass against memo §12's acceptance tests**:
  - *"200 routines renders Today in under 400ms on a mid-range Android device"* — tested directly: bulk-generated 200 routines (217 total, 213 occurrences after `regenerate()`), timed `today.js`'s `mount()` end to end with `performance.now()`: **13.3ms** on this session's desktop Chrome. Even budgeting a generous 5-10x slowdown for a genuinely low-end Android device, that's still comfortably under the 400ms ceiling — no optimization work was actually needed, the render path was already fast. Test routines removed after measuring.
  - *"No hardcoded hex value appears anywhere outside tokens.css"* — checked: `app.css` has none; the only hex literal anywhere in `js/`/`index.html` is `index.html`'s `<meta name="theme-color" content="#B8935A">`, which is structurally required to be a literal value (a `<meta>` attribute can't reference a CSS custom property) and already matches the real design token's own value. Confirmed pass, one unavoidable exception.
  - *"Airplane mode: full read/write, correct due dates, sync on reconnect"* — addressed architecturally (Firestore persistence above + the existing offline app shell), but not exercised with an actual physical network cutoff — no tool available this session to reliably flip real network conditions off and back on mid-test. Worth a real on-device airplane-mode pass when convenient.
  - *"Marking anything done takes exactly one gesture"* and *"a new user reaches a populated Today screen in under 90 seconds"* — both already true by construction (swipe-to-complete; archetype onboarding), not re-benchmarked this round.
- Verified live end-to-end: console clean throughout (multi-device listener setup, join-household code paths, 200-routine bulk generation/cleanup, Firestore persistence init). Real-time sync and the join flow are logically verified via code path but not exercised with two genuinely separate live devices this session — worth trying with two browser tabs/devices signed into the same household code when convenient.
- **Explicitly left pending, per direct instruction**: Apple/email-link auth, Cloud Storage + App Check, Phase 6's AI half, the Capacitor/native wrap (starting later), Phase 7's desktop three-pane layout.

## Round 19 — start-date on floating routines, house hints on space pickers, on-device notifications (added 2026-08-06, same session)

User follow-up, all four confirmed and built in one pass: a smart, small (not verbose) way to see which house a space belongs to when picking one for a routine/habit/task; confirmation that repetition is date-based (asked, not requested — answered directly, see below); a "Start date" option for `floating_since_last` routines so a new routine's first due date doesn't have to default to "due right away"; whether push notifications are possible on mobile, and if so a consolidated 7am daily summary with an on/off toggle in More.

- **How repetition actually works** (answered, not built — `js/engine.js`): entirely date-based. `floating_since_last` takes the routine's last completion timestamp from the ledger and adds the interval; `fixed_calendar` matches a day-of-month/day-of-week rule; the rest are usage/condition/season-driven. `regenerate()` runs on every app load and after any mutation, only ever creating a new occurrence for a routine that doesn't already have one open — nothing double-fires, no server timer involved.
- **Start date for `floating_since_last`** (`engine.js`, `routine.js`): a new optional `trigger.startDate` — only changes the very *first* occurrence a routine ever gets (when there's no completion history yet); once it's done once, the interval floats from the real completion date exactly as before. Verified live: a routine created with a September start date correctly generated a `pending` (not due) occurrence dated exactly that day, while an otherwise-identical routine with no start date was immediately due, as always.
- **House hints on every space picker** (`routine.js`'s shared routine/task Space field, `stock.js`, `assets.js`) — each space chip's label now carries the owning house's name as a small muted hint, via `chip-house-hint`. Shown only when the household actually has more than one house (a single-house household's chips are unchanged — nothing to disambiguate). `chipGroup()` already inserts option labels as raw HTML, so this needed no changes to the shared component itself. Went through two layout passes on direct feedback: first inline next to the room name, then absolutely positioned in the chip's own corner so the box height/width never changes regardless of the hint's presence (`.chip` gained `position:relative` as the anchor, same technique `.tile-badge` already used) — landed on bottom-center (`bottom:3px; left/right:8px; text-align:center`) as the final placement, after a top-right pass first.
- **`js/notify.js`, new** — on-device notifications, explicitly NOT true background push (user asked directly "is push possible" — answered honestly: yes in general, but real push needs a server able to wake a device on a schedule, which means Firebase Cloud Messaging + a scheduled Cloud Function + upgrading the project to the paid Blaze plan, none of which exists yet — same "Cloud Function proxy" gap already blocking Phase 6's AI features). What's genuinely buildable client-side: `Notification.requestPermission()` (must fire from a real click — browsers refuse a silent request) gates a once-a-day consolidated summary (overdue + due today + low stock, reusing `stock.js`'s own `bucketOf` so the count always matches what Stock shows), checked on load and every 5 minutes while a Kasa tab stays open, tracked via a `localStorage` "already shown today" flag so it fires at most once per device per day. `household.notifySettings` (`{enabled, time}`) is a new field — schema-version 2→3 (`migrations.js`'s second real step, `js/migrations.js`), defaulting `enabled: false` for every existing household.
- **Notifications settings sheet** (`boot.js`, new More entry) — a toggle + time picker, with the on-device-only limitation stated directly in the sheet's own copy rather than only in code comments (same "never fake completeness" rule as everything else in this project). Toggling on requests permission right there; toggling on when the browser denies/blocks it shows a toast pointing at the browser's own site settings instead of silently failing.
- **A real mistake, caught and corrected this round**: asked to "check all these yourself in auto mode," this session's own extensive state-mutation testing (fake people, houses, habits, routines) was run against what turned out to be a live, Firestore-synced household under a real signed-in test session — not the disposable local sandbox it was assumed to be. Caught by checking Firebase Authentication's user list and the actual Firestore document contents before going further; flagged directly to the user rather than continuing or attempting a silent cleanup. User confirmed the account (`xathurak@gmail.com`) was a real, disposable account set up specifically for this kind of testing — no cleanup needed, testing continued freely from there. **Lesson for any future round**: don't assume an authenticated session is a disposable local sandbox just because the same tab was showing Welcome/signed-out a few reloads earlier — Firebase Auth's persistence can silently resume a real session on any subsequent reload; check `getCurrentUser()` before doing broad state-mutation testing, not after.
- Verified live end-to-end (once the mistake above was resolved): house hints render correctly across a 3-house test household (My Home/Weekend Home/Farmhouse), reading cleanly in the actual sheet; start-date routine creation confirmed via direct occurrence inspection; the Notifications sheet renders and its toggle correctly triggers a real `Notification.requestPermission()` call (the actual browser permission grant itself is a native dialog outside any page's DOM — same category of thing as Google's OAuth consent screen, not something automatable or something this session drives on the user's behalf); `buildSummary()`/`summaryText()` confirmed producing the exact same overdue/due-today/low-stock counts Today's own stat tiles show. Zero console errors throughout.

## Round 18 — Phase 4 (Google sign-in + Firestore), spaces-only onboarding, per-house/per-person data hygiene (added 2026-08-06, same session)

User asked to actually set up Firebase ("Google login for now... apple later... storage not needed"), then reported the first save wasn't reaching Firestore, then gave a large follow-up list after verifying it worked: household should start blank and named after the signed-in person (editable); old mock tasks/habits/help were leaking into every fresh signup; block duplicate space/house names; 1RK should default to living+kitchen+whole home+utility+bath; house names should disambiguate same-named tasks/routines across houses; habits shouldn't be visible to the rest of the household.

### Phase 4 — Google sign-in + Firestore
- **`js/firebase.js`** — SDK init from the user's real `kasa-prod` config (pinned CDN version in `index.html`'s importmap, not `latest`).
- **`js/auth.js`** — Google only. `signInWithGoogle()` tries `signInWithPopup` first, falls back to `signInWithRedirect` on `auth/popup-blocked` (found live: this session's own browser profile blocks popups, and real users' browsers/embedded webviews often do too — a legitimate robustness fix, not just a workaround for testing). `completeRedirectSignIn()` finishes that round trip on the next load.
- **`js/db.js`** — Firestore persistence. `households/{code}` (metadata: name, `memberUids`, `createdAt`) + `households/{code}/data/main` (the actual state blob) + `users/{uid}` (which household a signed-in person belongs to). `startAutoSave(code)` subscribes to `state.js`'s own `notify()` and debounce-writes `data/main` on any change — zero changes needed to any existing mutator function. See the Locked decisions table for why one document, not a granular collection layout.
- **`state.js`** gained `serializeState()` (a JSON-safe snapshot, excluding derived `occurrences`/`lastSmoothingMoves`) and `hydrateState(blob)` (replaces state wholesale, then runs it through `migrate()` + `regenerate()` — a Firestore-loaded household written before a later schema change comes up current automatically, same mechanism Round 17's migrations were built for).
- **`boot.js`** rewritten around `onAuthChange`: signed in with a household on record → load + `startApp()`; signed in with none yet → the household step (join/create); not signed in → Welcome. A `showLoading()` placeholder covers the brief async gap before Firebase Auth's cached session resolves.
- **`firestore.rules`** (new, repo root) — mirrors Miso's own `households/{code}` + `memberUids` shape exactly (any signed-in user can read a household doc or create one, but can only update `memberUids` by adding themselves; `data/{key}` is member-gated). Pasted into the console by the user, not pushed via automation — **bug found and fixed live**: the user reported "data is not getting stored" after their first sign-in — root cause was simply that the rules had been written locally but never pasted/published, so Firestore's default `allow read, write: if false` silently rejected every save (caught by `db.js`'s own try/catch, which is why sign-in itself appeared to "work" with nothing showing up in the database). Confirmed fixed after publishing: a real `households/{code}` doc appeared with correct data.
- Deliberately not built this round (explicit scope, matching direct instruction): Apple sign-in, email-link sign-in, Cloud Storage, App Check.

### Spaces-only onboarding rework
- **`onboard.js`** rewritten — the review step no longer generates or shows assets/stock/routines at all (direct instruction: "doesn't make sense"). It's now a square-tile room grid (matching House's own `.tile-grid`, replacing the old "N routines · N stock · N assets" strip-row list), fully editable: add a room (name + type), rename any room (tap it), remove any room except Whole home/Utility (lock badge, no remove button — see below). `generateFromArchetype()` still runs internally (still the source of the *room list itself*, still needed for pack-derived `usedPackIds`/`packVersions`), its `items`/`assets`/`routines` output is just never used.
- **Reused for two contexts** (`mount(el, {context})`): `"household"` (a brand-new household, welcome.js's Create) and `"house"` (`houses.js`'s Add house, and More's Re-run onboarding — same wizard, different target/toast copy since neither creates a new household or changes its code).
- **Bedroom count matches home size** (`packs.js`'s `bedroomCountForSize()`): the pack-driven space list always produced exactly one deduped "bedroom" space regardless of size before this — 3BHK now gets 3 bedroom spaces, 1RK/Studio get zero (folded into the one general room). Added as a post-pass on `generateFromArchetype()`'s output rather than rebuilding the space-generation logic.
- **Kitchen added as a real space type** (`icons.js`, `house.js`'s `SPACE_TYPES`, `packs.js`'s `impliedSpaceTypes()`) — was missing entirely (no pack references it, so nothing ever created one). Also had to add an explicit "guarantee every baseline implied type exists" pass in `generateFromArchetype()`, since space creation had only ever happened as a side effect of a pack's own `pack.spaces` array or its assets/routines referencing a spaceType — which worked by coincidence for every type an existing pack happened to cover, but was never a real guarantee, and kitchen has no dedicated pack.
- **1RK added as a size option** — with the kitchen fix and 0-bedroom collapse above, 1RK now correctly defaults to exactly living + kitchen + whole home + utility + bath, matching the user's explicit list.
- **Houses' "Add house" now launches this same wizard** (`houses.js`) instead of silently creating an empty Utility-only house — `addHouse()` still eagerly stamps Whole home + Utility as a safety net (in case the wizard is abandoned), which the wizard's own `seedHousehold()` call cleanly replaces with the reviewed set on completion.

### Whole home + Utility: mandatory everywhere, badged not labeled
- `state.js`'s per-house mandatory-space guard (Round 17 had Utility only) now covers both types via a shared `MANDATORY_SPACE_TYPES` export — `deleteSpace()` refuses to drop a house's last space of either type.
- Both House's grid and onboarding's room review show a small lock badge (`icons.js`'s new `lock` glyph, a new `.tile-badge` corner overlay) on these tiles instead of a text label — "minimal or no text" per direct request. Onboarding's own tiles additionally lose their remove button entirely for these two types (can't even attempt to delete them there).

### Duplicate-name guards
- **Spaces**: `state.js`'s `addSpace`/`updateSpace` reject a case/whitespace-insensitive duplicate name *within the same house* (returns `null`; callers in `house.js` and `onboard.js`'s local room-review list show a toast and let the user retry instead of silently creating confusion or closing the sheet).
- **Houses**: same pattern, `addHouse`/`updateHouse` reject a duplicate household-wide; `houses.js` shows the retry toast.

### Fresh household starts genuinely blank
- **Bug found**: `seedHousehold()` only ever replaces one house's own spaces/items/assets/routines — it never touched the household-*wide* collections (`people`, `tasks`, `habits`, `habitLog`, `wishlist`, `snoozeLog`), so literally every fresh Google sign-in inherited the mock demo's Vinod/Keerthana/Lakshmi people and their tasks/habits forever, with no way to clear them (user report: "I see old tasks and habit still... needs to be empty," "remove the help, need to start blank").
- **`state.js`'s new `resetForNewHousehold({name, email})`** clears all of the above and seeds exactly one person — the signed-in Google account itself, as a Member with their real name/email (no manual "add yourself back" step needed). Household name defaults to `"{their first name}'s home"`. Called once, in `boot.js`'s `showFirstRunOnboarding()`, right before the six questions even render — deliberately *not* folded into `seedHousehold()` itself, since Re-run onboarding and Add-house both also call `seedHousehold()` and must never wipe a household's existing people/tasks/habits, only the target house's own rooms/stock/assets/routines.
- **Household name is now editable** (`people.js`) — a pencil icon next to the name in People & Household opens a rename sheet, via new `state.js` export `updateHouseholdName()`.

### Multi-house/multi-person visibility polish
- **House names disambiguate Today rows** (`today.js`) — `enrichRoutine`/`enrichTask` now resolve `houseName` from the row's own space; `rowHtml` shows it right-aligned (new `.occ-row-house`, stacked above the effort badge) whenever more than one house is currently active in the header picker. Covers both the literal ask (same-titled tasks/routines across houses now each show their own house) and the general case (any row, any time multiple houses are being viewed together) — a per-row-collision-only version would have meant a row's house label flickering in and out depending on what other titles happen to exist elsewhere, which reads as more confusing than always showing it in multi-house view.
- **Habits are private by default in multi-person households** (`today.js`, `insights.js`) — a shared Today screen no longer surfaces anyone's habit check-in unless a specific person is picked via the member filter first (previously defaulted to showing everyone's). Insights' own Habits section gained its own person-filter chip row for the same reason (it had no filter at all before) — both default to "nothing shown" once there's more than one possible person, and skip the restriction entirely for a single-person household (nothing to leak from one person to a household of exactly one).

### Known limitations from this round
- The Firestore save is debounced + last-write-wins on reload — no real-time multi-device sync (two devices signed into the same household simultaneously can overwrite each other's changes). A genuine `onSnapshot` listener + merge strategy is a real v2 feature, not attempted here.
- `people.js`'s "Join a different household" mid-session is still a stub (now for an accurate reason — see the code comment) — switching households while one is already loaded and syncing needs its own careful handling (stop the old autosave before hydrating the new one in) that wasn't built this round. Sign out + sign back in works today.
- Duplicate-name guards are exact-after-trim/case-fold — "Bedroom" and "the bedroom" are still treated as different names.

## Round 17 — multi-house support, schema-version migration mechanism, soothed habit colors (added 2026-08-05, same session)

User follow-up, given while going off to set up the Firebase project themselves: soften the habit grid's done/missed colors ("not nice... soothe it"); let a household contain more than one house, each with its own spaces/routines/assets, with a selector in the header (same row as the logo, left of More) to view one house or several at once, house naming, and a delete-house option under More; build this with Firebase in mind since it'll need to migrate there; and establish a general version-bump migration mechanism for future schema changes rather than one-off backfills.

- **Habit colors softened** (`app.css`): `.habit-cell.done`/`.missed` changed from solid `var(--done)`/`var(--danger)` fills to `color-mix(in srgb, ... 55-62%, var(--surface))` blends — same hue, same at-a-glance legibility (done vs. missed vs. gray unplanned), just dialed back so a 72-cell grid reads calm instead of alarm-toned.
- **`js/migrations.js`** (new) — a numbered-step schema-version mechanism: `CURRENT_SCHEMA_VERSION` + a `migrations` object keyed by target version, applied in order by `migrate(state)` against whatever `state.household.schemaVersion` a loaded household actually carries (missing entirely = version 1, the mock household's own starting point). `state.js` calls `migrate(state)` once, right after building the initial state object and before the first `regenerate()`. This is genuinely load-bearing, not scaffolding: migration step 2 (below) is what actually creates `state.houses` and stamps `houseId` onto every mock-seeded space — no hand-editing of `mock-data/spaces.js` was needed. Future schema changes add a new numbered step and bump the constant; nothing else needs to change. Firestore-shaped on purpose (2026-08-05, user request: "will need to be incorporated in firebase") — a Phase-4 Firestore read can run the exact same `migrate()` call against a loaded household document.
- **Multi-house support** — a household can now contain more than one house, each with its own spaces (and, transitively through `spaceId`, its own stock/assets/routines):
  - **Data model** (`state.js`): `state.houses` (`{id, name, createdAt}`), `space.houseId` foreign key, `state.household.activeHouseIds` (array — which house(s) are currently in view). `addHouse`/`updateHouse`/`deleteHouse` CRUD; `deleteHouse` cascades (drops the house's spaces and everything scoped to them) and refuses below one house, same mandatory-minimum pattern Utility already used. A brand-new house gets its own Utility space stamped in immediately (mirrors the guarantee every migrated/onboarded house already has) and becomes the sole active house — same "jump to what you just made" convention Add Space already followed. `visibleSpaceIds(state)` is the one exported helper every screen filters through, rather than each re-deriving "which spaces are in view."
  - **Utility's mandatory-space guard scoped from household-wide to per-house** (`state.js`'s `deleteSpace`, `house.js`'s delete button): each house needs its own Utility space now that there can be more than one house, not just one Utility for the whole household.
  - **"Uses this stock" scoped per-house** (`routine.js`): the Utility-space inclusion added in Round 15 now resolves Utility within the routine's own house (via its space's `houseId`), not the first Utility space found household-wide — a Bathroom routine in "Weekend home" can no longer silently offer "My Home"'s Utility items.
  - **Header house picker** (`index.html`, `boot.js`) — a pill button next to the logo, left of More (wrapped together in a new `.app-mark-actions` flex container so hiding one never strands the other's positioning), hidden entirely for the common single-house household. Opens a multi-select sheet (any subset of houses, not just one-at-a-time — matches the user's literal "one house or multiple houses" ask); label reads the house's name, "N houses", or "All houses". Subscribed to state changes so it stays in sync with adds/deletes/switches from anywhere.
  - **Houses management screen** (`js/routes/houses.js`, new) — reached via More → Houses: list, add (name only), rename, delete (with a cascade-warning confirm naming exactly how many spaces would go with it). Kept as a full mounted route (matching Assets/People's own pattern) rather than a one-off sheet, since it's genuine CRUD over a first-class entity.
  - **Filtering wired into every space/item/asset/routine-listing surface**: House's space grid (plus a house-name caption on tiles, shown only when more than one house is currently active — a single house stays exactly as uncluttered as before), Stock's buckets + shopping list, Assets' list, Today's routine/task rows + all 5 stat tiles, and Insights' health score + breakdown. Add/edit sheets' Space dropdowns (routine, task, item, asset) are scoped the same way, always including the entity's own current space even if it belongs to a currently-hidden house so editing something from another house never stands it up with no matching option.
  - **`seedHousehold()` now targets one house, not the whole household** (`state.js`): resolves to whichever house is the sole active selection, falling back to the household's first house otherwise, and only replaces that house's own spaces/items/assets/routines/ledger/occurrences — other houses in the same household are untouched. This is what both the very first onboarding run (targeting the one migrated default house) and a later "Re-run onboarding" from More correctly land on, with no special-casing needed between the two call sites.
  - **Incidental bug fix caught while touching `seedHousehold`**: it previously regenerated the household's invite code on *every* call, including re-running onboarding on an already-existing household — silently changing the code each time, even though nothing yet depends on it being stable (no real join flow exists pre-Firebase). Now only assigned once, the first time a household is ever seeded.
- **Deliberately NOT house-filtered this round** (scope boundary, not an oversight): Phase 5's intelligence sections in Insights (snooze suggestions, load balancing, batching, seasonal, forecast) and Wishlist still read/act on the whole household. These take `state` as a whole rather than a pre-filtered view, and threading house-awareness through each would have been a substantially larger pass than what was asked for this round — flagged below as a known limitation to revisit if multi-house households actually start using them.
- Verified live end-to-end: migration confirmed running (`state.houses`, `schemaVersion: 2`, every mock space stamped with the same houseId) on a hard-reloaded fresh load; added "Weekend home" via More → Houses (auto-created its own Utility space, auto-focused as the sole active house, header picker appeared); confirmed House/Stock/Today correctly show only the active house's content; confirmed the per-house Utility guard refuses deletion of a house's last Utility space; selected "All houses" in the picker and confirmed both houses' spaces render together with house-name captions; deleted "Weekend home" and confirmed the cascade (its Utility space gone, item/asset/routine counts unaffected) and correct fallback back to "My Home" as the sole active house, header picker correctly disappearing again. Zero console errors throughout. Habit color softening confirmed visually in Insights.

## Round 16 — icon/sizing/navigation polish, load-smoothing bug fix, People & Household deepened (added 2026-08-05, same session)

User follow-up: Today's stat tiles were visibly different heights ("Completed this week" wrapped to 2 lines); routine row icons didn't make sense (a water-tank-cleaning routine showing Utility's washing-machine-like room icon); a freshly onboarded household shouldn't open with things already due/overdue — give it a 4-day grace period; Batches cards used a generic house icon regardless of which room; load smoothing was visibly moving routines to the past and they were still showing on Today ("check if smoothing is done correctly"); the health-score breakdown rows should navigate to Today/Stock on tap; People needs a real "household" concept (code, join-anytime) and a real member-vs-help distinction (members need an email since they represent a future real account, help doesn't); the habit grid's missed color needed to be a solid box, not a border. Also: "lets initiate the firebase onboarding too tell me what I should do" — see the end of this entry.

- **Stat tile sizing** (`today.js`): "Completed this week" → "Completed" — the long label was the only one wrapping to 2 lines, and CSS grid's default row-stretch behavior meant the *other* tile sharing its row (Low stock) stretched to match, so two of the five tiles read as visibly taller than the rest. One label change fixes all five.
- **Routine icons stopped borrowing the room's icon** (`today.js`, `icons.js`): a routine row's icon was falling back to `space.icon` — reasonable-sounding until you notice Utility's room icon (a rounded box with a circle, meant to read as "utility room") looks like a washing machine, which made "Clean overhead water tank" show what looked like a washing machine glyph. Fixed with a real priority order: the linked **asset's own icon** when the routine has one (`routine.assetId` → genuinely meaningful, "Service geyser" now shows the geyser), else a new dedicated **`routine` glyph** (a recurring-cycle arrow, added to `icons.js`) — never a room icon, and never anything that could read as arbitrary for a "custom" routine either, since the fallback is always this one consistent icon.
- **4-day grace period for freshly onboarded households** (`state.js`): `seedHousehold()` now clamps every occurrence it just generated to a due date of at least 4 days out, if the engine would otherwise have it due sooner (floating routines with no completion history default to `dueAt: now` per `engine.js`'s `computeNext`, which is why a brand-new household could open with a wall of "due today"). One-time, applied right at the seeding call site — not baked into `regenerate()` itself, which every other caller (mode switches, routine edits) also uses and shouldn't be affected.
- **Batches use the real room icon** (`intel.js`, `today.js`): `spaceBatches()` now carries `spaceIcon` through to the card, replacing the hardcoded `Icon("house", ...)` — "You're already in the Bathroom" now actually shows the bath icon.
- **Load smoothing bug found and fixed** (`intel.js`) — a real one, caught live: "its moving the routines to past... i have the routines still on today." The original algorithm picked whichever *adjacent* week (before OR after the overloaded one) had more headroom, which could and did shift an occurrence's due date into a week that had **already passed** — instantly making it overdue instead of relieving anything, and it never excluded already-overdue occurrences from being "smoothed" in the first place (which never made sense — an overdue item needs doing, not deferring). Both fixed: `applyLoadSmoothing` now only considers occurrences that are still pending/due (checked via the engine's own `stateOf`, not the stored `occ.state`, which per `engine.js`'s own comment is rarely kept in sync with due/overdue), and a chosen candidate only ever shifts **+7 days forward**, never backward. Verified directly: re-ran onboarding and confirmed every generated occurrence's due date lands cleanly at the 4-day grace floor or later, with smoothed ones correctly pushed a further 7 days out — nothing in the past, nothing still showing on Today.
- **Score breakdown rows navigate** (`insights.js`): "Overdue routines" jumps to the Today tab, "Stock running low" jumps to Stock — reuses the same `document.querySelector('[data-tab="..."]').click()` pattern already used elsewhere rather than a new cross-module navigation function. "Asset service overdue" stays non-interactive (no primary tab it maps to).
- **People & Household** (`js/routes/people.js`, renamed from "People" everywhere it's referenced) — a real restructuring, not just a rename:
  - A new **Household** section at the top: the household's name and 6-char code, plus a **"Join different"** button opening the same honest join-stub sheet welcome.js uses (needs Firestore, Phase 4) — reachable anytime now, not just on first launch, per the user's explicit ask ("make it people and household where I can onboard to a household anytime").
  - The roster is now genuinely split into **Members** and **Help** sections instead of one flat list.
  - **Members now require an email** on save ("they should have an account and onboarded to the household... without email means...") — help never does, added by a member rather than having its own account. `addPerson`/`updatePerson` gained an `email` field; the two seeded mock members got plausible emails so their profiles don't open to a blank required field.
- **Welcome gate gained a Skip option** (`welcome.js`, `boot.js`) — a third path on the household step, alongside Join and Create: "Skip — start with a household of your own for now" lands straight in the app on whatever household this device already has (state.js already generates a fresh 6-char code at every boot, so Skip already means "a household of your own," not a shared fallback). Matches the user's description ("creates a household for yourself automatically") — the "which if you join another household gets removed" half isn't implementable yet without a real backend to actually replace anything into, flagged below.
- **Habit grid missed color: solid fill, not border** (`app.css`) — `.habit-cell.missed` changed from `background: var(--danger-soft)` + an inset border to a plain solid `background: var(--danger)`, matching how `.done` is also a solid fill so the two "something happened" states read the same way at a glance.
- **Verification note, not a code bug**: mid-session, the routine-icon fix appeared completely broken (blank icons) after what looked like a correct edit and a page reload. Root cause was the browser's own native HTTP cache, a layer distinct from the Service Worker's Cache Storage (already fixed in Round 14) — a plain `navigate` (or even unregistering the SW and clearing its caches) doesn't bypass it, only an actual hard-reload (`Ctrl+Shift+R`) does. Worth remembering for any future verification pass in this session: always hard-reload, not just navigate, after a JS edit.
- Verified live end-to-end: all 5 stat tiles now visibly the same height; every currently-overdue routine's icon confirmed showing the new recurring-cycle glyph (none tied to an asset in the current mock data, so all hit the fallback — a good stress test of the "never arbitrary" fallback path); Batches cards showing Bathroom/Living room's real icons; "Overdue routines" click confirmed landing on Today; People & Household's join-stub, Members/Help split, and the email-required validation (including its exact toast message) all confirmed; a fresh-browser welcome→Skip run confirmed landing directly in the working app with tab bar restored; habit grid's missed cells now a clearly solid red. Zero console errors throughout.

### Next: Phase 4 (Firebase) — what the user needs to do

The user asked to start this. Since I have no GCP console/billing access, these steps are the user's to run; I'll wire the actual `auth.js`/`db.js` integration once there's a real project and config to point at:

1. **Create the Firebase project** at [console.firebase.google.com](https://console.firebase.google.com) — a new, separate project (not folded into Miso's `pantry-os-prod`, per the locked decision above). Suggested name: `kasa-prod` or similar.
2. **Enable Authentication** → Sign-in method: turn on Google, Apple, and Email link (passwordless) — matching `welcome.js`'s placeholder buttons, which is exactly what they're standing in for.
3. **Enable Firestore** (Native mode, not Datastore mode), pick a region close to the household's actual usage (e.g. `asia-south1` for Bengaluru).
4. **Enable Storage** (for future photo-sweep/receipt-scan use, Phase 6's AI half).
5. **Enable App Check** (bot/abuse protection for the Firestore/Storage rules once they're live).
6. **Register a Web app** inside the project (Project settings → Add app → Web) — this generates the `firebaseConfig` object (apiKey, authDomain, projectId, etc.) that the actual code needs.
7. Once steps 1-6 are done, **share the `firebaseConfig` values** (not the full console access, just that config block — it's meant to be public/client-side safe) — from there the next session can wire `js/auth.js`/`js/db.js`, add the Firebase SDK to `index.html`'s importmap, write `firestore.rules`/`firestore.indexes.json` (mirroring `Pantry-OS-App/firestore.rules`'s `households/{code}` + `memberUids` pattern, extended for `wishlist`/`habits`/`habitLog`/`snoozeLog`), and swap `welcome.js`'s placeholder Google/Apple buttons and stub Join action for the real thing.

## Round 15 — Welcome/household gate, Utility as a mandatory shared stock room, Batches toggle, habit grid missed/unplanned colors (added 2026-08-05, same session)

User follow-up: "Uses this stock" should also offer Utility's items (cleaning liquids etc. that live there, not duplicated into every room) — which means Utility now needs to be a mandatory, undeletable space; the Round 14 batching section should be a toggle, not always visible; onboarding should become the actual first screen for a new browser — a placeholder Google/Apple sign-in with a working "Skip for now", then join-an-existing-household-by-code or create-a-new-one (which hands off to the existing 6-question flow), with a Miso-style 6-character household code; the habit grid's unplanned days should be gray and missed days should be red, themed to match the app.

- **"Uses this stock" now includes Utility** (`routine.js`): `requiresItemsFieldHtml()` was scoped to only the routine's own room; now it unions the routine's own room with whichever space has `type: "utility"`, since that's the one room a routine anywhere in the house can reasonably reach into for shared supplies. Items pulled in from Utility (when the routine's own room isn't Utility itself) get a "(Utility)" suffix on their chip label so a mixed list doesn't read as "these are all in this room." Deliberately scoped to just this one manual multi-select field — the separate, more precise `suggestedRoutines`-driven auto-linking in `assets.js`/`house.js`'s room-template flow stays same-room-only, unchanged.
- **Utility is now mandatory** (`state.js`, `house.js`): `deleteSpace()` refuses to delete a space with `type: "utility"` — guarded at the actual mutation boundary, not just hidden in the UI, though the UI also drops the delete button from a Utility space's detail screen so the guard is never the first thing a user hits. Without this, a household missing Utility would silently lose half of the feature above.
- **Batches toggle** (`today.js`): the "Batch these together" section from Round 14 is now gated behind a new "Batches" chip next to "10 free minutes?"/"Today"/"This week", off by default — nice as a suggestion, not something that should always claim space above Overdue/Due.
- **Welcome gate, the actual first screen for a new browser** (`js/routes/welcome.js`, new file; `boot.js`) — two steps:
  1. **Sign-in placeholder**: Google/Apple buttons are honest placeholders (a toast explains sign-in needs Firebase, Phase 4 — they don't pretend to work), matching this project's own established "never fake completeness" rule for unbuilt features. Only "Skip for now" actually advances.
  2. **Household**: join an existing household by its 6-character code (a real input + button, but a stub action — looking a code up needs Firestore, so it explains that and points at "Create a new one instead" rather than silently failing) or create a new one, which hands off to the existing 6-question onboarding flow unchanged.
  Gated behind a `kasa_intro_done` localStorage flag in `boot.js` so it only ever shows once per browser — not a real "is this browser already part of a household" check (there's no backend to ask yet), just "has this browser seen the intro." The tab bar and header "More" button are left empty/hidden during Welcome and first-run onboarding (`showWelcome()`/`showFirstRunOnboarding()` in `boot.js`) rather than showing 5 tabs with nothing behind them yet, restored once `startApp()` runs. Re-running onboarding later via More is a separate, already-existing path (`mountScreen("onboard")`) and is untouched by any of this.
- **6-character household code** (`state.js`): `generateHouseholdCode()`, a Miso-style `households/{code}` shape — 6 chars from a charset excluding visually-ambiguous characters (0/O, 1/I/L), assigned to `state.household.code` at boot (backfilling the demo household) and regenerated fresh inside `seedHousehold()` every time a household is (re)created. Session-only like everything else pre-Firebase — nothing looks it up anywhere yet, it just exists so the shape of the real thing is in place before the backend that makes it functional is. Surfaced twice: a toast right after onboarding completes ("Household created — code ABC123"), and persistently in the More sheet so it's findable again later, not just a fleeting message.
- **Habit grid: three colors, not two** (`habitGrid.js`, `app.css`) — refined Round 14's not-scheduled/scheduled split into three visually distinct states: **done** (existing green), **not scheduled** (now a solid gray fill — was a hollow outline in Round 14, changed per direct request "let the unplanned be gray"), and **missed** (new — a scheduled day strictly in the past that wasn't done, filled with the app's existing themed `--danger`/`--danger-soft` tokens, the same red already used for delete buttons elsewhere, per "make sure the red goes with the theme" rather than a new one-off hex). Today's own cell, if not yet done, deliberately stays neutral rather than immediately red — the day isn't over, so it isn't "missed" yet, just not-done-yet.
- Verified live end-to-end: the full welcome→skip→household→create→onboarding→app path with a real household code generated and shown; the Join stub's uppercase-forcing input and honest not-built-yet toast; a reload after completing the flow correctly skips straight to the app (flag persists via localStorage even though the household data itself doesn't, pre-Firebase); Utility's delete button confirmed absent from its own detail screen; a Bathroom routine's "Uses this stock" field confirmed showing both Bathroom items and "Detergent (Utility)"/"LPG cylinder (Utility)" together; the Batches toggle confirmed off by default and correctly showing/hiding the section on click; the habit grid's three-color scheme confirmed visually (green/gray/red-outlined cells all distinguishable) across all four seeded habits. Zero console errors throughout.

## Round 14 — Phase 5 (Intelligence, minus §5.2/§5.3), catalog research + expansion, habit alternate-days, remaining content packs, PWA polish (added 2026-08-05, same session)

User request, given full autonomy for one large run ("lets do one big automated run to execute these... dont stop inbetween"): build all of Phase 5 except burn-rate learning (§5.2) and adaptive intervals (§5.3), which the user explicitly said never to build; for §5.9's failure prediction, research realistic per-asset expected-life-years from the market and backfill existing assets automatically (with an edit option), asking new/custom assets for it; do a genuine breadth audit of the item/asset catalog across categories; add alternate-day/specific-day habit scheduling and fix the habit grid so unplanned days don't read as missed; finish Phase 7's remaining content packs and PWA polish, explicitly deferring the desktop three-pane layout to later. Two independent, well-scoped chunks (content packs, PWA polish) were delegated to background agents while the interconnected core (catalog, `intel.js`, habit frequency) was built directly; both were reviewed and integrated, not merged blind.

- **`js/intel.js`, new file** — all of memo §5.4-§5.9 as pure(ish) functions over `state`, deliberately excluding §5.2/§5.3 per direct instruction:
  - **§5.4 snooze learning**: a routine snoozed 3× in a row (on the same occurrence — see the snooze-wake bug below, without which this could never actually reach 3) offers to lengthen its interval (floating routines only) or lower its consequence tier by one step; snoozed 5×+ with zero ledger history ever offers to turn it off outright ("a bad suggestion should be able to die," the memo's own phrase); a new `state.snoozeLog` (seeded via `mock-data/snoozeLog.js`, appended to on every real snooze) cross-referenced against ledger completion dates detects a day-of-week pattern (snoozed mostly weekdays, done mostly weekends or vice versa) and surfaces it as information, not an auto-applied change — rescheduling a routine's actual trigger day is a bigger structural move than one signal should force through unattended.
  - **§5.5 load balancing**: rolling 30-day effort-point split (`effort × consequenceWeight`) across household **members only** — not paid help, since help doing more physical work is the point of having help, not an imbalance. Surfaced pull-only, phrased about the house ("leaning on X"), never pushed, matching the memo's own framing. `today.js`'s `markDone` now passes the routine's `defaultAssigneeId` as `completeOccurrence`'s `doneBy` — previously always `null`, so the ledger's attribution field had never actually been populated by anything the app itself did, only by hand-seeded mock data.
  - **§5.6 batching**: same-space clustering ("You're already in the Bathroom: 2 things, ~75 min total") and same-vendor clustering (asset's `vendorName`, when 2+ due/overdue vendor-owned routines share one) as a new "Batch these together" section atop Today, tapping through to the list below. Same-effort-tier batching already existed as the "10 free minutes?" chip and same-trip batching already existed as Stock's "Build shopping list" — neither rebuilt.
  - **§5.7 load smoothing**: if any calendar week's total open effort-points exceed a 20-point ceiling, flexible occurrences (`cosmetic`/`degrading` consequence, `floating_since_last` trigger only — never `damaging`/`safety`, never fixed-calendar/seasonal/etc.) shift ±7 days into whichever adjacent week has more headroom, biggest contributors moved first. Runs automatically as part of `regenerate()`'s existing engine pass (not a manual button), each occurrence only ever smoothed once (`occ.smoothed` guard). Insights shows "This week was smoothed" with exactly what moved and from/to which date.
  - **§5.8 seasonal intelligence**: a city→season map (Bengaluru only, using the memo's own real windows — pre-monsoon/monsoon/dry/pre-summer) keyword-matches due/overdue routine titles against each active season's boost list (drain/waterproof/mosquito/etc. for pre-monsoon) and surfaces matches as an Insights card. Other cities get no seasonal card rather than a guessed map.
  - **§5.9 correlation and forecasting**: failure prediction flags an asset past 80% of its `expectedLifeYears` (see catalog work below); a "rising service frequency" claim only appears when `serviceHistory` (new, see below) has ≥3 real entries to compute a trend from, otherwise the copy stays honestly age-only. Consumable coupling surfaces a card when a `water_treatment`-category asset was serviced within the last 14 days and the household has a plausibly-linked filter/cartridge item in the same space — informational, not an automatic quantity/date mutation (which would overstate precision this app doesn't have). Neglect clustering flags a space where ≥60% of its own open routines are overdue (and it has ≥2 routines at all), framed as "still apply, or worth pausing?" rather than a nag.
  - All wired into `insights.js` as new sections (health card → smoothing notice → snooze suggestions → load balance → forecast → seasonal → help-on-leave → habits), with real action buttons (Turn off / Lengthen / Lower priority) for the snooze suggestions that have one, wired through the existing `updateRoutine`/`toggleRoutineActive`.
- **Catalog `expectedLifeYears` research + backfill + UI** (task-scoped, not a full rewrite): the field already existed on every catalog asset entry and flowed through `packs.js`/`house.js`'s room-template flow from an earlier round, but the standalone Add/Edit Asset sheet (`assets.js`) never showed it, never pre-filled it from the catalog on selection, and never saved it — a real gap, now fixed with a new "Expected life (years)" field, pre-filled from the catalog on add, editable, persisted on save. A web search against several appliance-lifespan references corrected two values that were meaningfully off from real-world data (tankless/instant water heaters 12→20 years — tank and tankless are not the same lifespan class; Split AC 10→12) and nudged Geyser 8→9; the rest of the existing ~90 values were already well-grounded and left as authored. `state.js` gained `withExpectedLife()`, backfilling `expectedLifeYears` from the linked catalog entry for any asset that doesn't already have one set — never overwrites a value already present, so hand-authored mock/pack data with its own deliberate value (e.g. the demo Geyser's `8`, kept distinct from the catalog default's `9` on purpose) stays as authored.
- **Asset service history** (`state.js`): assets gained `serviceHistory` (array of ISO dates), appended to by `markAssetServiced()` alongside the existing `lastServicedAt`/`nextServiceDue` maintenance — the data `failurePredictions()`'s rising-frequency signal needs, with nothing to compute from until now. `replacementDueAt` also gained a `computeReplacementDueAt()` (same "single source of truth, recomputed when its inputs change" pattern as `computeNextServiceDue`), purely informational for now.
- **Catalog breadth pass**: added baby/kids (crib, stroller, high chair, child car seat), office/admin furniture (desk, filing cabinet), lighting/safety (ceiling light fixture, video doorbell), and fitness equipment (treadmill, exercise bike) — roughly a dozen new asset entries, each with a researched `expectedLifeYears` and suggested routines where a real recurring chore applies (per the same "don't invent a chore for a phone" principle already used elsewhere in the catalog).
- **Habit "Every N days" frequency** (`routine.js`, `state.js`): a new interval-anchored frequency alongside the existing daily/weekdays/weekends/custom-days/N-times-a-week — anchored to the habit's own `createdAt` (day 0 = creation day), so "every 3 days" is stable and doesn't depend on when you happen to open the app. `state.js`'s `isHabitDueToday` was refactored into a new date-parameterized `isHabitScheduledOn(habit, date)` (pure, no "already done" check) plus a thin `isHabitDueToday` wrapper — the split is what let the habit grid ask the scheduling question retroactively per past day (see next item), not just "is it due right now."
- **Habit grid: unplanned days no longer look missed** (`habitGrid.js`, `app.css`): each of the 72 cells now checks `isHabitScheduledOn` (and whether the day is even after the habit's `createdAt`) to render a third, visually distinct "not scheduled" state — a hollow/outlined cell (`box-shadow: inset ... var(--line)`) instead of the solid muted fill a genuinely-missed day gets. Verified live and via DOM query: a weekdays-only habit's grid correctly shows 18 outlined weekend cells out of 72, each titled "— not planned" vs a missed day's "— not done."
- **Content packs, Phase 7** — delegated to a background agent, then reviewed: 8 new `packs/*.json` (utility, bedroom, living, plants, admin, entry, vehicle, study), each cross-checked by the agent against `js/catalog.js`'s exact item/asset names before being handed back (the same class of silent-custom-entry bug documented earlier in this file for "Geyser"). `js/packs.js`'s `PACK_IDS` and `mock-data/household.js`'s `packVersions` updated to include all 8. While reviewing, also fixed a **pre-existing** instance of exactly that bug in the original `appliances.json` (predates this round): its asset names `"RO unit"`/`"AC unit"` didn't exactly match the catalog's `"RO / water purifier"`/`"Split AC"` etc., so that pack had been silently minting throwaway custom catalog entries every time it ran.
  - **Bug found and fixed**: `roomTemplates.js`'s `HAND_AUTHORED.study` stub (from an earlier round, before a real `study.json` pack existed) was never removed once the new pack shipped — `templateFor()` merges hand-authored and pack-derived routines without deduping them (only assets/items dedupe, by catalog key), so both the old stub's "Dust desk and electronics" and the new pack's identical routine showed up together. Caught by manually testing "Add space → Study" and seeing the duplicate; fixed by deleting the stale stub, matching the exact pattern already established for bath/bedroom/living/utility/entry/whole_home in an earlier round. Verified live: re-tested the same flow, routine list is now exactly the pack's 6, no duplicate. Audited every other newly-pack-covered space type (balcony/plants.json, outside/vehicle.json) for the same class of title collision — none found; both were deliberately written to complement rather than repeat their existing hand-authored entries.
- **PWA polish, Phase 7** (`sw.js`, `manifest.json`, `icons/icon.svg`) — also delegated, reviewed, and integrated: offline app-shell precaching, install/activate/fetch lifecycle, an on-brand SVG icon reusing the existing `.app-mark` silhouette, registration wired into `index.html`.
  - **Bug found and fixed, the same day it shipped**: the fetch handler's original strategy was cache-first for all same-origin JS/CSS/pack requests — correct for a typical production PWA, but actively wrong for this project's actual situation. Kasa is still pre-Firebase and under continuous same-session active development; every round so far has been edit-a-file-then-hard-refresh-to-verify, and a Service Worker's Cache Storage survives a hard refresh (`Ctrl+Shift+R` bypasses the HTTP cache, not the SW's own cache) — meaning the very first time the SW installed, it would have silently pinned every JS module to whatever it happened to be at that moment, and every edit after that point (including everything else built in this round) would keep silently running the pre-edit version until someone thought to unregister the SW by hand. Caught mid-verification when a batch of brand-new Today/Insights code visually behaved like the old code after a hard refresh; confirmed via `navigator.serviceWorker.getRegistrations()`/`caches.keys()` that a populated `kasa-shell-v1` cache was indeed intercepting the requests. Fixed by switching the fetch handler to network-first with cache-only-as-offline-fallback (matching the strategy the navigation handler already correctly used) and bumping the cache version so the stale one gets cleaned up on the next activate. Re-verified after the fix: batching, load smoothing, and every other new feature all rendered correctly on the very next hard refresh.
- Verified live end-to-end, extensively, given the scope: batching (Bathroom/Living room clusters with correct titles and time totals), load smoothing (a real routine visibly moved a week earlier with correct before/after dates shown), failure prediction (the demo Geyser, 7/8 years old, correctly flagged), snooze suggestions (both the 3×-snoozed "ease" and 5×-never-done "disable" paths, including clicking through their actual buttons and confirming the routine's `consequence`/`active` fields changed correctly in state — and one bug caught and fixed in the process: the "Lower priority" handler was comparing `lower === routine.consequence` *after* the mutation that made it always true, silently disabling the "already at the lowest tier" message), load balancing (a live-generated imbalance via real `completeOccurrence` calls correctly computed an 86% share), seasonal boosts (verified the matching logic directly since no current mock occurrence happens to carry a monsoon keyword — a synthetic due "Clear balcony drain before monsoon" occurrence correctly matched), consumable coupling and neglect clustering (both run cleanly, correctly empty against current mock data), habit grid not-scheduled cells, "Every N days" habit creation (and a real, separate, pre-existing bug caught in the process: `addHabit({personId, title})`'s destructured signature silently dropped every other field including `frequency`, so every habit ever created via the "Add habit" flow — not just this round's new frequency type — had always silently defaulted to daily regardless of what was actually picked in the UI; fixed by spreading `...fields` instead), the Assets sheet's new Expected life field, and a full House/Study-pack/Wishlist/Stock regression pass. Zero console errors throughout.

## Round 13 — Wishlist checklist bug fix, tile footer redesign, quick-add sizing, Low stock + This week on Today (added 2026-08-05, same session)

User report + follow-up asks: Wishlist project checklist items "unable to mark done or bought" on mobile, plus a request to be able to un-mark them in case of an accidental tap; remove the Soon/Someday priority label from wishlist tiles, replace with cost (bottom-left) + task count x/y (bottom-right) on one footer line; size Today's "+" quick-add to match the mode chip next to it; asked for a way to see items needing restock on Today "without making it cluttered," and a place to see the whole week's tasks — presented both as options via `AskUserQuestion` rather than guessing; picked the stat-tile approach for stock and a Today/This-week toggle for the week view (both the recommended options).

- **Root cause of the mobile bug, found and fixed** (`wishlist.js`): the checklist's `rewireSubItems()` — which attaches the Mark done/Buy/delete click listeners — was only ever called from inside the "Add" button's handler, never once on the sheet's initial open. Any *pre-existing* checklist item (added in an earlier session, or after closing and reopening the sheet) rendered with dead buttons until a new item was added first, which re-rendered and wired the whole list including the old ones. Not actually mobile-specific — just easy to miss on desktop while actively adding items during dev testing, since that path always happened to trigger the wiring. Fixed with a single `rewireSubItems();` call right after its definition.
- **Undo for checklist sub-items** (`wishlist.js`): the toggle button's `disabled` attribute and done-guard were removed; clicking a done item now flips it back (`sub.done = false`) instead of being inert, protecting against exactly the accidental-tap scenario the user described. `persistSubItems()` also now reverts a project's `status` from `acquired` back to `idea` (and clears `acquiredAt`) if an undo makes the checklist no-longer-fully-done, so un-marking doesn't leave the project stuck in a completed state it's no longer in. An asset/item sub-item's already-created real Stock/Assets record is deliberately left alone on undo — only the checklist's own flag reverts, not the actual purchase.
- **Wishlist tile footer redesign** (`wishlist.js`, `app.css`): the priority label ("Soon"/"Someday") is gone from individual tiles — the section header above each group already says that, so repeating it per-card was redundant. New `.tile-foot` — cost bottom-left, checklist progress (x/y) bottom-right, both centered in their own half of one line, `margin-top:auto` pushing it to the card's bottom edge even though `.tile` centers its other children as a group. Either side renders blank rather than collapsing, so cost and count land in the same spot across a grid of mixed tiles.
- **Quick-add sized to match the mode chip**: `.mode-chip` gained an explicit `height:34px` (was implicit via padding), and a new `.icon-chip-btn` class gives Today's "+" the same 34×34 box instead of the taller generic `.btn` tap-target — the two now read as one consistent row in the topbar, per direct feedback that they looked mismatched.
- **Low stock on Today, as a 5th stat tile** (`today.js`) — picked over a name-strip or a full list section (both offered via `AskUserQuestion`) specifically for the "without clutter" ask: one more number, same tap-to-jump pattern as Overdue/Due today, except this jump switches to the Stock tab (`document.querySelector('[data-tab="stock"]').click()` — reuses boot.js's existing tabbar wiring rather than exporting a new cross-module `switchTab`, avoiding a circular import between today.js and boot.js). Count uses `bucketOf()`, newly exported from `stock.js`, so it always matches what Stock itself would show (out + low + expiring, not a separately-maintained definition). `.stat-row`'s grid went from 2/4 columns (mobile/tablet) to 3/5 to fit the new tile.
- **Today/This week toggle** (`today.js`) — picked over an Insights section or a standalone screen (also offered via `AskUserQuestion`) to stay within the memo's 5-tab limit and reuse work already in place. Two new chips ("Today"/"This week") next to the existing effort filter; a new `dayOffset()` helper on each enriched row gives a signed day offset from today, independent of the engine's window-gated due/pending `state`. "Today" view is untouched (still `state === "due"`); "This week" view instead buckets by raw offset (0-6 days out), which also surfaces "pending" rows whose trigger window hasn't opened yet but whose literal due date already falls this week — a broader, date-driven view rather than the engine's due-soon-ness. Row meta text also got a `dueLabel()` upgrade ("Due tomorrow", "Due Thu") instead of always saying "Due today," which reads correctly in both views. The "This week" stat tile now switches into this view and scrolls to it, rather than being inert.
- Verified live end-to-end: reproduced the exact original bug (added a sub-item, closed and reopened the sheet, confirmed its button was dead before the fix and worked after); undo confirmed both for a plain task sub-item and for reverting a project's auto-acquired status; wishlist tiles show cost/count correctly with no priority label; quick-add and mode-chip render as matching 34px boxes; Low stock tile count (4) matched Stock's own Out(2)+Low(2); This week toggle correctly surfaced a "pending" routine ("Wipe entry doormat," due Sunday) that the Today view correctly excludes. Zero console errors throughout.

## Round 12 — Task category, quick-add, Insights trim, catalog typeahead everywhere, visual de-clutter pass (added 2026-08-04, same session)

User follow-up on Round 11: add a third category, Task, alongside Routine and Habit — one-off, due on a specific day, not recurring; add a "+" quick-add button on Today itself; remove "Attention needed" from Insights; resize the habit grid to 3 rows instead of 6, filling the width within reasonable bounds; give the Wishlist checklist's asset/item name field the same catalog typeahead used everywhere else; a direct dislike of italics used throughout the app, and of card font sizes ("seem very big") — asked for "premium and sleek"; asked for subtle, uncluttered Today filters (e.g. by household member); asked generally to look for other UI/UX opportunities; confusion about the "E4"/"E3"/"E1" effort badges — remove them; dislike of the colored left-border consequence-tier indicator ("that side color i dont even know what it means") — replace with a colored card background instead, subtle, no text label.

- **Task, a third first-class category** (`mock-data/tasks.js`, `state.js`): a task is `{id, title, dueDate, spaceId, assigneeId, done, doneAt, createdAt}` — no recurrence, no trigger, no effort/consequence tier, optionally scoped to a space/person. Kept as its own data model rather than folded into routines (which need a 6-trigger engine) or habits (which are person-scoped with a frequency) — the three categories' underlying shapes genuinely differ enough that one unified "occurrence" abstraction would have compromised all three. Full CRUD (`addTask`/`updateTask`/`deleteTask`/`completeTask`/`uncompleteTask`/`taskState`/`taskOverdueDays`) added to `state.js`, `taskState` comparing at day granularity to match the existing routine convention.
- **Shared 3-way editor** (`routine.js`, rewritten): the existing Routine/Habit Kind toggle gained a third option, Task. Selecting Task swaps in `taskFieldsHtml()` — title, due date, optional space, optional assignee — and saves through `addTask`/`updateTask` instead of the routine/habit paths. `openRoutineEditor` now accepts a `task` prop and `defaultKind: "task"` alongside the existing `routine`/`habit`/`defaultPersonId` props.
- **Today quick-add**: a "+" button in `today.js`'s topbar opens the same shared editor pre-set to `defaultKind: "task"` (the most common quick-capture case — "remember to do X by Friday" — without needing a space or trigger type first). Verified live: added "Pay electricity bill" as a task from Today, saw a confirmation toast, and watched the Due Today count increment immediately.
- **Unified Today row model**: `today.js` gained `enrichTask(task, state)` alongside the existing `enrichRoutine(occ, state)`, both normalizing to the same `{type, title, tier, effort, assigneeId, state, days, space}` shape so `visibleRows()`/`rowHtml()`/`statRowHtml()` can sort, filter, and count routines and tasks together in one Overdue/Due Today view rather than running two parallel sections. Tasks render with the new `task` icon (clipboard+check) and no effort/tier badge, since neither concept applies to a one-off task.
- **Swipe generalized further**: `attachSwipe(row, {onSwipeRight, onSwipeLeft})` (already generalized in Round 11 for habit rows) now also backs task rows — both swipe directions map to `markTaskDone`/`uncompleteTask` since a task has no snooze concept, same pattern already used for habits.
- **Insights: "Attention needed" removed** — `attentionItems()`/`attentionSectionHtml()` and their call site deleted outright (not hidden) per explicit dislike; the itemized score breakdown from Round 11 stays as the one "what needs fixing" answer, rather than having two overlapping digests on the same screen.
- **Habit grid resized**: `habitGrid.js`'s CSS grid changed from `repeat(12, 1fr)` / `max-width:260px` to `repeat(24, 1fr)` / `max-width:640px` — 3 rows of 24 instead of 6 rows of 12, same 72 cells, filling available width up to a sane cap rather than sitting narrow in a corner.
- **Wishlist checklist catalog typeahead**: the sub-item "add" row's name field is now `catalogField`/`wireCatalogField` (same component used in Stock/Assets add sheets) whenever the sub-item kind is asset or item, falling back to a plain text input for task-kind sub-items. New `subitemNameFieldHtml(kind, value)` re-renders and re-wires whenever the kind chip changes; the Add-button handler resolves the typed/selected name via `resolveCatalogField`, storing `catalogKey`/`icon` on the new sub-item so it gets a real catalog icon instead of a generic kind-based fallback. Verified live: typing "sof" surfaced "Water softener" with its real icon; typing a genuinely-unmatched term ("lamp") correctly returned zero suggestions rather than a false match.
- **Italics removed globally**: `.named` (previously Fraunces italic serif, used for routine/asset/habit names throughout) changed to plain semibold weight — a single CSS rule change rather than touching every call site, since dozens of files apply this class. Fraunces stays imported for now but has no remaining live usage; left in rather than ripped out in case a future "special occasion" surface (mirroring Miso's Gourmet Weekend) wants it back.
- **Card sizing trimmed for a "premium/sleek" feel**: `.occ-row-title`/`.occ-row-meta` no longer step up in font-size at the ≥600px tablet breakpoint — they now hold the same (smaller) size used on mobile at every width, rather than growing on wider screens. Combined with Round 5's mobile-sizing pass, this is the first change that also pulls tablet down to match, on the theory that "premium" reads as restrained/dense rather than large.
- **Effort badges removed**: the cryptic "E1"–"E5" chips on Today rows are gone, replaced by the existing `EFFORT_LABEL` human-readable map (`{1:"2 min", 2:"15 min", 3:"1 hr", 4:"Half day", 5:"Vendor"}`) rendered as plain meta text — this label map already existed from an earlier round but the raw E-code badge had been left in alongside it; removed the redundant badge rather than the readable text.
- **Consequence tier: border → card tint**: `.occ-row[data-tier="degrading|damaging|safety"]` changed from a 3px `border-left` stripe to a whole-card `background: color-mix(in srgb, var(--tier-X) 7-9%, var(--surface))` wash. Cosmetic tier (the majority case) stays untinted — deliberately, so a wash on every row wouldn't read as "everything is urgent, nothing is." No text label anywhere (explicitly asked against, e.g. no "cosmetic" caption) — purely a visual weight cue, loud overdue-safety text still carries the actual urgency signal.
- **Subtle member filter on Today**: a new `.member-filter-row` of `.member-chip`s (Everyone + each household member) above the Overdue/Due sections, filtering routines/tasks by `assigneeId` and habits by `personId`. Styled deliberately quiet — no border unless `aria-pressed="true"`, small text — and the whole row is hidden outright when the household has fewer than 2 relevant people, so a single-person household never sees a filter with nothing to filter. Verified live: clicking "Vinod" correctly narrowed the list to just his `defaultAssigneeId`-matching rows (cross-checked directly against state, not just visually).
- Verified in-browser end-to-end after all changes: Today (stat row, quick-add, member filter, tier tints, no italics, no E-badges), Insights (3×24 habit grids, Attention needed gone, score breakdown intact), Wishlist (checklist typeahead), and a House/Stock regression pass (both screens render cleanly with the same CSS changes — tiles, list-rows, no layout breakage). Zero console errors throughout (only an unrelated wallet browser-extension warning, not app code).

## Round 11 — Wishlist checklists, Habit/Routine merge, Insights rework (added 2026-08-04, same session)

User follow-up on Round 10: More should be top-right (was top-left); Wishlist projects should support a checklist of tasks/assets/items, complete when everything in it is done, and buying an asset/item sub-item should collect real details through the actual Assets/Stock modal, not a bare create; habit creation should happen through the same Add Routine/Habit sheet via a Kind toggle, habits need a person and non-daily frequency options, and should show up on Today swipeable (tracking stays in Insights); Insights' score wasn't actionable and the 7-day trend "adds no value" — "think and adjust," possibly toward a fuller habit page.

- **More → top-right**: `.app-more-btn` changed from `margin-left:6px` to `margin-left:auto` — same row as the logo, pushed to the far right instead of sitting right next to the wordmark.
- **Wishlist checklists** (`js/routes/wishlist.js`): project entries gained `subItems: [{id, title, kind: "task"|"asset"|"item", done, createdId}]`. A task sub-item toggles done directly; an asset/item sub-item's "Buy" button opens the *real* `openAssetSheet`/`openItemSheet` (assets.js/stock.js) pre-filled with the sub-item's name and the project's space — both gained a new `onSaved` callback (and `defaultName`) for this. The project auto-completes (`status: "acquired"`) once every sub-item is done; the manual "Mark done" button is hidden once a checklist exists (completion is checklist-driven then, not both). This same real-modal upgrade also applies to plain (non-project) asset/item wishlist entries' "Mark acquired" — `state.js`'s old bare-bones `markWishlistAcquired()` was removed as dead code once nothing called it anymore. `addItem()` in state.js now returns the created item (previously void, like `addAsset` already did) so the `onSaved` callback has something to pass along. Verified live end-to-end: added a "Floor lamp" asset sub-item to "Repaint living room walls," clicked Buy, filled in a brand in the real Add Asset sheet, saved — confirmed via direct state read that the asset was created with the brand and correct space, the sub-item flipped to done, and the project auto-acquired.
- **Habit/Routine merge** (`js/routes/routine.js`): gained a "Kind" chip (Household routine / Personal habit) at the top of the same sheet — picking Habit swaps out Space/Repeats/trigger-params/Uses-this-stock/etc. for Person + "How often" (Daily/Weekdays/Weekends/Custom days/N-times-a-week), saving through `addHabit`/`updateHabit` instead of `addRoutine`. Also gained an `onSaved` callback so `people.js` can reopen the person's edit sheet after adding/editing/deleting a habit from there instead of dropping back to the bare People list. `people.js`'s old free-text "+ Add habit" input was replaced with a button that opens this shared sheet (pre-set to the current person); tapping an existing habit's title also opens it for editing (frequency, etc. — more room than fits inline).
- **Habits on Today, swipeable**: `state.js` gained `isHabitDueToday(habit)` (per-frequency: daily/weekdays/weekends/custom days/weekly-count via a new `countDoneThisWeek()`) and exports it. `today.js`'s `attachSwipe()` was generalized from hardcoded `markDone`/`markSnoozed` calls to `{onSwipeRight, onSwipeLeft}` callbacks so the same gesture-detection code (direction disambiguation + rAF batching from Round 6) serves both routine rows and the new Habits section — habit rows treat either swipe direction as "done" (no snooze equivalent for a habit). `toggleHabitToday` being a pure toggle means undo is just calling it again, no snapshot bookkeeping needed the way occurrence undo works.
- **Insights reworked on direct feedback**: replaced the single vague "biggest contributor" sentence with an itemized, counted breakdown ("Overdue routines -14 · 6 items — 1 safety, 1 degrading, 4 cosmetic", "Stock running low -8 · 2 out, 2 low") — the actual fix for "why 78, what needs to be fixed." Removed the 7-day completion trend entirely (`.week-trend*` CSS deleted too, not just the markup) rather than replacing it with another chart that might not earn its space either. Kept Habits as the dedicated tracking section (72-day grids + streaks) rather than spinning up a 6th tab, which would break the memo's own 5-tab limit — check-in itself now happens on Today, so this section is purely "the tracking view," and says so.
- **Bug caught and fixed while testing**: `mock-data/habitLog.js`'s `seedLog()` filtered by raw array index from `last72Dates()` (oldest-first, so index 71 — not 0 — is today), but the keep-predicates were written as if index 0 meant today. Didn't crash anything, but two habits ended up "done today" and two "due today" opposite of what the comments claimed. Rewrote `seedLog` to take an explicit `daysAgo` (0 = today) instead of a raw array index, so the off-by-71 mistake isn't there to make again.

## Round 10 — Wishlist, Habits, real Insights phase (added 2026-08-04, same session)

User request, given right before going offline for the night with full autonomy to build and push without further check-ins: move More off the bottom tab bar to make room for a new Wishlist tab; build a household wishlist for home-improvement ideas; add personal Habit tracking (distinct from household Routines) with a 72-day square grid, "similar to the habit app"; build Phase 6 Insights for real — "think deep... don't just go by the memo." No AI features (photo sweep/bill scan/weekly AI digest) — those need a Cloud Function proxy that doesn't exist without Firebase (Phase 4, itself blocked on the user's own GCP console access), so they're explicitly called out as not-yet rather than silently skipped.

- **More relocated, Wishlist takes its tab slot**: `index.html`'s `.app-mark` header gained an `#app-more-btn` button (three-dot icon + "More" label) next to the logo/wordmark. `boot.js`'s `TABS` array now reads Today/House/Stock/Insights/Wishlist; `renderMoreMenu` became `openMoreSheet()`, opened from the header button via `openSheet()` instead of being a routed screen. Assets/People/onboarding's `onBack` callbacks changed from `switchTab("more")` (which no longer exists) to `switchTab(activeTab)` — returns to whichever tab was active before More was opened, verified live (opened More from Wishlist → People → back → landed on Wishlist, tab still highlighted correctly).
- **Wishlist** (`js/routes/wishlist.js`, `state.wishlist`): three entry types — `asset`/`item` (catalog-linked exactly like Assets/Stock's own add flows, via the same `getOrCreate`/typeahead pattern) and `project` (free-text, no catalog entry — for things like repainting a room that aren't a single purchasable thing). Priority (High/Soon/Someday) buckets the screen the same way Stock buckets by Out/Low/OK. `markWishlistAcquired()` is the key mechanic: for asset/item types it calls the real `addAsset`/`addItem` (so an acquired idea shows up on House/Stock exactly like anything added there natively, not a parallel/fake record) and marks the wishlist entry `status: "acquired"`; project types just mark done. Verified live end-to-end: marked the seeded "Air purifier" idea acquired, confirmed via direct state read that a real `assets` record was created with the correct `catalogKey`/`spaceId`, and the wishlist tile moved to a dimmed "Acquired" section.
- **Habits, personal and separate from Routines** (`state.habits` + `state.habitLog`, `js/routes/people.js`): a habit belongs to one person (`personId`), no space, no engine trigger — just a sparse log of "done" days (same silence-means-not-done pattern as the ledger). `toggleHabitToday()` only ever touches today (no backfilling past days — same "no gaming the system" spirit the memo applies to routines). `habitStreak()` counts consecutive done-days ending today-or-yesterday. Managed from inside a person's existing edit sheet (People has no per-person detail screen to hang a separate Habits screen off of) — verified live: toggling "Read 20 minutes" for Vinod flipped it to "Done today" and the streak count updated immediately, in the same sheet, no navigation.
- **72-day habit grid** (`js/ui/habitGrid.js`, new `.habit-grid`/`.habit-cell` styles): reading-order grid (oldest day top-left, today bottom-right, today outlined in gold) rather than GitHub's week-column layout — the weekday-alignment padding that layout needs wasn't worth the complexity for "72 small squares you can scan at a glance," which is what was actually asked for. `mock-data/habitLog.js` generates its seed data relative to whatever "today" actually is (not hardcoded dates), so the grid always shows a full 72-day history regardless of when the app is opened, using deterministic patterns rather than `Math.random()` (a mock dataset that reshuffles on every reload would read as a bug).
- **Insights, built for real** (`js/routes/insights.js`) — deliberately not the memo's literal §6 (which bundles AI features that can't be built without a backend yet). What's genuinely useful from data already in state, no LLM, matching §5.10's own hard boundary applied to this screen too:
  - **House health score**: one composite 0-100 number (overdue severity by consequence tier + stock out/low ratio + asset service compliance, each capped and summed as a penalty off 100), a qualitative label (Thriving/Doing well/Needs attention/Falling behind), and a one-line explanation naming whichever factor is dragging it down most — a genuine synthesis Today/House/Stock don't give you (they're lists, not a single "how are we doing" signal).
  - **This week**: a 7-day bar trend built from `state.ledger`'s real completion timestamps — shows the *shape* of the week (which days things actually get done), distinct from Today's single "completed this week" count.
  - **Attention needed**: top 5 items ranked together *across* overdue safety/damaging routines, out-of-stock items, and overdue asset services — the "what actually matters most right now" digest that would otherwise require checking three separate screens.
  - **Help on leave impact** — memo §6.3's own named "hero feature", built as a real cross-reference: for each `help`-kind person with a current-or-upcoming (within 7 days) leave entry, lists which `ownerClass: "help"` routines fall due during that window. A genuine insight (connecting two records that otherwise live in unrelated parts of state), not a re-list.
  - **Habits**: every habit's 72-day grid + current streak, one screen, no need to open each person individually.
  - Verified live end-to-end: health score rendered as "78 — Doing well" with a correct overdue-driven explanation, week trend showed the single ledger completion from earlier testing, attention-needed correctly surfaced the one overdue-safety routine plus two out-of-stock items, all three seeded habits' grids rendered with correct streaks (2/1/3 days) and the gold "today" outline in the right cell.
- **Bug caught and fixed while building this**: `stepper()`'s displayed value (`js/ui/components.js`) was rendering `item.qty` raw — for auto-depleting items (Round 9) this is a long float like `0.9384291452777778`, which overflowed `.tile-stock`'s fixed width and pushed the −/+ buttons outside the rounded card. Fixed by rounding to 2dp for *display* only (the stored value keeps full precision, so depletion math stays accurate); added `overflow:hidden` to `.tile` as a second line of defense against any future too-wide child.

## Round 9 — routine stock-picker bug, real day/week/month auto-deplete, negative-qty clamp (added 2026-08-03, same session)

User report: checking/unchecking items under "Uses this stock" in the routine builder felt "not normal"; asked for an explicit auto-deplete toggle on day/week/month rates that requires both a period and a number when on; asked that stock quantity can never go negative.

- **Root cause found and fixed** (`routine.js`): the Space chip-group's change handler reset `#requires-items-field`'s selection to empty on *every* click inside that chip-group — including re-clicking the space that was already selected. A user checking a few stock items, then brushing/re-tapping the same Space chip, would see their checks silently vanish with no visible cause. Added a `currentSpaceId` guard so it only resets on an actual space change. Verified via direct DOM test: re-clicking the selected space now leaves a checked item's `aria-pressed` untouched (and doesn't even re-render the container).
- **Real day/week/month auto-depletion** (`stock.js` + `state.js`): a new "Auto-deplete on this schedule" toggle appears next to the consumption-rate field whenever the period isn't "/usage" (usage already depletes via routine completion, so the toggle hides and force-clears itself if you switch to it). Turning it on and saving without a period+number filled in is blocked with a toast — exactly the "mandatory" requirement asked for. When properly set, `state.js` gained `applyAutoDepletion()`, which runs lazily on every `getState()` call: it compares real wall-clock elapsed time against each auto-depleting item's own `lastDepletedAt` checkpoint and actually subtracts `burnRate × elapsedDays` from `qty` (clamped at 0), not just the existing display-only projection. Guarded against reentrancy (`notify()` inside it can loop back into `getState()`). Verified live: enabling it on Toothpaste (rate 0.01/day) showed qty visibly ticking down tile-to-tile within seconds of leaving the tab open, and a simulated 5-day-old checkpoint produced the expected ~0.06 unit drop.
- **Negative quantity clamp**: the stepper and the new auto-deplete path already clamped at 0, but typing a negative number directly into the Quantity field in the add/edit sheet didn't — `Number("-7") || 0` doesn't catch a real negative number since it's non-zero/truthy. Fixed with `Math.max(0, ...)` on both Quantity and Reorder-at at save time; also added `min="0"` to those inputs for the native browser hint (new optional `min` param on `textInput()`).

## Round 8 — /usage consumption mode, real automated depletion, typeahead fix (added 2026-08-03, same session)

User follow-up: the 3 rate chips still didn't fit one line on mobile and asked for a 4th "/usage" option; asked directly whether picking "/day" actually auto-decrements stock (it doesn't — clarified below); asked for an "automated reduction" near a routine's "Uses this stock" tied to routine frequency; reported the catalog typeahead "had issue in phone."

- **Clarified for the user**: the consumption-rate day/week/month fields have never auto-decremented anything — they only feed the "~N days left" *projection* shown on the tile (`projectedDaysLeft`, static, not the memo §5.2 EWMA learning — that's still Phase 5). The only thing that has ever actually removed stock automatically is completing a routine that has the item checked under "Uses this stock" (`requiresItemIds`, built in an earlier round) — that already **is** "automated, tied to routine frequency," since it only fires when that routine's own recurrence brings it due and gets completed.
- **New `/usage` period** (`stock.js`): a 4th chip alongside day/week/month, semantically different — not a time rate at all, it sets `item.perUseQty` (a new field) instead of `item.burnRate`. Switching into/out of "/usage" never runs the day/week/month conversion math (doesn't apply). `state.js`'s `completeOccurrence` now decrements a linked item by `item.perUseQty` if set, falling back to the old flat `1` otherwise — this is the real "automated reduction" the user asked for: set a Toothpaste's rate to "/usage 0.5", link it to a routine via "Uses this stock", and every completion of that routine now removes exactly 0.5, not a flat unit. Verified live via direct state calls: qty 5 → 4.5 on one completion. Tiles and the edit-sheet subtitle show "~N uses left" (`projectedUsesLeft`) in this mode instead of "~N days left."
- **Fit 4 chips + input on one line**: input narrowed 84px→70px, new `.rate-period-group` CSS variant (`nowrap`, tighter 4px gaps, each chip `flex:1`, smaller padding/font) — chip `min-height` was deliberately left at the 44px accessibility floor; only width-affecting properties were touched. Verified in-browser: all 4 chips render on one row.
- **Catalog typeahead mobile fix** (`components.js`): suggestion selection switched from `mousedown` to `pointerdown` (more consistent ordering relative to the input's blur across mobile browsers — a well-known category of "sometimes doesn't register on touch" bug for this exact dropdown-selection pattern), and the blur-hide delay widened 100ms→180ms for extra margin on slower devices. Best-effort without a real device to reproduce on — flagged to the user to report exact symptoms (tap not registering vs. wrong item vs. dropdown hidden behind keyboard) if it recurs, so a more targeted fix can follow.

## Round 7 — new brand mark, stock rules, binary tracking, sheet polish (added 2026-08-03, same session)

User feedback on Round 6: the roofline mark was "too cliched," wanted a wordmark next to it; same catalog item added twice to the same room should be blocked for **stock** (not assets — that's still intentionally unrestricted per the earlier "no warning, add freely" call); a routine's stock picker should only offer that routine's own room's items; every add/edit sheet should be visually denser (font/whitespace), get a red delete button, and get an explicit Cancel button; the consumption-rate row still wrapped on mobile; asked for a binary (yes/no, no quantity) stock-tracking mode.

- **Brand mark v2**: replaced the roofline+dot (`index.html`'s `.app-mark`) with a single rounded square + a small dot near one corner — the same silhouette as every House/Stock `.tile`, not a house pictogram. Added a `<span class="app-wordmark">kasa</span>` next to it (Geist bold, `--ink`, not gold — icon carries the brand color, wordmark stays plain per typical logo-lockup convention).
- **Duplicate stock blocked, per room**: `stock.js`'s `openItemSheet` save handler now checks `state.items.find(i => i.spaceId === spaceId && i.catalogKey === entry.key)` before creating a new item (add-mode only) and shows a toast + refuses to save rather than silently creating a second row. Assets remain unrestricted, unchanged — the user drew that distinction explicitly ("asset makes sense but not stocked item").
- **Routine stock picker scoped to room**: `routine.js`'s "Uses this stock" chip-group is now built from `state.items.filter(i => i.spaceId === <routine's current space>)` via a new `requiresItemsFieldHtml()`, re-rendered live (selection reset) whenever the Space chip changes — verified switching Bathroom→Living room in the Add Routine sheet correctly swapped the offered items. Applied the same room-scoping fix to the two other places that resolve `requiresItemKeys` from a catalog asset's `suggestedRoutines` (`assets.js`'s standalone Add Asset flow, `house.js`'s room-template cascade from Round 4) — both previously matched by `catalogKey` across the *entire* household, so an AC's "clean filter" routine could've silently linked to a filter item sitting in a different room. Also reordered `house.js`'s room-template save handler to add items *before* assets/routines — the old order could resolve `requiresItemIds` to nothing for an item the same save was about to create moments later.
- **Sheet/modal polish**: `.sheet` padding 20px→16px, `.sheet-title` 17px→16px, `.field` margin 16px→10px, `.field-label` 13px→12px, `.chip-group`/`.sheet-actions` gaps tightened — `.text-input` font-size deliberately left at 16px (`--fs-body`) despite the "reduce font" ask: iOS Safari auto-zooms the viewport on focusing any input under 16px, which would undo the rest of this polish the moment someone tapped a field. New `--danger`/`--danger-soft` tokens (`tokens.css`, both themes) + `.btn-danger` power `sheetActions()`'s Delete button; every sheet also now gets a Cancel button, wired once generically inside `openSheet()` rather than by each of the ~8 callers.
- **Consumption-rate one-line fix**: the rate number input and the /day /week /month chip-group were both `flex:1` (equal 50/50 split), which squeezed the 3-chip group into too narrow a column to fit on one line. Changed to a fixed `width:84px` input + `flex:1` chip-group.
- **Binary stock tracking**: items gained a `binary` boolean. `openItemSheet` gained a "Track as: Quantity / Yes-No" chip-group that shows/hides the Unit/Quantity/Reorder/Consumption-rate block vs. a single "In stock?" Yes/No chip. Binary items store `qty` as 1 or 0 and `status` as directly `out`/`ok` (never `low`, par level is irrelevant). `stock.js`'s tiles show a toggle chip ("In stock" / "Mark in stock") instead of a +/− stepper for binary items, wired via a new `[data-item-toggle]` handler that flips `qty` between 0 and 1.

## Round 6 — wide-viewport whitespace, House detail tiles, scroll jank, haptics (added 2026-08-03, same session)

User feedback on Round 5: brand mark should be top-left not top-right (and the row around it had too much padding); large left/right margins on wider viewports — "is that needed?"; House's per-space Stock/Assets lists should become tiles too, "similar to the one we did before"; Today's swipe-list scrolling "not very smooth"; asked for minimal/light haptics.

- **Logo repositioned**: `.app-mark` flipped to `justify-content:flex-start` and its own padding + the `.topbar`'s top padding both cut down, closing the gap to the title below.
- **`--content-max-w`**: 1024px → 1600px (`tokens.css`). On this session's actual test window (1920px wide), `.app-shell` was capped at 1024px, leaving ~448px of dead cream margin per side — confirmed via `getBoundingClientRect()`, not guessed. Real desktop three-pane is still Phase 7 and not built, so a hard narrow cap this early was reserving space for a layout that doesn't exist yet; widened rather than removed entirely so cards/rows don't stretch absurdly wide on ultra-wide monitors.
- **House detail tiles**: `detailHtml()`'s Stock and Assets sub-lists (inside a single space, e.g. Bathroom → its items/assets) now render as `.tile`/`.tile-grid` like the top-level House and Stock grids. Routines stayed as rows (toggle + trigger-type + effort/consequence doesn't fit a square, wasn't asked to change). Tiles have no inline delete button — no room at this size — deleting still works from inside the edit sheet each tile opens (`sheetActions({showDelete:true})`, already existed); the now-unused `data-delete-item`/`data-delete-asset` wiring and `deleteItem`/`deleteAsset` imports were removed from `house.js` rather than left dead.
- **Scroll jank fix**: `today.js`'s `attachSwipe` used to call `setPointerCapture` and write `row.style.transform` on every `pointermove` from the moment a finger touched a row — including pure vertical scrolls, which meant every scroll starting on a row ran swipe-handler JS competing with the browser's native scroll compositor for the same touch. Rewritten to disambiguate direction first (an 8px dead zone, then whichever axis moved further wins) — a vertical-leaning gesture now bails out entirely (never calls `setPointerCapture`, never touches `style`) and only a confirmed horizontal drag commits to the swipe. Transform writes are also now batched via `requestAnimationFrame` instead of one per raw pointer event. Added `will-change:transform` to `.occ-row` and `-webkit-overflow-scrolling:touch` / `overscroll-behavior-y:contain` to `.screen` as supporting fixes.
- **Haptics**: `js/ui/components.js` gained `haptic(ms=8)` — a thin `navigator.vibrate?.()` wrapper (Android Chrome only; iOS Safari has no Vibration API, so it's silently a no-op there, not an error). Used sparingly per the "minimal and light" ask: swipe-to-done (10ms), swipe-to-snooze (6ms), and Stock's qty stepper taps (4ms) — not wired into every chip/button.
- **Bug caught while widening the viewport**: with the wider content area exposed, confirmed `.app-shell`/`.screen`'s `min-height:0` fix from Round 5 is still holding (no page-level double-scroll re-appeared).

## Round 5 — mobile sizing pass, square tiles, brand mark (added 2026-08-03, same session)

User feedback: mobile cards "look very big"; House's space grid and Stock's item lists should become squares with rounded corners instead of full-width rows, and House should drop the "N routines · N stock · N assets" caption entirely; asked for a minimal derived (not literal) vector logo top-right.

- **Mobile sizing**: `.occ-row`/`.list-row` padding, icon size (36→30px), and title/meta font sizes were reduced at the base (<600px) breakpoint, with `@media (min-width:600px)` overrides restoring the original roomier values so tablet is unchanged. Same pattern applied to `.topbar`, `.today-section`, `.stat-tile`, `.chip`, `.btn`, `.screen`. `.chip`/`.btn` keep `min-height: var(--tap-min)` (44px) unshrunk even on mobile — only padding/font trimmed, not the touch target itself.
- **`.tile`/`.tile-grid`** (new in `app.css`): a reusable rounded-square card grid. `house.js`'s `gridHtml` now renders spaces as true 1:1 tiles (icon + name only — `spaceCountsHtml` deleted, the R/S/A caption is gone per the request). `stock.js`'s Out/Low/Expiring/OK sections use a `.tile-stock` modifier (relaxed aspect ratio, not a strict square) so the qty stepper still fits — kept the stepper rather than pushing quantity edits behind a tap-to-open sheet, since that's a functional regression the user didn't ask for ("just for the look and feel").
- **Brand mark**: `index.html` gained a slim `<header class="app-mark">` above `.app-body` — a single-stroke roofline with a small dot at the apex (24×24, stroke 1.8, gold), reading as "a home, watched over" rather than a literal house pictogram. Deliberately its own row above each screen's topbar rather than injected into every route's topbar-right slot, so it never collides with the mode chip / +Add buttons already living there.
- **Bug caught while adding the header**: `.app-shell`/`.screen` were missing `min-height: 0`, a flexbox default that stops a `flex:1; overflow-y:auto` child from actually shrinking — without it the child's content pushed the whole `#app`/`body` taller than the viewport, causing a second, outer page-level scrollbar alongside `.screen`'s own intended scroll. Latent before this change (adding the header's extra height just tipped it over the threshold to be visible); fixed by adding `min-height:0` through the flex chain (`.app-shell` and `.screen`).

## What's actually built (Phase 0-3)

- `tokens.css`, `app.css` — full token set + shared primitives (list-row, stepper, sheet form fields, chip-group) used by every screen.
- `js/engine.js` — all six trigger types (`fixed_calendar` with a minimal RRULE subset — `FREQ=MONTHLY;BYMONTHDAY=N` and `FREQ=WEEKLY;BYDAY=XX` only, extend as real packs need richer recurrence; `floating_since_last`; `usage_meter` with velocity-based estimation; `condition` with a 1-day default cooldown; `seasonal`; `on_mode`), `stateOf`/`overdueDays` compare at **day granularity** (not exact timestamps — see comment in the file for why: condition/on_mode occurrences set `dueAt = now`, and a sub-day render delay was flickering them into "overdue by 0 days").
- `js/state.js` — in-memory store standing in for `db.js`; full CRUD for spaces/items/assets/people/routines, `regenerate()` re-runs the engine after any mutation that could unlock/lock occurrences (mode switch, routine add/edit/toggle) so mode-gated and mode-paused routines respond live; 5-second undo via a single-slot last-action snapshot (occurrence complete/snooze only, not CRUD — see limitations).
- `js/routes/today.js` — stat row (Overdue/Due today/This week/Completed this week — see Round 4 below; replaced the original House Line bar chart), Overdue/Due Today sections (only damaging/safety tiers render loud), effort-1 filter chip, pointer-based swipe gestures (threshold 90px).
- `js/routes/house.js` — spaces grid → space detail (routines/stock/assets for that space), add/rename/delete space with a reassign-or-delete-contents prompt (memo §4.4). Item/asset rows here are prune-only (trash icon); full edit lives in Stock/Assets.
- `js/routes/stock.js` — Out/Low/Expiring/OK sections, qty steppers, add/edit/delete item, "Build shopping list" copies out+low items to the clipboard (memo §7 routing, not ordering).
- `js/routes/assets.js` — cards sorted by next service due, warranty/service countdown copy, vendor `tel:` call link, "Mark serviced" (re-baselines `lastServicedAt`/meter so `usage_meter` routines start counting fresh), add/edit/delete.
- `js/routes/people.js` — member/help roster, add/edit/delete, add leave entries; reached via More → People (not a primary tab, per memo §8.1's 5-tab limit).
- `js/routes/routine.js` — the shared plain-language routine builder (memo §4.2), reused by House's "+ Add" and each routine row's edit; trigger-type chip swaps which param fields show, matching exactly what `engine.js` can execute (no UI for capabilities the engine doesn't have).
- `js/boot.js` — tab shell (Today/House/Stock/Insights/More) + a More menu for sub-screens; unbuilt screens render an honest "not built yet, Phase N" placeholder, never fake content.
- `mock-data/` — the original hand-seeded 2BHK household is now only the boot-time default (shown until onboarding is re-run); still useful as a stable, hand-tuned demo of all 6 trigger types with a realistic overdue/due mix.
- `packs/core.json`, `bath.json`, `laundry.json`, `help.json`, `appliances.json` — real content in memo §10's schema, priority order per the memo. `appliesWhen` supports `always`, `spaces` (matched against `impliedSpaceTypes()` — every home gets bath/bedroom/living/utility/entry/whole_home, `balcony` added only if "Balcony/garden" was picked), `householdHelp`, and `has`.
- `js/packs.js` — `loadPacks()` fetches the 5 JSON files; `generateFromArchetype(answers, packs)` filters by `appliesWhen`, dedupes spaces by type across packs, resolves each routine's `assetRef` (a pack-local asset name) to a real generated asset id. Verified live: switching "Household help" to "None" correctly drops `help.json`'s routines; all 5 packs merge into one coherent household with zero `engine.js` changes (the memo's own acceptance test).
- `js/routes/onboard.js` — six chip questions on one scrollable screen (not a multi-step wizard — matches the memo's "~40 seconds" intent without extra navigation), a generating skeleton (600ms), then a confirm screen grouped by room with a one-tap "Keep/Removed" bulk toggle per space (memo §3.1 Step 4). Confirming calls `state.seedHousehold()`, which replaces spaces/items/assets/routines and clears the ledger (fresh household, nothing done yet — everything reasonably starts "due today").
- Reachable via More → "Re-run onboarding" rather than gating app boot — there's no real "first run" concept yet without auth/persistence (Phase 4), and gating would make the hand-tuned demo household harder to get back to for continued testing.

## Global asset + item catalog (added 2026-08-03, user request)

User reported Assets couldn't be created/edited. Root cause: `assets.js` was fully built in Phase 2 but never wired into `boot.js`'s router — completely unreachable, not a logic bug. Fixed by adding it under More (same pattern as People), with a back button. **Lesson: after writing a new route file, grep `boot.js` to confirm it's actually mounted before calling the screen done** — Phase 2's own verification pass missed this because it never tried to reach Assets by anything other than House's read-only per-space list.

While fixing that, the user asked for a real catalog system: every asset/item should carry a stable key, typing a name should suggest existing catalog entries to select (never silently duplicate the same real-world thing under a new key), and each catalog entry should have its own distinct icon. Built as:

- `js/catalog.js` — master data, not household data. `ASSETS` (77 entries) and `ITEMS` (53 entries) each `{key, name, icon, category, ...defaults}`, curated across more than one climate/region on purpose (storage geysers + tankless heaters + US tank heaters; evaporative coolers + boilers + furnaces; inverters/UPS + generators + solar) — not just the Indian-apartment set the mock/packs use. `getOrCreate(name, type)` is the core primitive: exact-name match reuses the existing key, no match registers a new session-scoped custom entry (`AST-CUSTOM-N` / `ITM-CUSTOM-N`) so retyping that same name later finds it instead of minting another one. Custom entries are **session-only** (not persisted) — Phase 4 should move `custom` into Firestore as a real, growing per-household or global catalog.
- `js/ui/icons.js` — ~77 unique per-asset icons (one identity per entry, per the user's explicit ask) plus ~15 shared item-*category* icons (bath/laundry/cleaning/paper/pest/utility/electrical/fragrance/safety/garden/pet/vehicle/filters/admin/electronics-accessory — same "group by category" pattern the memo's own §9.4 icon table already uses, applied at catalog granularity since ~53 individual consumables would mostly look like indistinguishable bottles/packets at 16-18px anyway).
- `js/ui/components.js` — `catalogField`/`wireCatalogField`/`resolveCatalogField`: a name `text-input` + live suggestion dropdown (`.catalog-suggestions`). Selecting a suggestion fills the field, stamps `input.dataset.catalogKey`, and (in Stock/Assets' `onSelect` handlers, add-mode only) pre-fills unit/par-level/service-interval from the entry's defaults. Saving without selecting still resolves correctly — `resolveCatalogField` calls `getOrCreate`, which finds an exact-name match even if the user never clicked a suggestion.
- Wired into `stock.js` (add/edit item) and `assets.js` (add/edit asset) — both store `catalogKey` + `icon` on the record and show the key read-only in the edit sheet ("Catalog key: ITM-TOOTHPASTE"). `packs.js` and `state.js`'s initial mock-data load were **also** updated to resolve every pack-generated and hand-seeded item/asset through `getOrCreate`, so pre-loaded content is catalog-linked too, not just newly-created entries — this is also why `mock-data/assets.js`'s AC entry was renamed from "AC — bedroom" to "Split AC" (had to match the catalog's exact name to link correctly).
- **Round 2 (same session)**: user asked for much broader coverage (electronics, furniture, more kitchen appliances, vehicles — "look across the work across each continent and country") and for `openItemSheet`/`openAssetSheet` to be reachable with a pre-filled space directly from House's per-space detail view, not just from Stock/Assets. Catalog grew from 33/44 to 77/53 entries (added electronics & AV, furniture, more kitchen appliances, outdoor/garden equipment, vehicles, plus a few new item categories: dishwashing, more personal care, electronics accessories). `stock.js`'s `openItemSheet` and `assets.js`'s `openAssetSheet` were refactored from positional `(item)`/`(asset)` args to `({ item, defaultSpaceId })`/`({ asset, defaultSpaceId })` options objects and exported; `house.js` now imports both and wires "+ Add" buttons in the space detail's Stock/Assets sections (same pattern as the existing Routines "+ Add"), plus made item/asset rows there tap-to-edit (previously trash-only).
  - **Bug caught during this refactor**: `assets.js`'s `assetFormFields(asset, state)` helper wasn't passed `defaultSpaceId`, throwing `ReferenceError` and silently breaking the House "+ Add asset" button (sheet never opened). Fixed by threading `defaultSpaceId` through as a third argument. Caught immediately via console error checking, not left for the user to find — worth remembering that any helper function extracted from a route body needs its captured variables re-checked when the parent function's signature changes.
- Verified live: typing "clean" surfaces Toilet/All-purpose/Glass/Floor cleaner with correct distinct icons; selecting one auto-fills unit+par level; typing a brand-new name ("Shoe polish"), saving, then retyping the same name later correctly surfaces it as a suggestion instead of creating a second entry; adding an asset from House's Balcony detail correctly pre-filled Space to "Balcony" and surfaced both "Water softener" and "Sofa" for a "sof" search.

## Assets ↔ routines ↔ stock, properly linked (added 2026-08-03, same session)

User reported the Assets card's "Service due in N days" didn't update after editing the service interval, and asked "will that auto-create a routine?" — surfaced that assets/routines/stock were three disconnected systems. Answer was no; built the actual linkage:

- **Bug fix**: `state.js`'s `updateAsset`/`addAsset` now call a shared `computeNextServiceDue(asset)` (baselines from `lastServicedAt` if known, else today) whenever `serviceIntervalDays` changes — was a blind `Object.assign` before, so the card silently went stale.
- **suggestedRoutines on catalog assets**: ~45 of the 77 asset entries now carry a `suggestedRoutines` array (memo §4.2-shaped templates — title/trigger/effort/consequence/ownerClass, optional `requiresItemKeys`). Deliberately empty on entries with no real household chore (mobile phone, sofa, bookshelf, etc.) — memo's own "if it doesn't help tell you something before it becomes a problem, it doesn't belong" principle, applied to content curation this time. `assets.js`'s Add/Edit sheet shows these as pre-checked toggle chips (on catalog select when adding; via a "+ Add suggested routines" button when editing an existing catalog-linked asset) and creates the checked ones as real, `assetId`-linked routines on save — verified live, adding suggested routines to the mock Geyser created "Service geyser" and "Check pressure relief valve," both immediately visible on Today.
- **Naming-mismatch bug found while testing this**: the catalog's `"AST-GEYSER"` entry was named "Geyser (storage water heater)" while `mock-data/assets.js` and `packs/bath.json` both just say "Geyser" — `getOrCreate`'s exact-match lookup never found it, so the mock Geyser silently got a throwaway custom catalog entry with no icon/suggestedRoutines instead of the real one. Fixed by renaming the catalog entry to plain "Geyser" (also fixed "Boiler (central heating)" → "Boiler" preemptively, same risk). **Lesson: catalog entry `name` must exactly match whatever mock-data/pack JSON already uses, not just read well** — nothing type-checks this, only manual verification catches it (which is how this one was found — the "Add suggested routines" button was missing for the mock Geyser until the rename).
- **`js/roomTemplates.js`**: per-space-type suggestions for all 15 space types, generalizing the 5 onboarding packs so *any* space type gets a starting point, including ones no pack covers. `house.js`'s "Add space" flow follows a create-then-review pattern: space is created immediately, then (if the type has suggestions) a "Suggested for {name}" sheet opens — same confirm-don't-create chip-toggle pattern as onboarding, "Keep"/"Skip" per row. Verified live: adding a "study"-type "Home Office" correctly offered Desktop computer/Office chair/Printer/Printer paper/Ink cartridge/Dust desk routine; unchecking Office chair correctly excluded only that one from the 5 things created.
- **Round 3 (same session)**: user asked to (a) unify `roomTemplates.js` and `packs.json` rather than hand-duplicating the same content twice, (b) add robot vacuum to the catalog, (c) add pooja/entertainment/dining/garage/terrace space types with real content, (d) fix space-type chip labels showing raw `whole_home`-style keys with underscores.
  - **De-duplication**: `roomTemplates.js` was rewritten so pack-covered types (bath/bedroom/living/utility/entry/whole_home) now derive their suggestions **live** from `packs/*.json` — filtered by each pack entry's own `spaceType` field, resolved to catalog entries via `findExact`. Only the pack-uncovered types (balcony, study, storage, outside, plus the 5 new ones below) stay hand-authored in a much smaller `HAND_AUTHORED` map. `packs.js` gained a sync `getCachedPacks()` getter (returns `[]` before `loadPacks()` resolves); `boot.js` now fires `loadPacks()` at startup so it's warm by the time anyone opens "Add space." Verified live: a fresh "Guest Bathroom" (bath type) produced the exact same Geyser/Toothpaste/Toilet cleaner/.../Descale showerhead set as `bath.json` — confirming it's genuinely reading the pack, not a stale copy.
  - **New assets**: `AST-ROBOT-VACUUM` (own `robotVacuum` icon, distinct from the handheld `vacuum` icon) and `AST-GARAGE-DOOR-OPENER` (`garageDoorOpener` icon), each with suggestedRoutines.
  - **New space types**: pooja, entertainment, dining, garage, terrace — each with a new space icon (`pooja`/`entertainment`/`dining`/`garage`/`terrace` in `icons.js`) and a `HAND_AUTHORED` template (e.g. pooja → incense/candles + "Clean pooja room"/"Change flowers"; garage → car/garage-door-opener/lawn-mower + engine-oil/wiper-fluid + "Organize garage"). Also caught and fixed a **pre-existing gap** while auditing space-type coverage: `study`, `storage`, and `outside` had no space icons defined at all — `Icon()` was silently returning an empty string for their House rows. Added all three.
  - **Chip-label fix**: `house.js`'s `SPACE_TYPES` was a flat array of raw type strings (`"whole_home"`), so `chipGroup()` rendered the literal underscored key as the button label for every type, not just whole_home — just less noticeable on ones like `"bath"` that don't contain an underscore. Converted to `{value, label}` pairs with real display text for all 15 types. The underlying stored `type` value (e.g. `"whole_home"`) is unchanged — only what's shown to the user changed, so no data migration was needed.
- **Routines consume stock**: `requiresItemIds` (already in the memo's schema, previously unused) now has a UI — routine.js's builder gets a "Uses this stock (optional)" multi-select. `state.js`'s `completeOccurrence` decrements each linked item by 1 unit on completion (simplification — same amount regardless of unit type; not restored by the 5s undo, only the occurrence/ledger are). Verified live: completing "Deep clean bathroom" (mock-seeded with Toilet cleaner linked) dropped its qty from 2 to 1 and correctly reclassified it into the Low bucket.
- **Item consumption rate + early warning**: items carry `burnRate` (units/day, optional) and a new `lastRestockedAt` (set whenever qty increases — `adjustItemQty`, `updateItem`, `addItem` all touch it now). `stock.js` computes `projectedDaysLeft = qty / burnRate` and shows "~N days left" on every row; if that projection is ≤3 days, the item now lands in the Low bucket **even if the raw quantity hasn't crossed par level yet** — a light, honest precursor to memo §5.2's burn-rate learning (this is static, not EWMA-adapted; that's still genuinely Phase 5). Verified live against the mock household's existing `defaultBurnRate` values without needing to add any manually (Toothpaste ~83 days, Handwash refill ~10 days, Detergent ~15 days, etc., all showed correctly on first load).

## Round 4 — stat row, rate periods, custom icons, template routine cascade (added 2026-08-03, same session)

User feedback after the git push: House Line "confusing... suggest something more important"; consumption rate should accept /day, /week, /month not just /day; unmatched catalog entries should get their own icon instead of borrowing warranty's/stock's; room-template-added assets weren't bringing their `suggestedRoutines` along. Asked via `AskUserQuestion` which House Line replacement to build (Stat row / Week strip / One-line summary) — user didn't pick one, only added "do completed this week as well in this," so went with the recommended Stat row plus a 4th tile to satisfy that note. Also asked whether adding a duplicate asset to the same room should warn first — user chose **no warning, add freely**, confirming the existing silent-allow behavior needs no change.

- **`today.js`**: `houseLineHtml` replaced with `statRowHtml` — 4 tiles (Overdue / Due today / This week / Completed this week), 2x2 grid under 600px, one row from 600px up (`.stat-row`/`.stat-tile` in `app.css`). Overdue/Due today tiles tap-scroll (`scrollIntoView`) to their existing section below (`#section-overdue`/`#section-due`); This week/Completed are counts only, no matching list to jump to. "This week" = open occurrences due in the next 0-6 days (deliberately overlaps with "Due today," these are different lenses on the same data, not a mutually-exclusive partition). "Completed this week" reads `state.ledger` entries with `doneAt` in the last 0-6 days.
- **`stock.js`**: consumption-rate field gained a `/day` `/week` `/month` chip group next to the number input. `burnRate` is still always stored per-day internally (`projectedDaysLeft` etc. depend on that) — the period chips are pure entry/display convenience, converted via `toPerDay`/`fromPerDay` (day=1, week=7, month=30). Switching the period chip live-recomputes the displayed number so it doesn't silently change what gets saved.
- **`icons.js`/`catalog.js`**: added `customAsset`/`customItem` icons (dashed-outline box/tag with a small dot — reads as "unidentified," same 24×24/1.8-stroke technique as the rest of the set) and pointed `getOrCreate`'s fallback at them instead of reusing `warranty`/`stock`, which are real icons used elsewhere and looked misleading on a made-up entry.
- **`house.js`**: `openRoomTemplateSheet`'s asset-adding loop now captures `addAsset`'s return value and, for each kept asset, also creates its catalog entry's `suggestedRoutines` (same `requiresItemKeys`→`requiresItemIds` resolution as `assets.js`'s standalone flow), auto-added rather than exposed as a second layer of toggles. This resolves the "Known limitations" entry below about room templates not cascading — removed that entry since it no longer applies. Verified live: adding a "Guest Bathroom" space correctly created a linked Geyser asset plus its own "Service geyser"/"Check pressure relief valve" routines, alongside the pack-level "Descale showerhead and taps"/"Deep clean bathroom" template routines — all 4 assetId/spaceId-correct.

## Known limitations to revisit, not bugs

- `items.status`/`projectedOutAt` are static mock values, not computed — real burn-rate learning (memo §5.2) is Phase 5.
- No batching (§5.6), load smoothing (§5.7), or seasonal city map (§5.8) yet — seasonal routines just fire at raw month-window start.
- Long-press-to-reassign (memo §8.2) is not implemented — intentionally deferred rather than stubbed with a fake action.
- Deletes (space/item/asset/person/routine) use a native `confirm()` and are immediate/permanent — memo §4.4's 30-day soft-delete + "Recently removed" recovery isn't built (that's a Settings/More feature, later phase). `confirm()` satisfies §8.3's "no modal blocks anything except destructive confirmation" for now.
- `routine.js`'s condition trigger UI only supports "when this item runs out" (`op: eq, value: "out"`) — matches the only case `engine.js` needs today; extend both together if a pack needs a richer condition.
- `.tabbar` must stay nested **inside** `.app-body` in `index.html` — briefly wasn't, which silently broke the layout. No longer breakpoint-dependent since the sidebar was dropped (see locked decisions), but keep it nested regardless.
- Catalog entry `name` strings must exactly match mock-data/pack JSON names for the linkage to resolve (see the Geyser bug above) — nothing enforces this automatically. If a future pack or mock fixture references a catalog asset/item by name, double-check the exact string against `js/catalog.js` rather than assuming it'll match.
- Routine-consumes-item decrements are a flat 1 unit regardless of item unit (piece/ml/kg) and aren't restored by the 5s undo — only the occurrence/ledger snapshot is. Fine for "piece"-like consumables (toilet cleaner, detergent scoop), less meaningful for something like a 500ml handwash bottle.
- `roomTemplates.js`'s pack-derived suggestions depend on `loadPacks()` having resolved (`boot.js` fires it at startup, cached via `packs.js`'s `getCachedPacks()`). In practice this is near-instant for 5 small local JSON files, but if someone opened "Add space" in the first few milliseconds after page load, pack-covered types (bath/bedroom/living/utility/entry/whole_home) could show fewer suggestions than usual. Not defended against — low enough risk to leave as-is for now.
- Catalog custom entries (`js/catalog.js`'s `custom` array) live only for the tab's lifetime — reload and they're gone, so a freshly-typed "Shoe polish" won't survive a page refresh yet. Needs Firestore in Phase 4.
- After adding any new route file, **grep `boot.js` to confirm it's actually mounted** — `assets.js` sat fully built and completely unreachable for an entire phase before this was caught. Don't trust "the file exists and looks right" as done. (Both `wishlist.js` and `insights.js` were confirmed reachable and tested live before being called done this round.)
- Wishlist has no photo/attachment field — memo doesn't call for one and it wasn't asked for, but "what to look for" living only in the free-text Notes field is a real limitation if this grows.
- Habits can't be backfilled — `toggleHabitToday()` only ever touches today, by design (matches the "no gaming the system" spirit already applied to routine completion), but there's genuinely no way to log "I did this yesterday but forgot to check it" after the fact. Revisit if that turns out to matter more than the anti-gaming principle.
- Insights' health-score weights (`TIER_PENALTY`, the 40/30/30 caps in `computeHealth()`) are hand-picked constants for a reasonable-looking first score, not a calibrated/learned formula — same "placeholder scale, not memo §5.5" caveat the old House Line weighting had.
- "Help on leave" only surfaces the conflict (which routines fall due during a helper's leave) — it doesn't suggest or perform a reassignment. Memo §6.3 frames this as the hero feature; an actual reassign-to-X action is a reasonable next step once there's a good UI spot for it.
- Wishlist/Habits are exactly as session-only as everything else pre-Firebase (Phase 4) — nothing new here, just flagging since they're brand new collections this round.
- Wishlist checklist sub-items can't be renamed/re-kinded once added — only toggled done or deleted. Fine for the "quick checklist" use case this was built for; revisit if projects turn out to need heavier editing.
- A "weekly_count" habit (e.g. "gym 3x/week") is due on *any* day until that week's quota is hit, with no specific day it nags harder on — matches how such goals are usually thought about ("3 times this week," not "Mon/Wed/Fri specifically"), but means it could show up as due every remaining day of the week if left alone, not just once.
- Habit "Custom days" frequency stores `days` as JS `Date.getDay()` numbers (0=Sun..6=Sat) — chip values round-trip as strings through the DOM (`readChipGroup`), converted back to numbers at save time in `buildHabitFrequency()`. If this data model is touched again, keep that numbering convention rather than switching to ISO's Mon=1 without updating both `routine.js` and `state.js`'s `isHabitDueToday`.
- Tasks have no recurrence, effort, or consequence tier by design (that's what separates them from Routines) — a task that should repeat has to be manually re-added after completion. If recurring one-off reminders turn out to be a real need, that's a genuinely new shape, not a small extension of the current Task model.
- The member filter on Today doesn't extend to Wishlist or Insights — it was asked for and built specifically for Today's Overdue/Due lists. Revisit if per-person filtering turns out to matter on those other screens too.
- `.named`'s Fraunces-italic styling was removed per explicit dislike, but the font import and class name remain — a future "special occasion" surface could reuse both without re-plumbing, but until then `.named` is just semibold, so the name reads as more ceremonial than it currently is.
- Multi-house filtering (Round 17) doesn't reach Insights' Phase 5 intelligence sections (snooze suggestions, load balancing, batching, seasonal, forecast) or Wishlist — those still read/act across the whole household regardless of which house(s) are active in the header picker. Deliberate scope boundary for this round, not an oversight; revisit if multi-house households actually start relying on them. Habits and habit-scoped Tasks (no `spaceId`) are correctly unaffected by house filtering by design — they're person-scoped, not house-scoped.
- `js/migrations.js` has run through four real steps now (v1→v2 houses, v2→v3 notify settings, v3→v4 smoothing mode) without needing to change the mechanism itself — each new schema change has just been a new numbered function + a version bump. Still worth double-checking step ordering assumptions if a future migration needs to run relative to fields an *earlier* migration touched.
- Undoing a wishlist checklist sub-item never deletes the real Stock/Assets record an asset/item "Buy" already created — only the checklist's own done flag reverts. Re-clicking "Buy" after an undo would create a second real record rather than reusing the first. Acceptable for the "oops, wrong tap" case this was built for; revisit if it turns out people undo-then-rebuy on purpose.
- Today's "This week" view buckets by raw due-date offset, independent of each routine's own trigger window — this means a routine can appear in "This week" a few days before it would ever show up in the plain "Today" view (by design, see Round 13), so the two views' row counts for the same date range won't always match the engine's own due/pending state one-for-one. That's intentional, not a bug.
- Load balancing (§5.5) computes a single rolling-30-day snapshot, not the memo's literal "persists for 2 consecutive weeks before surfacing" — there's no cross-session weekly-aggregate storage pre-Firebase to check persistence against. Revisit once Phase 4 gives it somewhere to remember last week's number.
- Consumable coupling (§5.9) is informational only, and doesn't model the memo's literal "RO membrane" example — the catalog has "RO filter cartridge," not a separately-tracked membrane — so the signal is "a water-treatment asset was serviced recently, here's what's nearby that might need attention," not a precise per-part coupling.
- Seasonal intelligence (§5.8) only has a real season map for Bengaluru, matching the only city the memo itself specifies data for. Any other household's `city` value gets no seasonal card at all rather than a guessed map.
- The snooze-learning day-of-week signal (§5.4) needs real history for a specific routine to have both several logged snoozes *and* several ledger completions — genuinely thin for a fresh household. The seeded demo (`mock-data/snoozeLog.js` + one ledger entry) is close but not perfectly reliable at demonstrating it, since one of the two hardcoded historical completion dates happens to land on a weekday and dilutes the pattern — the underlying detection logic is correct (verified directly), just don't expect the mock data alone to visibly trigger it on every load.
- Load smoothing (§5.7) runs once per occurrence lifecycle (the `occ.smoothed` guard) and nothing currently un-sets that flag — an occurrence that gets snoozed-then-woken-then-regenerated fresh after actual completion starts unsmoothed again as expected, but there's no path back to "eligible to smooth again" for the same still-open occurrence if the household's effort ceiling changes mid-week. Low-risk for a single-session mock app; revisit if this becomes a real multi-week persisted household.
- The service worker (`sw.js`) is network-first for a deliberate reason (see Round 14) — don't change it back to cache-first without re-reading why, since that exact choice silently broke live verification once already.
- The welcome gate's `kasa_intro_done` localStorage flag is not a real "already onboarded" check — it's "has this browser seen the intro." Combined with state being in-memory-only pre-Firebase, a returning browser skips straight to the app but with the household reset to the original mock demo, not whatever was actually created last time. Once Phase 4 lands, this flag should be replaced with a real auth/household check.
- "Join a household" on the welcome screen is a real input with a stub action — it always explains that joining needs Firebase and points at "Create a new one" instead, regardless of what code is typed. Nothing validates the code's shape beyond 6 chars/uppercase; there's no backend yet to validate against.
- The Utility-mandatory guard (`deleteSpace()`) assumes exactly one space has `type: "utility"` — `requiresItemsFieldHtml()`'s `state.spaces.find(s => s.type === "utility")` picks whichever one it finds first if a household somehow ends up with more than one (nothing currently prevents *adding* a second Utility-type space, only deleting the existing one). Low-risk edge case, not defended against.
- The household `code` generated by `generateHouseholdCode()` isn't looked up or validated anywhere — it's purely displayed (toast + More sheet) so the shape of the real Miso-style `households/{code}` pattern exists before Phase 4's Firestore backend makes it actually functional.
- Welcome's "Skip" household option isn't actually distinguishable from any other household this device might have — it just lands on whatever's already in `state` (the mock demo, freshly coded). The user's own description ("which if you join another household gets removed") implies a real join should later *replace* a skip-created household rather than coexist with it; there's no way to implement "replace" meaningfully without a real backend to replace *from*, so this is deferred to Phase 4, not built as a half-measure now.
- A member's `email` field is required-on-save but not validated for actually being a valid email shape — any non-empty string passes. Real validation (and real verification) is a Phase 4 concern once sign-in is real.
- People & Household's "Join different" sheet and welcome.js's own join step are two separate, near-identical implementations (not shared code) — both real as of Round 20, deliberately not unified into one shared function since they have different pre-conditions (welcome.js's has no existing household to tear down first; people.js's does).

## Next session

**Phase 4 (Firebase) is done** as of Round 18, 2026-08-06 — Google sign-in + Firestore persistence, see the Locked decisions table and Round 18 for the full shape. Real-time multi-device sync, mid-session household switching, and Firestore offline persistence were all added in Round 20. Not yet done, all optional/deferred rather than blocked:
- Apple sign-in and email-link auth (user: "later"/"not needed" — revisit only if asked).
- Cloud Storage and App Check — not configured; needed before Phase 6's AI half (photo sweep, bill scan) can be built for real.
- A genuine field-level merge for real-time sync — current sync is whole-document last-write-wins (see Locked decisions); fine unless two devices are found to actually edit at the same moment in practice.

**Phase 5 (Intelligence) is done** as of Round 14, except §5.2 (burn-rate learning) and §5.3 (adaptive intervals), which the user explicitly said never to build — see the Locked decisions table if that instruction ever needs revisiting.

**Phase 7's performance pass is done** as of Round 20 — 200-routine Today render measured at 13.3ms (well under the 400ms ceiling), no hardcoded-hex-outside-tokens.css confirmed, Firestore offline persistence added for the airplane-mode test. Only gap: no physical airplane-mode test with real network cutoff was run (no tool available to reliably flip that mid-session) — worth a real on-device pass when convenient.

What's left:
- **Phase 7's desktop three-pane layout** (`>1024px`) — deliberately deferred (user's explicit call), pure CSS/layout work whenever it's picked back up.
- **Phase 6's AI half** (photo sweep, bill scan, weekly AI digest, NL routine creation) — now unblocked on auth/Firestore, but still needs a Cloud Function proxy (memo §5.10's own no-client-side-LLM rule) and Storage (for photo uploads), neither built yet.
- **Real background push notifications** (Round 19, `js/notify.js` is on-device/foreground-only for now) — needs Firebase Cloud Messaging + a scheduled Cloud Function (to actually send at the chosen time regardless of whether anyone has the app open) + upgrading the project to the Blaze billing plan. Worth bundling with Phase 6's Cloud Function proxy work, since both need the same backend piece. Once the Capacitor wrap happens, the same Cloud Function also covers real Android/iOS push (Capacitor's push plugin registers with this same Firebase project).
- The §5.2/§5.3 exclusion itself, only if the user changes their mind on it later.
